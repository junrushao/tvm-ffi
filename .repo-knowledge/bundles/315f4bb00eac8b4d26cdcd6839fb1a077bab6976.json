{
  "sha": "315f4bb00eac8b4d26cdcd6839fb1a077bab6976",
  "sha7": "315f4bb",
  "author_name": "Ruihang Lai",
  "author_email": "ruihangl@cs.cmu.edu",
  "date_iso": "2025-09-10T00:29:02-04:00",
  "subject": "[FFI] Fix system library symbol lookup (#18298)",
  "body": "",
  "is_merge": false,
  "parents": [
    "8068d1df64275452ced5d7a960a4468f663f50de"
  ],
  "name_status": [
    [
      "M",
      "src/ffi/extra/library_module_system_lib.cc"
    ]
  ],
  "numstat": [
    {
      "path": "src/ffi/extra/library_module_system_lib.cc",
      "additions": 14,
      "deletions": 1
    }
  ],
  "diff_truncated": false,
  "diff":
      "diff --git a/src/ffi/extra/library_module_system_lib.cc b/src/ffi/extra/library_module_system_lib.cc\nindex e93c660..9d077fe 100644\n--- a/src/ffi/extra/library_module_system_lib.cc\n+++ b/src/ffi/extra/library_module_system_lib.cc\n@@ -69,12 +69,25 @@ class SystemLibrary final : public Library {\n   explicit SystemLibrary(const String& symbol_prefix) : symbol_prefix_(symbol_prefix) {}\n \n   void* GetSymbol(const String& name) final {\n+    // The `name` might or might not already contain the symbol prefix.\n+    // Therefore, we check both with and without the prefix.\n     String name_with_prefix = symbol_prefix_ + name;\n-    return reg_->GetSymbol(name_with_prefix);\n+    void* symbol = reg_->GetSymbol(name_with_prefix);\n+    if (symbol != nullptr) {\n+      return symbol;\n+    }\n+    return reg_->GetSymbol(name);\n   }\n \n   void* GetSymbolWithSymbolPrefix(const String& name) final {\n+    // The `name` might or might not already contain the symbol prefix.\n+    // Therefore, we check both with and without the prefix.\n     String name_with_prefix = symbol::tvm_ffi_symbol_prefix + symbol_prefix_ + name;\n+    void* symbol = reg_->GetSymbol(name_with_prefix);\n+    if (symbol != nullptr) {\n+      return symbol;\n+    }\n+    name_with_prefix = symbol::tvm_ffi_symbol_prefix + name;\n     return reg_->GetSymbol(name_with_prefix);\n   }\n ",
  "heuristics": {
    "tags": [],
    "extensions": [
      ".cc"
    ],
    "scopes_guess": [
      "src"
    ]
  }
}
