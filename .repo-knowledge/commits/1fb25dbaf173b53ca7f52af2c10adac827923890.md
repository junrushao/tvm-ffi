---
sha: "1fb25dbaf173b53ca7f52af2c10adac827923890"
date: "2025-10-17T21:09:27-04:00"
author: "Tianqi Chen"
subject: "[DOCS] Polish quickstart (#168)"
nature: ["docs"]
tags: ["build"]
scope: ["docs", "examples"]
risk: "low"
---

# 1fb25db — [DOCS] Polish quickstart (#168)

## TL;DR
- Further polishes `docs/get_started/quickstart.rst` (+162/-64): renames example files from `main.cc/libmain.so` to `add_one_cpu.cc/add_one_cpu.so` and `add_one_cuda.cu/add_one_cuda.so` for consistency with the actual quick_start example structure.
- Fixes the CMake `execute_process` command from `python -m tvm-ffi-config` to `python -m tvm_ffi.config` (correct module path).
- Adds `set_target_properties(... PREFIX "" SUFFIX ".so")` to CMake examples for clean library naming.
- Adds C++ bundle-with-export example using `Function::InvokeExternC`.
- Adds JAX code example using `jax-tvm-ffi` integration.
- Fixes the quick_start `CMakeLists.txt` to use `python -m tvm_ffi.config --cmakedir`.

## Why (intent / motivation)
- The quickstart guide from the previous commit used inconsistent file names and incorrect CMake invocations that would fail when copy-pasted.
- Adding the JAX tab and bundle example makes the guide more complete.

## What changed (facts from diff)
- `docs/get_started/quickstart.rst`: all `main.cc`/`libmain.so` references renamed; CMake module path fixed; JAX tab added; C++ bundling hint added; Rust example updated with typed function call; prose improvements throughout.
- `examples/quick_start/CMakeLists.txt`: added `execute_process` block to query `tvm_ffi.config --cmakedir` and set `tvm_ffi_ROOT`.

## Public surface changes (if any)
- API: none.
- Flags/config: `examples/quick_start/CMakeLists.txt` now auto-detects tvm_ffi CMake dir via Python.
- Data formats/schemas: none.

## Tests & verification
- Added/updated tests: none.
- How to verify manually: Follow the quickstart guide verbatim; confirm CMake commands succeed.
- CI impact: none.

## Risk & rollout notes
- Risk level: low — docs and example CMake fix.
- Rollout/migration: none.
- Follow-ups: none.

## Evidence
### Changed files
- `docs/get_started/quickstart.rst` +162/-64 (modified)
- `examples/quick_start/CMakeLists.txt` +7/-1 (modified)

### Notable symbols / endpoints / configs touched
- `tvm_ffi.config --cmakedir` (CMake discovery command)
- `Function::InvokeExternC` (documented in bundling hint)
- `jax_tvm_ffi.register_ffi_target` (JAX example)

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/1fb25dbaf173b53ca7f52af2c10adac827923890.md`
