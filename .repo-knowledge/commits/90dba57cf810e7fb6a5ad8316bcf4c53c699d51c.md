---
sha: "90dba57cf810e7fb6a5ad8316bcf4c53c699d51c"
date: "2025-09-23T16:01:42-07:00"
author: "Junru Shao"
subject: "fix(Array): Make array slicing behavior to return list (#50)"
nature: ["fix"]
tags: []
scope: ["python", "tests"]
risk: "low"
---

# 90dba57 — fix(Array): Make array slicing behavior to return list (#50)

## TL;DR
- Reverts the slice return type of `Array.__getitem__` back to `list[T]` (was changed to `Array[T]` in PR #37)
- Updates the `@overload` signature accordingly
- Adds an explicit assertion in existing tests confirming slices return `list`

## Why (intent / motivation)
- Existing TVM stack consumers rely on `Array[start:stop]` returning `list[T]`
- PR #37 inadvertently changed this to `Array[T]`, breaking downstream tests

## What changed (facts from diff)
- `container.py`: `__getitem__` overload for `slice` changed from `Array[T]` to `list[T]`; implementation simplified by removing the `isinstance(result, list)` branch that was wrapping in `Array`
- `tests/python/test_container.py`: `assert isinstance(a_slice, list)` added with comment explaining the behaviour

## Public surface changes (if any)
- API: `Array[start:stop]` returns `list[T]` — restored to original contract
- Flags/config: none
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: `tests/python/test_container.py` (assertion on slice type)
- How to verify manually: `uv run pytest -vvs tests/python/test_container.py::test_array`
- CI impact: none

## Risk & rollout notes
- Risk level: low — restores prior behaviour; no new semantics
- Rollout/migration: none
- Follow-ups: none

## Evidence
### Changed files
- `python/tvm_ffi/container.py` +3/-5 (M)
- `tests/python/test_container.py` +1/-0 (M)

### Notable symbols / endpoints / configs touched
- `Array.__getitem__` overloads in `container.py`

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/90dba57cf810e7fb6a5ad8316bcf4c53c699d51c.md`
