---
sha: "ef6d57b8debeb6630da726444efa85058062be74"
date: "2025-09-13T16:10:41-04:00"
author: "tqchen"
subject: "[CI] Setup initial CI"
nature: ["ci"]
tags: ["github-actions", "lint", "pre-commit", "wheel-publish"]
scope: [".github", "tests", "pyproject.toml", "python"]
risk: "low"
---

# ef6d57b — [CI] Setup initial CI

## TL;DR
- Adds GitHub Actions workflows: `ci_test.yml` (lint + test matrix across Linux x86/arm64, macOS arm64, Windows) and `publish_wheel.yml` (cibuildwheel-based wheel publishing to PyPI).
- Adds reusable actions `detect-env-vars` (CPU count detection) and `detect-skip-ci` (`[bypass ci]` and docs-only skip detection).
- Adds `.pre-commit-config.yaml` with standard pre-commit hooks.
- Adds lint scripts: `tests/lint/check_asf_header.py`, `tests/lint/check_file_type.py`, `tests/lint/git-clang-format.sh`, `tests/scripts/task_lint.sh`.
- Updates `pyproject.toml` black exclude list and adds initial ruff/isort config.

## Why (intent / motivation)
- The newly established standalone `apache/tvm-ffi` repository needed a complete CI infrastructure from scratch.

## What changed (facts from diff)
- `.github/actions/detect-env-vars/action.yml`: Detects CPU count for parallel builds.
- `.github/actions/detect-skip-ci/action.yaml`: Detects `[bypass ci]` in commit message and docs-only change sets.
- `.github/workflows/ci_test.yml`: Runs lint then test matrix (C++ tests, Python tests) on 4 platforms.
- `.github/workflows/publish_wheel.yml`: Builds wheels via cibuildwheel and publishes to PyPI on manual dispatch.
- `.pre-commit-config.yaml`: Standard pre-commit hooks (trailing whitespace, merge conflict, etc.).
- `tests/lint/check_asf_header.py`: Checks/fixes Apache 2.0 license headers in all git-tracked files.
- `tests/lint/check_file_type.py`: Validates that only allowed file types are committed.
- `tests/lint/git-clang-format.sh`: Runs clang-format-15 on C++ files.
- `tests/scripts/task_lint.sh`: Orchestrates all lint checks (file type, ASF header, isort, black, ruff, clang-format).

## Public surface changes (if any)
- API: none
- Flags/config: `.pre-commit-config.yaml` added; new ruff/isort sections in `pyproject.toml`
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: `tests/lint/` (lint scripts)
- How to verify manually: `pre-commit run --all-files`
- CI impact: establishes CI for all PRs

## Risk & rollout notes
- Risk level: low — CI infrastructure addition only.
- Rollout/migration: Contributors should install pre-commit: `pip install pre-commit && pre-commit install`.
- Follow-ups: none

## Evidence
### Changed files
- `.github/actions/detect-env-vars/action.yml` +40/-0 (A)
- `.github/actions/detect-skip-ci/action.yaml` +121/-0 (A)
- `.github/workflows/ci_test.yml` +134/-0 (A)
- `.github/workflows/publish_wheel.yml` +129/-0 (A)
- `.pre-commit-config.yaml` +30/-0 (A)
- `tests/lint/check_asf_header.py` +437/-0 (A)
- `tests/lint/check_file_type.py` +232/-0 (A)
- `tests/lint/git-clang-format.sh` +95/-0 (A)
- `tests/scripts/task_lint.sh` +41/-0 (A)

### Notable symbols / endpoints / configs touched
- `detect-skip-ci` GitHub Action: detects `[bypass ci]`
- `publish_wheel.yml`: cibuildwheel v3.1.4, PyPA gh-action-pypi-publish
- `.pre-commit-config.yaml`: hooks v5.0.0

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/ef6d57b8debeb6630da726444efa85058062be74.md`
