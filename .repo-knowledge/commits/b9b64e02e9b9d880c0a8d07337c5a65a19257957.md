---
sha: "b9b64e02e9b9d880c0a8d07337c5a65a19257957"
date: "2025-10-12T12:32:57-07:00"
author: "Junru Shao"
subject: "chore(build): Add CMake as build dependency (#104)"
nature: ["build"]
tags: ["deps", "chore"]
scope: ["pyproject.toml"]
risk: "low"
---

# b9b64e0 — chore(build): Add CMake as build dependency (#104)

## TL;DR
- Adds `cmake>=3.18` to `pyproject.toml` `[build-system] requires`.
- CMake is available on PyPI; specifying it as a build dependency means users no longer need to install CMake separately.

## Why (intent / motivation)
- Simplifies the developer and CI setup by removing CMake as a manual prerequisite; `pip install` / `uv pip install -e .` now automatically provides the required CMake version.

## What changed (facts from diff)
- `pyproject.toml`: `requires = ["scikit-build-core>=0.10.0", "cython"]` becomes `requires = ["scikit-build-core>=0.10.0", "cython", "cmake>=3.18"]`.

## Public surface changes (if any)
- API: none
- Flags/config: `cmake>=3.18` added to build-system requirements.
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: none
- How to verify manually: `uv pip install --force-reinstall -e .` on a machine without system CMake.
- CI impact: none

## Risk & rollout notes
- Risk level: low — only affects build dependency resolution
- Rollout/migration: none
- Follow-ups: none

## Evidence
### Changed files
- `pyproject.toml` +1/-1 (modified)

### Notable symbols / endpoints / configs touched
- `[build-system] requires` in `pyproject.toml`

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/b9b64e02e9b9d880c0a8d07337c5a65a19257957.md`
