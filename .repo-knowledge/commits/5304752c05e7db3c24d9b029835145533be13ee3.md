---
sha: "5304752c05e7db3c24d9b029835145533be13ee3"
date: "2025-09-13T17:05:56-04:00"
author: "tqchen"
subject: "[LINT] Run lint to be complaint with the new CI"
nature: ["chore"]
tags: ["lint", "isort", "black", "ruff", "style"]
scope: ["python", "tests", "examples", "docs", "pyproject.toml"]
risk: "low"
---

# 5304752 — [LINT] Run lint to be complaint with the new CI

## TL;DR
- Runs isort, black, and ruff across all Python files to comply with the newly added CI lint configuration.
- Adds `[tool.ruff]` configuration to `pyproject.toml`.
- Adds `isort check` and `ruff check` steps to `tests/scripts/task_lint.sh`.
- No logic changes; purely formatting and import reordering.

## Why (intent / motivation)
- After the CI setup commit, all existing Python files needed to pass the newly configured linters.

## What changed (facts from diff)
- `pyproject.toml`: Adds `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.ruff.lint.per-file-ignores]`, and `[tool.ruff.lint.pylint]` sections.
- `tests/scripts/task_lint.sh`: Adds `isort --check --diff .` and `ruff check --diff .` steps.
- Python source files: import reordering (alphabetical), trailing blank line removal, minor style fixes (no logic changes).
- `tests/python/test_device.py`: Renamed `test_device` parametrize target to `test_device_dlpack_device_type` to avoid name collision; removed unused `dev = ` assignments.
- `python/tvm_ffi/_tensor.py`: Collapsed `__all__` to single line.
- `tests/python/test_load_inline.py`: Build directory changed from `./build_add_one` to `./build/build_add_one`.

## Public surface changes (if any)
- API: none
- Flags/config: ruff config added to `pyproject.toml`; lint script updated
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: none (test files reformatted only)
- How to verify manually: `pre-commit run --all-files`
- CI impact: makes existing code pass the new lint CI

## Risk & rollout notes
- Risk level: low — no logic changes.
- Rollout/migration: none
- Follow-ups: none

## Evidence
### Changed files
- `pyproject.toml` +12/-0 (M) — ruff config
- `tests/scripts/task_lint.sh` +7/-1 (M) — add isort and ruff steps
- ~20 Python files reformatted (imports reordered, trailing whitespace removed)

### Notable symbols / endpoints / configs touched
- `[tool.ruff]` config section in `pyproject.toml`
- `isort`, `ruff check` in lint script

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/5304752c05e7db3c24d9b029835145533be13ee3.md`
