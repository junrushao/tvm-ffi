---
sha: "4a8a0b01f45f726147cbf75742d6b772eceab369"
date: "2026-01-07T10:28:42+08:00"
author: "Guan-Ming (Wesley) Chiu"
subject: "chore: replace C-style cast with static_cast in String::substr (#386)"
nature: ["chore"]
tags: []
scope: ["include"]
risk: "low"
---

# 4a8a0b0 — chore: replace C-style cast with static_cast in String::substr (#386)

## TL;DR
- Fixes a clang-tidy `google-readability-casting` violation introduced in the previous commit
- Replaces `size_t(-1)` default parameter with `npos` in `String::substr`
- One-line change; no behavioral difference

## Why (intent / motivation)
- The CI clang-tidy job on the main branch failed after merging #383 because the `size_t(-1)` C-style cast in the default parameter of `String::substr` triggered `google-readability-casting`
- The existing `String::npos` constant (introduced in #383) is the correct idiomatic replacement

## What changed (facts from diff)
- `include/tvm/ffi/string.h`: Changed default parameter of `String::substr` from `size_t count = size_t(-1)` to `size_t count = npos`

## Public surface changes (if any)
- API: Signature of `String::substr` is unchanged in behavior; only the spelling of the default argument changes
- Flags/config: none
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: none
- How to verify manually: `pre-commit run clang-tidy --all-files`
- CI impact: Fixes clang-tidy failure on main

## Risk & rollout notes
- Risk level: low — purely cosmetic fix; identical runtime behavior
- Rollout/migration: none
- Follow-ups: none

## Evidence
### Changed files
- `include/tvm/ffi/string.h` +1/-1 (modified)

### Notable symbols / endpoints / configs touched
- `String::substr` (default parameter spelling)

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/4a8a0b01f45f726147cbf75742d6b772eceab369.md`
