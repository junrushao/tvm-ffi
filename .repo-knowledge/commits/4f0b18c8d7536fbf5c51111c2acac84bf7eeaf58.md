---
sha: "4f0b18c8d7536fbf5c51111c2acac84bf7eeaf58"
date: "2025-09-20T12:45:03-07:00"
author: "Junru Shao"
subject: "feat: Use uv-based workflow for doc building (#34)"
nature: ["build"]
tags: ["docs"]
scope: ["docs", "pyproject.toml"]
risk: "low"
---

# 4f0b18c — feat: Use uv-based workflow for doc building (#34)

## TL;DR
- Replaces the `docs/requirements.txt` + `docs/Makefile` approach with a `uv`-managed `[dependency-groups].docs` section in `pyproject.toml`.
- Deletes `docs/Makefile` and `docs/requirements.txt`.
- Updates `docs/README.md` with new build instructions using `uv run --group docs sphinx-autobuild ...`.

## Why (intent / motivation)
- The old `requirements.txt`-based workflow was ad-hoc and inconsistent with the rest of the project's `uv`-based tooling. Moving to a `[dependency-groups]` section makes doc dependencies version-controlled and the build command a one-liner.

## What changed (facts from diff)
- `docs/Makefile` deleted (41 lines removed).
- `docs/requirements.txt` deleted (21 lines removed).
- `docs/README.md` rewritten with new uv-based workflow instructions.
- `pyproject.toml`: Added `[dependency-groups].docs` with all 21 doc dependencies; removed blank lines.

## Public surface changes (if any)
- API: none
- Flags/config: `pyproject.toml` gains `[dependency-groups].docs`
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: none
- How to verify manually: `uv run --group docs sphinx-autobuild docs docs/_build/html --ignore docs/reference/cpp/generated`
- CI impact: none

## Risk & rollout notes
- Risk level: low — build tooling change; old `make livehtml` no longer works
- Rollout/migration: Use `uv run --group docs sphinx-autobuild` instead of `make livehtml`
- Follow-ups: none

## Evidence
### Changed files
- `docs/Makefile` +0/-41 (deleted)
- `docs/README.md` +40/-13 (modified)
- `docs/requirements.txt` +0/-21 (deleted)
- `pyproject.toml` +25/-2 (modified)

### Notable symbols / endpoints / configs touched
- `[dependency-groups].docs` in `pyproject.toml`
- `docs/README.md` build instructions

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/4f0b18c8d7536fbf5c51111c2acac84bf7eeaf58.md`
