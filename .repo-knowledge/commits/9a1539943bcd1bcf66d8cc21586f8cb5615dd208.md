---
sha: "9a1539943bcd1bcf66d8cc21586f8cb5615dd208"
date: "2025-11-17T13:51:41-05:00"
author: "Tianqi Chen"
subject: "[LINT][MINOR] Fix clang-tidy (#275)"
nature: ["chore"]
tags: ["test"]
scope: ["include", "src"]
risk: "low"
---

# 9a15399 — [LINT][MINOR] Fix clang-tidy (#275)

## TL;DR
- Fixed two clang-tidy warnings from recent commits: added `// NOLINT(performance-unnecessary-value-param)` to `TypedFunction::operator()` in `function.h`, and changed `Optional<TensorView>` to `const Optional<TensorView>&` in the test lambda in `testing.cc`.

## Why (intent / motivation)
- CI clang-tidy checks failed on two recently added code patterns: by-value `Args...` in `TypedFunction::operator()` and by-value `Optional<TensorView>` argument in a lambda.

## What changed (facts from diff)
- `include/tvm/ffi/function.h`: Added `// NOLINT(performance-unnecessary-value-param)` suppression on `TypedFunction<R(Args...)>::operator()`.
- `src/ffi/testing/testing.cc`: Changed lambda argument from `Optional<TensorView> t` to `const Optional<TensorView>& t`.

## Public surface changes (if any)
- API: none
- Flags/config: none
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: none
- How to verify manually: `uv run --no-project --with "clang-tidy==21.1.1" python tests/lint/clang_tidy_precommit.py --build-dir=build-pre-commit ./include ./src`
- CI impact: Clang-tidy CI pass restored.

## Risk & rollout notes
- Risk level: low — lint-only changes; no behavioral difference.
- Rollout/migration: none
- Follow-ups: none

## Evidence
### Changed files
- `include/tvm/ffi/function.h` +1/-1 (modified)
- `src/ffi/testing/testing.cc` +1/-1 (modified)

### Notable symbols / endpoints / configs touched
- `TypedFunction<R(Args...)>::operator()` — NOLINT suppression
- `testing.optional_tensor_view_has_value` lambda — `const Optional<TensorView>&`

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/9a1539943bcd1bcf66d8cc21586f8cb5615dd208.md`
