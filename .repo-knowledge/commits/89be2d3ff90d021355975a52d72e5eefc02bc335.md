---
sha: "89be2d3ff90d021355975a52d72e5eefc02bc335"
date: "2025-12-22T10:46:45-08:00"
author: "Junru Shao"
subject: "doc: Final tweaks on Python Packaging (#358)"
nature: ["docs"]
tags: []
scope: ["docs", "examples"]
risk: "low"
---

# 89be2d3 — doc: Final tweaks on Python Packaging (#358)

## TL;DR
- Added a note showing how to retrieve global functions via `tvm_ffi.get_global_func` and equivalent C/C++ APIs
- Reverted the `abi_overview.md` heading from "ABI Specification" back to "ABI Overview"
- Updated `examples/python_packaging/src/extension.cc` to add a proper `TVM_FFI_STATIC_INIT_BLOCK` with `add_one` registration and split the `[global_function.end]` marker

## Why (intent / motivation)
- The packaging tutorial was missing a note that shows users how to call global functions by name from Python/C/C++
- The ABI overview heading revert aligns with earlier naming convention

## What changed (facts from diff)
- `docs/packaging/python_packaging.rst`: added 11-line note block with `tvm_ffi.get_global_func` code example
- `docs/concepts/abi_overview.md`: title changed from "ABI Specification" back to "ABI Overview"
- `examples/python_packaging/src/extension.cc`: `add_one` registration moved into its own `TVM_FFI_STATIC_INIT_BLOCK`; `[global_function.end]` marker repositioned after the `add_one` block so `literalinclude` captures only the intended snippet

## Public surface changes (if any)
- API: none
- Flags/config: none
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: none
- How to verify manually: build `examples/python_packaging` and confirm `tvm_ffi.get_global_func("my_ffi_extension.add_one")(3)` returns 4
- CI impact: none

## Risk & rollout notes
- Risk level: low — documentation and example code only
- Rollout/migration: none
- Follow-ups: none

## Evidence
### Changed files
- M `docs/concepts/abi_overview.md` +1/-1
- M `docs/packaging/python_packaging.rst` +11/-0
- M `examples/python_packaging/src/extension.cc` +8/-3

### Notable symbols / endpoints / configs touched
- `tvm_ffi.get_global_func` — documented in the note
- `TVM_FFI_STATIC_INIT_BLOCK` in `extension.cc` — `add_one` registration separated

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/89be2d3ff90d021355975a52d72e5eefc02bc335.md`
