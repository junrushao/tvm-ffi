---
sha: "0d8fec88eca5acaeaa7e771003d183c31711574a"
date: "2025-10-07T10:26:49-07:00"
author: "Junru Shao"
subject: "feat: Address warnings from clang-tidy  (#78)"
nature: ["refactor"]
tags: ["perf", "correctness", "lint"]
scope: ["include/tvm/ffi", "src/ffi", "tests/cpp", "tests/lint"]
risk: "medium"
---

# 0d8fec8 — feat: Address warnings from clang-tidy (#78)

## TL;DR
- Introduces a `.clang-tidy` configuration file and a `tests/lint/clang_tidy_precommit.py` runner script.
- Applies a large batch of clang-tidy-suggested fixes across the entire C++ codebase (headers + sources + tests).
- Changes include style modernizations (`_v`/`_t` suffixes, `using` instead of `typedef`, range-based for loops), performance improvements (`std::move`), and correctness fixes (`noexcept`, explicit casts, dangling-pointer avoidance).

## Why (intent / motivation)
- Establishes clang-tidy as the C++ static analysis baseline and addresses all current violations to prevent regressions.
- Improves correctness and performance of the C++ runtime without changing observable behavior.

## What changed (facts from diff)
- `.clang-tidy` added with checks configuration.
- `tests/lint/clang_tidy_precommit.py` added as a pre-commit script.
- Broad modernization across ~40 C++ files: typedefs replaced with using, range-based for, `std::move` additions, `noexcept` annotations, explicit casts.
- No API or ABI changes — purely internal implementation improvements.

## Public surface changes (if any)
- API: none (internal refactor only)
- Flags/config: none
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: multiple C++ test files updated to compile with tidy changes
- How to verify manually: `uv run --with "clang-tidy==21.1.1" python tests/lint/clang_tidy_precommit.py --build-dir=build-pre-commit --jobs=16 ./src/ ./include ./tests`
- CI impact: clang-tidy not added to CI in this PR; manual invocation documented.

## Risk & rollout notes
- Risk level: medium — large number of files changed; behavior unchanged but regressions possible
- Rollout/migration: none
- Follow-ups: Future PR may add clang-tidy to CI.

## Evidence
### Changed files
- `.clang-tidy` +29/-0 (added)
- `cmake/Utils/AddLibbacktrace.cmake` +3/-2 (modified)
- `include/tvm/ffi/*.h` (multiple files, ~40+ total)
- `src/ffi/*.cc` (multiple files)
- `tests/cpp/*.cc` (multiple files)
- `tests/lint/clang_tidy_precommit.py` +new (added)
- `tests/lint/check_file_type.py` (modified)

### Notable symbols / endpoints / configs touched
- `.clang-tidy` (new config)
- `tests/lint/clang_tidy_precommit.py` (new script)

### Diff notes
- Diff truncated: no (diff is large but complete in bundle)

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/0d8fec88eca5acaeaa7e771003d183c31711574a.md`
