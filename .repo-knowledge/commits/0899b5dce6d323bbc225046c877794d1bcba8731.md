---
sha: "0899b5dce6d323bbc225046c877794d1bcba8731"
date: "2025-10-28T12:09:49-07:00"
author: "Junru Shao"
subject: "feat: add clang-tidy to CI (#95)"
nature: ["ci"]
tags: ["lint", "cpp"]
scope: [".github"]
risk: "low"
---

# 0899b5d — feat: add clang-tidy to CI (#95)

## TL;DR
- Adds a new GitHub Actions workflow `ci_mainline_only.yml` that runs clang-tidy on every push to `main`.
- Uses `clang-tidy==21.1.1` via `uv run` on `src/`, `include/`, and `tests/`.

## Why (intent / motivation)
- Ensures C++ code quality is continuously checked after each merge to `main`, catching clang-tidy regressions before they accumulate.

## What changed (facts from diff)
- New `.github/workflows/ci_mainline_only.yml` with a `clang-tidy` job triggered on `push` to `main` and `workflow_dispatch`.
- Job: checks out with full depth/tags, sets up `uv` and env vars, runs `clang_tidy_precommit.py` with `clang-tidy==21.1.1`.

## Public surface changes (if any)
- API: none
- Flags/config: none
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: none (CI workflow)
- How to verify manually: Push to `main` branch; check Actions tab.
- CI impact: New `mainline-only` workflow runs clang-tidy on every main push.

## Risk & rollout notes
- Risk level: low — new optional CI job; does not block PRs.
- Rollout/migration: none
- Follow-ups: none

## Evidence
### Changed files
- `.github/workflows/ci_mainline_only.yml` +62/-0 (A)

### Notable symbols / endpoints / configs touched
- `clang-tidy` job in `ci_mainline_only.yml`

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/0899b5dce6d323bbc225046c877794d1bcba8731.md`
