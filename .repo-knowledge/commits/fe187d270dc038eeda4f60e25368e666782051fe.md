---
sha: "fe187d270dc038eeda4f60e25368e666782051fe"
date: "2025-12-06T13:25:28-08:00"
author: "Junru Shao"
subject: "bugfix: Fix clang-tidy warnings in `test_c_env_api.cc` (#318)"
nature: ["fix"]
tags: ["lint"]
scope: ["tests"]
risk: "low"
---

# fe187d2 — bugfix: Fix clang-tidy warnings in `test_c_env_api.cc` (#318)

## TL;DR
- Replaces C-style pointer casts with `static_cast<>` throughout `test_c_env_api.cc`.
- Adds `threads.reserve(num_threads)` to avoid reallocation in thread vectors.
- Replaces `std::lock_guard` with `std::scoped_lock` to satisfy clang-tidy's modernize suggestions.

## Why (intent / motivation)
- CI clang-tidy job flagged warnings introduced by the `TVMFFIHandleInitOnce` test code added in #317.

## What changed (facts from diff)
- `tests/cpp/extra/test_c_env_api.cc`: C-style casts replaced with `static_cast`, `threads.reserve()` added, `std::lock_guard` -> `std::scoped_lock`.

## Public surface changes (if any)
- API: none
- Flags/config: none
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: `tests/cpp/extra/test_c_env_api.cc` (style fixes only, no logic changes)
- How to verify manually: `uv run --no-project --with "clang-tidy==21.1.1" python tests/lint/clang_tidy_precommit.py --build-dir=build-pre-commit ./tests`
- CI impact: resolves clang-tidy CI failures

## Risk & rollout notes
- Risk level: low — no behavior change, only code style fixes
- Rollout/migration: none
- Follow-ups: none

## Evidence
### Changed files
- `tests/cpp/extra/test_c_env_api.cc` +13/-11 (M)

### Notable symbols / endpoints / configs touched
- `DeinitSuccess`, `DeinitWithError`, `DeinitWithCounter` — cast style fixed
- `TVMFFIHandleInitDeinitOnceMultithreaded` — reserve + scoped_lock

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/fe187d270dc038eeda4f60e25368e666782051fe.md`
