---
sha: "40e9c83345636b187f39df86a077e0b56683c792"
date: "2025-09-22T12:58:15-07:00"
author: "Junru Shao"
subject: "feat: Enable mypy type checking (#35)"
nature: ["feat"]
tags: ["ci", "chore"]
scope: ["python", "tests", "examples", ".pre-commit-config.yaml", "pyproject.toml"]
risk: "low"
---

# 40e9c83 — feat: Enable mypy type checking (#35)

## TL;DR
- Enables mypy type checking as a pre-commit hook covering `python/`, `tests/`, and `examples/`.
- Adds `python/tvm_ffi/_ffi_api.pyi` type stub for the `_ffi_api` Cython module.
- Applies widespread annotation fixes and `type: ignore` comments across the codebase to pass mypy.

## Why (intent / motivation)
- mypy provides static type safety guarantees beyond what Ruff's annotation enforcement can catch. Enabling it as a pre-commit hook ensures new code is type-checked from the start.

## What changed (facts from diff)
- `.pre-commit-config.yaml`: Added mypy hook (+27 lines) covering all source directories.
- `pyproject.toml`: Added mypy configuration section.
- `python/tvm_ffi/_ffi_api.pyi` added (43 lines): stubs for all global functions registered via `init_ffi_api`.
- `python/tvm_ffi/core.pyi` updated with additional type annotations.
- `python/tvm_ffi/cython/type_info.pxi`: Minor annotation fix.
- ~25 Python files updated with type annotation fixes and `type: ignore` suppressions where Cython/runtime types conflict with mypy's understanding.

## Public surface changes (if any)
- API: none (annotation changes only; `_ffi_api.pyi` stub is typing-only)
- Flags/config: mypy pre-commit hook added; mypy config in `pyproject.toml`
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: test files annotated/adjusted for mypy compliance
- How to verify manually: `pre-commit run mypy --all-files`
- CI impact: mypy now runs on every commit; failures will block merge.

## Risk & rollout notes
- Risk level: low — typing-only changes; no behavioral modifications
- Rollout/migration: New contributors must ensure mypy passes before opening PRs
- Follow-ups: none

## Evidence
### Changed files
- `.pre-commit-config.yaml` +27/-0 (modified)
- `pyproject.toml` +4/-0 (modified)
- `python/tvm_ffi/_ffi_api.pyi` +43/-0 (added)
- `python/tvm_ffi/core.pyi` (modified)
- ~25 Python source and test files (annotation fixes)

### Notable symbols / endpoints / configs touched
- `[tool.mypy]` in `pyproject.toml`
- `_ffi_api.pyi` stubs
- mypy pre-commit hook

### Diff notes
- Diff truncated: no (read partially)

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/40e9c83345636b187f39df86a077e0b56683c792.md`
