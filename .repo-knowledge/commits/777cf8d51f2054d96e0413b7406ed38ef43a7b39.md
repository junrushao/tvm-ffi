---
sha: "777cf8d51f2054d96e0413b7406ed38ef43a7b39"
date: "2025-08-30T10:30:25-04:00"
author: "Tianqi Chen"
subject: "[FFI][ABI] ABI Updates to for future metadata and complex ordering (#18254)"
nature: ["feat"]
tags: ["abi", "module", "c-api"]
scope: ["include/tvm/ffi", "src/ffi/extra", "python/tvm_ffi"]
risk: "medium"
---

# 777cf8d — [FFI][ABI] ABI Updates to for future metadata and complex ordering (#18254)

## TL;DR
- Updates the C ABI to improve ordering of static object types and enable future metadata retrieval from dynamically loaded modules.
- Adds `GetModuleMetadata` virtual method stub to `ModuleObj` for future module metadata access.
- Bumps `pyproject.toml` version.
- Updates Cython `base.pxi` and `module.cc` global function registrations.

## Why (intent / motivation)
- Before freezing the ABI, reorders simple C-ABI objects before complex C++-dependent ones.
- Reserves space in the type system for future metadata access from dynamically loaded modules.

## What changed (facts from diff)
- `include/tvm/ffi/c_api.h` +13/-10: ABI ordering adjustments.
- `include/tvm/ffi/extra/module.h` +14/-0: Added `GetModuleMetadata` virtual method stub.
- `pyproject.toml` +1/-1: Version bump.
- `python/tvm_ffi/cython/base.pxi` +5/-4: Updated Cython type index bindings.
- `scripts/run_tests.sh` +3/-1: Test script update.
- `src/ffi/extra/module.cc` +18/-0: Additional global function registrations.

## Public surface changes (if any)
- API: New `ModuleObj::GetModuleMetadata()` virtual method. Additional global functions registered in `module.cc`.
- Flags/config: Version bump in `pyproject.toml`.
- Data formats/schemas: ABI ordering change.

## Tests & verification
- Added/updated tests: none
- How to verify manually: Build and run existing tests.
- CI impact: Version bump; ABI change.

## Risk & rollout notes
- Risk level: medium — ABI changes before freeze; downstream code must be rebuilt.
- Rollout/migration: Rebuild all code against the new headers.
- Follow-ups: Implement `GetModuleMetadata` in concrete module types.

## Evidence
### Changed files
- `include/tvm/ffi/c_api.h` +13/-10 (M)
- `include/tvm/ffi/extra/module.h` +14/-0 (M)
- `pyproject.toml` +1/-1 (M)
- `python/tvm_ffi/cython/base.pxi` +5/-4 (M)
- `scripts/run_tests.sh` +3/-1 (M)
- `src/ffi/extra/module.cc` +18/-0 (M)

### Notable symbols / endpoints / configs touched
- `ModuleObj::GetModuleMetadata()`
- C ABI type ordering in `c_api.h`

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/777cf8d51f2054d96e0413b7406ed38ef43a7b39.md`
