---
sha: "262ca30aad858d818b919d5e670e80b7a57b89e4"
date: "2025-12-12T11:22:40-08:00"
author: "Junru Shao"
subject: "chore(lint): Fix clang-tidy warnings (#340)"
nature: ["chore"]
tags: ["lint"]
scope: ["include", "src", "tests"]
risk: "low"
---

# 262ca30 — chore(lint): Fix clang-tidy warnings (#340)

## TL;DR
- Fixes clang-tidy warnings introduced by the strided-view additions in #335.
- Changes `alloc_(alloc)` to `alloc_(std::move(alloc))` in `TensorObjFromNDAlloc` constructors.
- Adds `explicit` keyword to `ViewNDAlloc` constructor.
- Converts `prototype->ndim * 2` to `static_cast<size_t>(prototype->ndim) * 2` to avoid signed/unsigned width warning.
- Replaces `int64_t` loop variable with `size_t` in test fill loops.

## Why (intent / motivation)
- CI clang-tidy job flagged warnings on the code added in #335.

## What changed (facts from diff)
- `include/tvm/ffi/container/tensor.h`: `alloc_(std::move(alloc))` in both `TensorObjFromNDAlloc` constructors.
- `src/ffi/tensor.cc`: `explicit ViewNDAlloc(...)`, `static_cast<size_t>(prototype->ndim) * 2`.
- `tests/cpp/test_tensor.cc`: Loop bounds use `size_t` via `GetDataSize(tensor) / sizeof(float)`.

## Public surface changes (if any)
- API: none
- Flags/config: none
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: none (logic unchanged)
- How to verify manually: run clang-tidy on `src/` and `include/`
- CI impact: resolves clang-tidy failures

## Risk & rollout notes
- Risk level: low — code style only, no behavior change
- Rollout/migration: none
- Follow-ups: none

## Evidence
### Changed files
- `include/tvm/ffi/container/tensor.h` +2/-2 (M)
- `src/ffi/tensor.cc` +3/-2 (M)
- `tests/cpp/test_tensor.cc` +6/-2 (M)

### Notable symbols / endpoints / configs touched
- `TensorObjFromNDAlloc` constructors
- `ViewNDAlloc` constructor

### Diff notes
- Diff truncated: no

### Knowledge links
- Per-commit file: `.repo-knowledge/commits/262ca30aad858d818b919d5e670e80b7a57b89e4.md`
