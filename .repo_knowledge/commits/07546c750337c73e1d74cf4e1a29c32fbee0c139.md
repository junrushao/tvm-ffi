---
sha: "07546c750337c73e1d74cf4e1a29c32fbee0c139"
date: "2026-02-20"
author: "Tianqi Chen"
subject: "[COMPACT][TEMP] Array compact temp fix (#467)"
nature: ["fix"]
tags: ["api-break"]
scope: ["cpp/container"]
risk: "low"
---

# 07546c7 -- [COMPACT][TEMP] Array compact temp fix (#467)

## TL;DR
- Reverted `Array::operator[]` return type from `T` back to `const T` as a temporary workaround for FlashInfer compatibility
- This is a temporary fix to unblock the 0.1.19 release; will be re-landed once FlashInfer's fix is released

## Why (intent / motivation)
- The 0.1.19 patch changed `Array::operator[]` to return `T` (from `const T`), which was the correct change
- However, FlashInfer depends on a subtle implicit conversion rule that requires the `const T` return type
- A FlashInfer patch exists but has not been released yet, so this temporary revert unblocks the 0.1.19 release

## What changed (facts from diff)
- `include/tvm/ffi/container/array.h`: Changed `T operator[](int64_t i) const` back to `const T operator[](int64_t i) const`

## Public surface changes (if any)
- API: `Array::operator[]` returns `const T` instead of `T` (revert to old behavior)
- Flags/config: none
- Data formats/schemas: none

## Tests & verification
- Added/updated tests: none
- How to verify manually: Build with FlashInfer and verify compilation succeeds
- CI impact: none

## Risk & rollout notes
- Risk level: low -- Reverts to previous known-good behavior
- Rollout/migration: none
- Follow-ups: Re-land the `T` return type once FlashInfer releases their fix

## Evidence
### Changed files
- `include/tvm/ffi/container/array.h` (+1/-1, modified)

### Notable symbols / endpoints / configs touched
- `Array::operator[]` return type

### Diff notes
- Diff truncated: no
