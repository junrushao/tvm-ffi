---
author: "Tianqi Chen"
subject: "Add missing packaging example files and rename get_started to quick_start"
scope:
  - "examples/packaging"
  - "examples/quick_start"
impact: "low"
---
# [FFI][DOCS] Add missing files in packaging example (#18261)
## TL;DR
- Adds `examples/packaging/run_example.py` and `examples/packaging/src/extension.cc` which were previously missing from the packaging example.
- Renames `examples/get_started/` to `examples/quick_start/` (the old `get_started` directory under `quick_start/get_started/` is removed).
## Impact
- API: none
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- TVM FFI module system (`load_module`, `TVM_FFI_DLL_EXPORT_TYPED_FUNC`)
- Reflection-based global function registration (`refl::GlobalDef().def(...)`)
Design elements this commit produces:
- Complete packaging example showing both C-symbol export and reflection-based registration
## Reflection
- The packaging example demonstrates two approaches to function export: direct C symbol export (`TVM_FFI_DLL_EXPORT_TYPED_FUNC`) and reflection-based registration (`refl::GlobalDef().def()`), each suited for different deployment scenarios (dynamic load vs bundled).
