---
author: "Kathryn (Jinqi) Chen"
subject: "Fix: use dim() instead of ndim() in stride normalization check"
scope:
  - "python/tvm_ffi/_optional_torch_c_dlpack.py"
impact: "low"
---
# small fix (#27)
## TL;DR
- Changes `src.ndim()` to `src.dim()` in the 1D stride normalization guard introduced in #23, aligning with the PyTorch C++ API convention used elsewhere in the file.
## Impact
- API: none
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none
## Reflection
- Trivial one-liner fix; `ndim()` and `dim()` are equivalent in PyTorch, but the codebase consistently uses `dim()`.
