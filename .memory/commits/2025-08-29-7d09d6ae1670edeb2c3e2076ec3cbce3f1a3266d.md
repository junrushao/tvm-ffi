---
author: "Balint Cristian"
subject: "Revert cmake libbacktrace URL, update submodule to upstream HEAD"
scope:
  - "build/cmake"
  - "3rdparty"
impact: "low"
---
# [FFI][CMAKE] Revert cmake libbacktrace URL and update submodule (#18249)
## TL;DR
- Reverts the `GIT_REPOSITORY`/`GIT_TAG` addition from #18246 in `AddLibbacktrace.cmake`
- Updates `3rdparty/libbacktrace` submodule from `08f7c7e` to `7939218` (upstream HEAD), which already includes the macOS patch that was previously carried as a fork

## Impact
- API: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- The download-URL approach from #18246

Design elements this commit produces:
- Submodule-based libbacktrace dependency at upstream commit `7939218` which includes native macOS support

## Reflection
- The upstream libbacktrace now includes the macOS `macho.c` fix that the TVM fork previously carried, so no fork is needed anymore
