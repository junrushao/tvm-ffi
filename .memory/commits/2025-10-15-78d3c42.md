---
author: ""
subject: "Upgrade Python dependencies, add dev group, and pre-import torch workaround"
scope:
  - "build"
  - "python"
impact: "medium"
---
# chore: Upgrade Python dependency (#135)
## TL;DR
- Added a `[dependency-groups] dev` section in pyproject.toml with development tools (ruff, mypy, clang-format, etc.).
- Added `ninja` to build-system requirements and configured scikit-build ninja settings.
- Added a hack in `__init__.py` to pre-import torch before tvm_ffi to avoid a symbol conflict on Windows x64 + Python 3.12 + torch 2.9.0.
- Added `[tool.uv.dependency-groups] docs` with `requires-python >= 3.13`.
## Impact
- API: none
- Flags/config: new `[dependency-groups] dev` and `docs` sections in pyproject.toml
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none
Design elements this commit produces:
- Torch pre-import workaround pattern for Windows symbol conflicts
## Reflection
- The torch pre-import hack is a narrow workaround for a specific CI environment. The comment documents the exact reproduction conditions.
