---
author: "wrongtest"
subject: "Fix None argument passing: zero-initialize v_int64 for kTVMFFINone"
scope:
  - "python/cython"
impact: "low"
---
# [FFI] fix two seemingly migration issue (#18258)
## TL;DR
- Adds `out[i].v_int64 = 0` when setting `kTVMFFINone` type index for `None` arguments in the Cython argument packing code, ensuring the union value is zero-initialized

## Impact
- API: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `TVMFFIAny` union type where `type_index` and the value union are separate fields

Design elements this commit produces: none

## Reflection
- Without zeroing the value, the union retains whatever was previously in memory; downstream C++ code that reads the raw bytes (e.g., for hashing or comparison) could observe garbage, leading to nondeterministic behavior
- Despite the PR title mentioning "two" issues, the diff shows only one change; the second fix may have been in a different file or was addressed separately
