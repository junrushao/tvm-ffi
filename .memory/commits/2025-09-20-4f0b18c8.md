---
author: "Junru Shao"
subject: "Migrate doc building from Makefile+requirements.txt to uv dependency groups"
scope:
  - "pyproject.toml"
  - "docs/README.md"
  - "docs/Makefile"
  - "docs/requirements.txt"
impact: "medium"
---
# feat: Use uv-based workflow for doc building (#34)
## TL;DR
- Removes `docs/Makefile` and `docs/requirements.txt`, consolidating all doc dependencies into a `[dependency-groups] docs` section in `pyproject.toml`.
- Rewrites `docs/README.md` with `uv run --group docs` commands for interactive (`sphinx-autobuild`) and one-off (`sphinx-build`) doc builds.
## Impact
- API: none
- Flags/config: Doc build now uses `uv run --group docs` instead of `pip install -r docs/requirements.txt && make livehtml`.
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- uv dependency groups feature (`[dependency-groups]` in `pyproject.toml`)

Design elements this commit produces:
- `docs` dependency group in `pyproject.toml`
## Reflection
- Aligns doc workflow with the project-wide `uv` tooling strategy described in CLAUDE.md.
