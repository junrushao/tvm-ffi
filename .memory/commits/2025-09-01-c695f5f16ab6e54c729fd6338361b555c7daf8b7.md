---
author: "Tianqi Chen"
subject: "Add initial Sphinx documentation scaffolding with concept guides and examples"
scope:
  - "docs"
  - "examples"
  - "src/ffi/extra/testing.cc"
  - "tests/cpp/test_example.cc"
impact: "medium"
---
# [FFI][DOCS] Initial docs scaffolding (#18263)
## TL;DR
- Creates the full Sphinx documentation structure: `docs/conf.py`, `docs/index.rst`, `docs/Makefile`, `docs/requirements.txt`, with pages for ABI overview, install guide, quick start, C++ guide, Python guide, and packaging guide.
- Moves `examples/quick_start/get_started/` up one level to `examples/quick_start/` and renames the packaging example's Python package from `tvm_ffi_extension` to `my_ffi_extension`.
- Adds `src/ffi/extra/testing.cc` (testing utilities) and `tests/cpp/test_example.cc` (C++ tests covering example-style code).
## Impact
- API: none (documentation and test additions only)
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- TVM FFI core abstractions (Any, Object, Function, Module, containers)
Design elements this commit produces:
- Sphinx documentation site with MyST-parser Markdown support and pydata-sphinx-theme
- `docs/concepts/abi_overview.md` -- detailed explanation of TVMFFIAny storage format, object storage, function ABI
- Testing helper utilities in `src/ffi/extra/testing.cc`
## Reflection
- The ABI overview doc provides the authoritative narrative for the C ABI design, complementing the `c_api.h` header as the source of truth.
- Renaming the example package from `tvm_ffi_extension` to `my_ffi_extension` clarifies that it is a user-defined package, not an official extension.
