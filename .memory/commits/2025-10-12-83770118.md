---
author: "Tianqi Chen"
subject: "Expose Tensor.strides in Cython and bump DLPack to v1.2"
scope:
  - "python/tvm_ffi/cython/tensor.pxi"
  - "3rdparty/dlpack"
impact: "low"
---
# [CYTHON] Expose Tensor.strides (#102)
## TL;DR
- Adds a `strides` property to the Cython `Tensor` class, returning stride info from DLTensor.
- Bumps `3rdparty/dlpack` submodule to DLPack v1.2.
- Adds test coverage for strides.
## Impact
- API: Adds new `Tensor.strides` property (additive, non-breaking).
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none
Design elements this commit produces:
- `Tensor.strides` property available from Python
## Reflection
- Small additive change; strides were already present in the underlying DLTensor but not exposed to Python.
