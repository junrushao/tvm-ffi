---
author: "Junru Shao"
subject: "Revamp mypy configuration and add PEP 561 py.typed marker"
scope:
  - "pyproject.toml"
  - ".pre-commit-config.yaml"
  - "python/tvm_ffi/py.typed"
  - "python/tvm_ffi/dataclasses/c_class.py"
impact: "medium"
---
# chore: Revamp mypy config and publish typing metadata (#51)
## TL;DR
- Adds comprehensive `[tool.mypy]` config in `pyproject.toml`: sets Python 3.9 target, explicit file paths, namespace packages, and `torch`/example ignore overrides.
- Creates `python/tvm_ffi/py.typed` PEP 561 marker file and includes it in sdist packaging.
- Simplifies `c_class.py` by removing the `try/except` fallback chain for `dataclass_transform`, requiring `typing-extensions>=4.5` instead.

## Impact
- API: none
- Flags/config: `typing-extensions>=4.5` is now a hard minimum dependency. mypy config is significantly expanded. PEP 561 typing metadata is now published.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- PEP 561 `py.typed` marker convention for typed packages

Design elements this commit produces:
- Fully configured mypy for the repo (python, examples, tests) with proper namespace package support
- Simplified `dataclass_transform` import path (always from `typing_extensions`)

## Reflection
- The `typing-extensions>=4.5` lower bound ensures `dataclass_transform` is available, eliminating the fragile triple-try-except import chain.
- Adding `py.typed` means downstream consumers of the package will get type checking support automatically.
