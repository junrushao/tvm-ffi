---
author: "Tianqi Chen"
subject: "Fix lint errors and improve docs with Sphinx cross-references"
scope:
  - "docs/guides"
  - "docs/reference/python"
  - ".asf.yaml"
impact: "low"
---
# [DOCS] Fix lint and improve docs (#13)
## TL;DR
- Adds ASF license header to `docs/reference/python/cpp/index.rst`
- Replaces inline code references in `compiler_integration.md` with Sphinx cross-reference roles (e.g., `{cpp:func}`, `{cpp:class}`, `{c:macro}`)
- Updates `.asf.yaml` branch protection comment (clarifies committer checks CI before merge)
## Impact
- API: none
- Flags/config: none -- `.asf.yaml` comment change only, no protection rule change
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- Sphinx `{cpp:func}`, `{cpp:class}`, `{c:macro}` cross-reference roles
- Compiler integration guide from commit 240ea44a

Design elements this commit produces:
- Improved docs with clickable cross-references to C++/C API symbols
## Reflection
- Fixes lint failures introduced by prior doc PRs (missing ASF headers)
