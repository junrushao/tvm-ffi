---
author: "Yaxing Cai"
subject: "Expose get_raw_stream method for querying current device stream"
scope:
  - "python/tvm_ffi/stream.py"
  - "python/tvm_ffi/__init__.py"
  - "python/tvm_ffi/cython/base.pxi"
impact: "low"
---
# Expose get stream method (#97)
## TL;DR
- Adds `_env_get_current_stream(device_type, device_id)` Cython binding wrapping `TVMFFIEnvGetStream`
- Adds `get_raw_stream(device)` Python function to query the current FFI stream for a given device
- Exports `get_raw_stream` from `tvm_ffi.__init__`
- Adds basic test in `tests/python/test_stream.py`

## Impact
- API: New public function `tvm_ffi.get_raw_stream(device) -> int`
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `TVMFFIEnvGetStream` C API (from #5)
- `StreamContext` pattern in `stream.py`

Design elements this commit produces: none

## Reflection
- Follow-up of PR #5 which introduced set-stream but did not expose get-stream
