---
author: "tqchen"
subject: "Add ASF YAML configuration for GitHub repo settings and branch protection"
scope:
  - "ci"
  - "repo-config"
impact: "low"
---
# [CI] Setup asf yaml

## TL;DR
- Adds `.asf.yaml` to configure Apache GitHub repository metadata: description, homepage, labels, branch protection (1 required review on `dev` branch), and notification routing.
- Minor whitespace change in `ci_test.yml`.

## Impact
- API: none
- Flags/config: `.asf.yaml` configures branch protection for `dev` branch requiring 1 approving review. Notifications route to `commits@tvm.apache.org` and `discuss-archive@tvm.apache.org`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces:
- ASF-managed GitHub repository configuration (`.asf.yaml`)

## Reflection
- Branch protection is set on `dev` only at this stage, with a note to migrate to `main` once confirmed working.
- No status checks are required yet, per the inline comment.
