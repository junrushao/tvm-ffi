---
author: "unknown"
subject: "Fix CUDA stream passing bug in Cython function dispatcher"
scope:
  - "python/cython"
impact: "low"
---
# [CYTHON] Fix stream passing bug (#68)
## TL;DR
- Fixed a typo in `function.pxi` where `ctx.device_id` was checked instead of `ctx.device_type` for determining whether a device has already been queried during DLPack export, causing incorrect stream passing behavior.
- Extended `test_load_inline.py` CUDA test to verify stream identity with a `raw_stream` parameter.
- Version bumped to 0.1.0b11.
## Impact
- API: none
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- DLPack C exporter stream passing in Cython dispatcher

Design elements this commit produces: none
## Reflection
- Single-character fix (`device_id` -> `device_type`) with high impact on CUDA stream correctness.
