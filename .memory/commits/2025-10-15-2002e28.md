---
author: ""
subject: "Fix Windows build: ARM64 intrinsics and Python SABI linking"
scope:
  - "build"
  - "windows"
impact: "medium"
---
# chore(build): Fix Windows Build (#134)
## TL;DR
- Added `_InlineInterlockedAdd64` workaround for Windows ARM64 in `base_details.h`.
- Added explicit `Python::SABIModule` and `Python::Module` link targets for Cython module on non-Windows platforms.
- Changed CI aarch64 test from Python 3.9 to 3.10.
## Impact
- API: none
- Flags/config: none (CI matrix update)
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none
Design elements this commit produces:
- Windows ARM64 interlocked intrinsics compatibility pattern
## Reflection
- Addresses MSVC ARM64 compilation where `_InlineInterlockedAdd64` may be undefined, mapping it to `InterlockedAdd64`.
