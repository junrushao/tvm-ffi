---
author: ""
subject: "Add test case for subclassing ffi.Function with mixin pattern"
scope:
  - "test"
  - "python"
impact: "low"
---
# [TEST] Add testcase about subclassing an ffi.Function (#140)
## TL;DR
- Added `test_function_subclass()` demonstrating how to subclass `tvm_ffi.Function` with a mixin class (`JitFunction`).
- Tests handle transfer via `__move_handle_from__`, callable behavior, and round-trip through echo.
- Documents the pattern of explicitly assigning `__call__` when subclassing Cython cdef classes.
## Impact
- API: none (test only)
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- `Function.__move_handle_from__()` and `Function.__chandle__()` protocols
Design elements this commit produces:
- Documented pattern for subclassing Cython extension types with mixins
## Reflection
- The explicit `__call__ = tvm_ffi.Function.__call__` assignment is a Cython-specific quirk worth remembering.
