---
author: "unknown"
subject: "Expose package version and add pre-commit version sync check"
scope:
  - "python/init"
  - "tests/lint"
impact: "low"
---
# [VERSION] Expose version (#62)
## TL;DR
- Added `__version__` to `python/tvm_ffi/__init__.py`, sourced from `pyproject.toml` (manually synced).
- Added `check_version.py` lint script that verifies `pyproject.toml` and `__init__.py` versions match, auto-fixing on mismatch.
- Registered the check as a pre-commit hook.
- Version bumped to 0.1.0b9.
## Impact
- API: `tvm_ffi.__version__` is now available.
- Flags/config: new `check-version` pre-commit hook
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none

Design elements this commit produces:
- Version sync pattern between `pyproject.toml` and `__init__.py`
- `tests/lint/check_version.py` lint tool
## Reflection
- The version is manually managed in `pyproject.toml` as the source of truth.
