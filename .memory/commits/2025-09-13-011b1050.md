---
author: "Tianqi Chen"
subject: "Update documentation URLs and code samples to reflect standalone tvm-ffi repo"
scope:
  - "docs"
impact: "low"
---
# [DOCS] Update docs to reflect the new repo (#4)

## TL;DR
- Updates Sphinx `conf.py`: `repository_url` from `apache/tvm` to `apache/tvm-ffi`, `conf_py_path` from `/ffi/docs/` to `/docs/`.
- Updates install/quickstart/packaging guides: clone URL from `apache/tvm` (with `cd tvm/ffi`) to `apache/tvm-ffi` (with `cd tvm-ffi`).
- Updates quickstart code examples: `DLTensor*` parameters changed to `ffi::Tensor`, adds `#include <tvm/ffi/container/tensor.h>` and `namespace ffi = tvm::ffi`.

## Impact
- API: none -- documentation-only changes.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces:
- Documentation aligned with standalone `apache/tvm-ffi` repository structure
- Code samples updated to use the `ffi::Tensor` wrapper instead of raw `DLTensor*`

## Reflection
- The code sample changes in `quick_start.md` and `packaging.md` reflect the project's preference for typed `ffi::Tensor` wrappers over raw `DLTensor*` pointers -- consistent with the container/object system design.
