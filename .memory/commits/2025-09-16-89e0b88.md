---
author: "tqchen"
subject: "Switch branch protection from 'dev' to 'main' in ASF config"
scope:
  - ".asf.yaml"
impact: "low"
---
# [CI] setup asf yaml to protect main
## TL;DR
- Changed the protected branch name from `dev` to `main` in `.asf.yaml`, enabling required pull-request reviews on the main branch.
## Impact
- API: none
- Flags/config: branch protection now applies to `main` instead of `dev`.
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none
Design elements this commit produces: none
## Reflection
- One-word change. Comments in the file explain the two-step rollout: first protect `dev`, confirm it works, then switch to `main`.
