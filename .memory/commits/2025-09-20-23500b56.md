---
author: "Junru Shao"
subject: "Add Taplo pre-commit hook for TOML formatting"
scope:
  - ".pre-commit-config.yaml"
impact: "low"
---
# chore(lint): Add Taplo for TOML (#33)
## TL;DR
- Adds the `taplo-format` hook (v0.9.3) from `ComPWA/taplo-pre-commit` to the pre-commit configuration for automatic TOML file formatting.
## Impact
- API: none
- Flags/config: New pre-commit hook `taplo-format` will auto-format `.toml` files.
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none
## Reflection
- Small lint infrastructure addition; ensures consistent TOML style across `pyproject.toml` and any future `.toml` configs.
