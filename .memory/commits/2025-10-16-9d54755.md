---
author: ""
subject: "Test wheel building on every main commit by triggering publish workflow"
scope:
  - "ci"
impact: "low"
---
# chore(release): Test wheel building on every `main` commit (#149)
## TL;DR
- Added `push: branches: [main]` trigger to `publish_wheel.yml` so wheel builds run on every main commit.
- Updated checkout refs to use `github.ref` for push events (falling back to `inputs.branch` for manual dispatch).
- Publishing to PyPI remains gated on `workflow_dispatch` only.
## Impact
- API: none
- Flags/config: CI workflow trigger change
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none
Design elements this commit produces: none
## Reflection
- Ensures wheel build issues are caught early on every main commit, while actual publishing stays manual.
