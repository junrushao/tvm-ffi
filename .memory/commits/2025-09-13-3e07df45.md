---
author: "tqchen"
subject: "Bump version to 0.1.0b0 and configure squash-only merge in ASF YAML"
scope:
  - "packaging"
  - "repo-config"
impact: "low"
---
# [VERSION] Bump to beta version

## TL;DR
- Bumps `pyproject.toml` version from `0.1.0a13` (alpha) to `0.1.0b0` (beta).
- Extends `.asf.yaml` with merge button configuration: enables squash-only merges, disables merge and rebase buttons.

## Impact
- API: none
- Flags/config: Package version changes from `0.1.0a13` to `0.1.0b0`. GitHub merge strategy locked to squash-only.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `.asf.yaml` from 306c8d0d

Design elements this commit produces:
- Beta version milestone marker
- Squash-merge-only policy enforced via `.asf.yaml`

## Reflection
- The commit message notes the version should change to a normal release once formally released.
