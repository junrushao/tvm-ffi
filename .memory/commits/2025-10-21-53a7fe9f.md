---
author: "Tianqi Chen"
subject: "Handle OSError in library path resolution"
scope:
  - "python/tvm_ffi/libinfo.py"
impact: "low"
---
# [PYTHON] Robustify libinfo finding (#185)
## TL;DR
- Wrapped `path.is_dir()` and `path.resolve()` calls in `get_dll_directories()` with try/except for `OSError`
- Prevents crashes when PATH contains directories the process cannot access
## Impact
- API: none
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none
Design elements this commit produces: none
## Reflection
- Defensive fix; `Path.resolve()` can raise OSError on permission-restricted directories
