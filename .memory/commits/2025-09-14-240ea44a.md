---
author: "Tianqi Chen"
subject: "Add compiler integration guide with kernel, graph, and custom module sections"
scope:
  - "docs/guides"
  - "include/tvm/ffi/function.h"
  - "src/ffi/extra/library_module.cc"
impact: "medium"
---
# [DOCS] Add Guides for Compiler Integration (#10)
## TL;DR
- Adds `docs/guides/compiler_integration.md` (140 lines) covering kernel language, graph compiler, and custom module integration patterns
- Improves docstring on `TVM_FFI_DLL_EXPORT_TYPED_FUNC` macro in `function.h` (removes misleading comments, adds `__tvm_ffi_<ExportName>` note)
- Clarifies binary layout comments in `library_module.cc` (`<val: bytes>` is optional per key type)
## Impact
- API: none -- documentation and comment changes only
- Flags/config: none
- Data formats/schemas: none, but documents the `__tvm_ffi__library_bin` binary layout
## Design Elements
Design elements this commit consumes:
- `TVM_FFI_DLL_EXPORT_TYPED_FUNC` macro and `__tvm_ffi_` symbol prefix convention
- `__tvm_ffi__library_bin` binary serialization format (CSR import tree)
- `ModuleObj` interface (`GetFunction`, `GetPropertyMask`, `SaveToBytes`)

Design elements this commit produces:
- Reference documentation for compiler authors integrating with TVM FFI
- Three integration tiers: kernel DSL codegen, graph compiler `call_tvm_ffi`, custom `ModuleObj` subclass
## Reflection
- The C code snippet in the guide shows the exact low-level ABI a codegen must produce, useful as a codegen spec
- Documents both `_lib` and custom-kind entries in the library binary format
