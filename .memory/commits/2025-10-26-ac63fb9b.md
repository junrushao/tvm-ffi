---
author: "Junru Shao"
subject: "Introduce setuptools_scm for automated Python/C++/Rust versioning"
scope:
  - "pyproject.toml"
  - "tests/lint/check_version.py"
  - ".github/workflows"
  - "include/tvm/ffi/c_api.h"
impact: "high"
---
# chore(build): Introduce `setuptools_scm` for better versioning (#154)
## TL;DR
- Python version is now dynamically derived from git tags via `setuptools_scm` (removed hardcoded `version` from `pyproject.toml`)
- Rewrote `tests/lint/check_version.py` to check C++ macros and Rust Cargo.toml versions against the canonical setuptools_scm version
- Updated build-system requires, CI workflows, and `.gitignore` for the new `_version.py` file
## Impact
- API: none (version string derivation changes but public API unchanged)
- Flags/config: `pyproject.toml` now uses `dynamic = ["version"]` with `setuptools_scm` provider; `_version.py` is auto-generated
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes: none
Design elements this commit produces:
- Automated versioning pipeline: git tags -> setuptools_scm -> Python/C++/Rust version linting
## Reflection
- Removes manual version bumping; linter catches cross-language version drift; version bumped to 0.1.1 in C++ macros
