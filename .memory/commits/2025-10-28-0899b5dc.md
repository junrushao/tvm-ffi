---
author: "Junru Shao"
subject: "Add clang-tidy CI job running on main branch pushes"
scope:
  - ".github/workflows"
impact: "medium"
---
# feat: add clang-tidy to CI (#95)
## TL;DR
- Added `.github/workflows/ci_mainline_only.yml` with a clang-tidy job that runs on every push to `main`
- Uses `clang-tidy==21.1.1` via uv to lint `src/`, `include/`, and `tests/` directories
- Depends on #197 for clean baseline
## Impact
- API: none
- Flags/config: new CI workflow `mainline-only` triggered on push to `main` and `workflow_dispatch`
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- Clean clang-tidy baseline from #197
Design elements this commit produces:
- Per-commit clang-tidy enforcement on the main branch
## Reflection
- Only runs on main (not PRs) to avoid duplicating the separate PR clang-tidy check; acts as a safety net for any regressions that slip through
