---
author: "tqchen"
subject: "Fix lint violations across Python sources and tests to comply with new CI"
scope:
  - "python"
  - "lint"
impact: "low"
---
# [LINT] Run lint to be compliant with the new CI

## TL;DR
- Applies `isort` and `black` formatting fixes across all Python source files (`python/tvm_ffi/`) and test files (`tests/python/`).
- Adds `[tool.ruff]` configuration to `pyproject.toml` with per-file ignores (`F401` for `__init__.py`, `E741` for tests) and `max-args=10`.
- Extends `task_lint.sh` to run `isort --check`, `ruff check`, and reorders `black` invocation.

## Impact
- API: none -- all changes are formatting/import-order only.
- Flags/config: `pyproject.toml` gains `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.ruff.lint.per-file-ignores]`, `[tool.ruff.lint.pylint]` sections.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- CI lint pipeline from ef6d57b8

Design elements this commit produces:
- Ruff lint configuration integrated into project tooling
- `task_lint.sh` now runs isort + black + ruff + clang-format in sequence

## Reflection
- Pure style/formatting commit with no behavioral changes. The `_tensor.py` `__all__` list was reformatted from multi-line to single-line, and `container.py` had import grouping consolidated.
