---
author: "Tianqi Chen"
subject: "Remove `using ffi::*` aliases from `tvm` namespace in FFI headers"
scope:
  - "c-abi-boundary-layer"
  - "object-system"
  - "container-library"
impact: "medium"
---
# Remove `using ffi::*` aliases from `tvm` namespace in FFI headers
## TL;DR
- Removes `using ffi::X` declarations from the `tvm` namespace across 8 FFI headers (cast.h, array.h, map.h, variant.h, memory.h, optional.h, string.h, dtype.h).
- Ensures symbols like `Array`, `Map`, `String`, `make_object`, `Optional`, `Variant`, `GetRef`, `GetObjectPtr`, `Bytes` are only accessible via `tvm::ffi::` qualified names.
- Also removes a stale `TODO(tvm-team)` comment in dtype.h.
## Impact
- API: **Breaking** for any downstream C++ code that uses `tvm::Array`, `tvm::Map`, `tvm::String`, `tvm::make_object`, `tvm::Optional`, `tvm::Variant`, `tvm::GetRef`, `tvm::GetObjectPtr`, `tvm::Bytes` without the `ffi::` qualifier. All such call sites must migrate to `tvm::ffi::X`.
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- [Object system and reference counting](/.memory/designs/0002-object-system-and-reference-counting.md): `GetRef`, `GetObjectPtr`, `make_object`, `ObjectPtr` are object system primitives whose namespace qualification is being tightened.
- [Container library](/.memory/designs/0004-container-library.md): `Array`, `Map`, `String`, `Bytes`, `Optional`, `Variant` are the container types losing their `tvm::` alias.
- [C ABI boundary layer](/.memory/designs/0005-c-abi-boundary-layer.md): This is a namespace isolation step preparing FFI for standalone extraction.

Design elements this commit produces:
- **Namespace isolation invariant**: After this commit, no FFI symbol is re-exported into `tvm::` from FFI headers. This is a prerequisite for extracting `tvm::ffi` as an independent package with its own top-level namespace.
## Reflection

* **Design docs to write**:
  - `.memory/designs/0013-namespace-isolation-strategy.md` — Document the invariant that FFI headers must not leak symbols into `tvm::` and the migration path for downstream code. This helps classify future namespace-related commits and prevents regression.

* **ADRs to write**:
  - `.memory/ADRs/0018-ffi-namespace-isolation.md` — Context: FFI symbols were historically aliased into `tvm::` for convenience. Decision: Remove all `using ffi::X` from FFI headers to enable standalone packaging. Trade-off: Breaking change for downstream users vs. clean namespace boundary.

* **Design diagrams to draw**: None

* **Skill evolution (`/commit-ledger`)**: None
