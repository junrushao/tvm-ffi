---
author: "Junru Shao"
subject: "Enable comprehensive Ruff rule coverage: UP, PL, I, RUF, NPY, F, PTH, D rules"
scope:
  - "pyproject.toml"
  - "python/tvm_ffi"
  - "tests"
  - "examples"
  - "docs/conf.py"
impact: "high"
---
# chore: Basic Ruff rule coverage (#20)
## TL;DR
- Expanded `[tool.ruff.lint]` select from default to explicit rule sets: UP (pyupgrade), PL (pylint), I (isort), RUF, NPY, F (pyflakes), PTH (pathlib), D (pydocstyle). ANN (annotations) left commented out for next commit.
- Removed legacy `[tool.black]` and `[tool.isort]` config sections; ruff now owns formatting and import sorting entirely.
- Applied auto-fixes across the codebase: added docstrings, replaced `os.path` calls with `pathlib` equivalents, modernized type unions, removed unused imports, fixed pylint warnings.
- Deleted `tests/lint/git-clang-format.sh` (92 lines) -- functionality now covered by the clang-format pre-commit hook.
- Deleted `tests/scripts/task_lint.sh` (46 lines) -- linting is now entirely via pre-commit.
## Impact
- API: none (all changes are internal style / lint compliance)
- Flags/config: `pyproject.toml` `[tool.ruff]` section is the new canonical lint configuration with explicit rule IDs and per-file ignores for tests.
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- Pre-commit hook infrastructure from commit 64e4b7f
Design elements this commit produces:
- Canonical ruff rule selection (referenced by future PRs adding rules like ANN)
## Reflection
- Large refactor (400+ line churn across 40+ files) but semantically a no-op in terms of runtime behavior.
- The D (pydocstyle) rules required adding Google-style docstrings to many public functions.
- The deleted shell scripts were superseded by pre-commit hooks in the previous commit.
