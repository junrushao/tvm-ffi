---
author: "unknown"
subject: "Swap TVM_FFI_CHECK argument order: condition first, error kind second"
scope:
  - "core/error"
impact: "low"
---
# [ERROR] Place ErrorKind second in error handling (#70)
## TL;DR
- Changed `TVM_FFI_CHECK` macro signature from `TVM_FFI_CHECK(ErrorKind, cond)` to `TVM_FFI_CHECK(cond, ErrorKind)` so that the condition comes first, matching the convention of `TVM_FFI_ICHECK(cond)`.
- Updated all tests to use the new argument order.
## Impact
- API: `TVM_FFI_CHECK` argument order changed (was just introduced in #69, so no external callers yet).
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- `TVM_FFI_CHECK` from #69

Design elements this commit produces:
- Final `TVM_FFI_CHECK(cond, ErrorKind)` API
## Reflection
- Quick followup to #69 to finalize the API before external adoption.
