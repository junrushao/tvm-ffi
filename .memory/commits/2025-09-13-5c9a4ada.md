---
author: "Tianqi Chen"
subject: "Move scripts to tests/scripts and extend ASF YAML notifications"
scope:
  - "repo-structure"
  - "repo-config"
impact: "low"
---
# [TEST] Move script location to be consistent (#3)

## TL;DR
- Renames `scripts/benchmark_dlpack.py` to `tests/scripts/benchmark_dlpack.py` and `scripts/run_tests.sh` to `tests/scripts/task_cpp_tests.sh`.
- Extends `.asf.yaml` notifications to include `pullrequests`, `jobs`, and `discussions` channels, and adds comments for `squash_commit_message` and `merge_commit_message` options.

## Impact
- API: none
- Flags/config: `.asf.yaml` gains three new notification channels. Script paths change but no external references depend on them.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces:
- Consistent script location under `tests/scripts/` (all test/benchmark scripts consolidated)

## Reflection
- Minor cleanup: removes unused `import os` from `benchmark_dlpack.py` during the move.
- The PR number (#3) indicates this was one of the first PRs in the new repository.
