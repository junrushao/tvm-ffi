---
author: "Kathryn (Jinqi) Chen"
subject: "Add inline C++ tests for torch DLPackExchangeAPI protocol"
scope:
  - "tests/python/test_dlpack_exchange_api.py"
impact: "low"
---
# [DLPack] Add inline C++ tests for torch DLPack Exchange API protocol (#111)
## TL;DR
- Adds comprehensive unit tests (206 lines) validating PyTorch's `__c_dlpack_exchange_api__` struct-based fast exchange protocol using `torch.utils.cpp_extension.load_inline`.
- Tests cover managed tensor round-trips, non-owning DLTensor conversion, stream queries, and allocator functionality.
## Impact
- API: none (test-only)
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- `DLPackExchangeAPI` struct from commit 22a78943
Design elements this commit produces: none
## Reflection
- Uses inline C++ compilation to avoid GIL issues with `THPVariable_Wrap`, which is a practical workaround.
