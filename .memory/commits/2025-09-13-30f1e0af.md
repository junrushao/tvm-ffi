---
author: "tvm-ffi contributors"
subject: "Establish standalone tvm-ffi repository"
scope:
  - "repo-root"
impact: "high"
---
# [SETUP] Establish tvm-ffi repo
## TL;DR
- Adds top-level repository scaffolding: `.gitignore` (286 lines), `.gitmodules` (dlpack + libbacktrace submodules), `LICENSE` (Apache 2.0), and `NOTICE` file.
- Updates the `3rdparty/dlpack` submodule pointer from `3ea601b` to `addbc8b`.
## Impact
- API: none (no code changes)
- Flags/config: none
- Data formats/schemas: none
## Design Elements
Design elements this commit consumes:
- none
Design elements this commit produces:
- Repository foundation: Apache 2.0 licensing, gitignore, and submodule declarations for tvm-ffi as a standalone project
## Reflection
- This is the initial repo-establishment commit that separates tvm-ffi into its own repository (previously part of the TVM monorepo, as indicated by the PR number series #18xxx in earlier commits).
