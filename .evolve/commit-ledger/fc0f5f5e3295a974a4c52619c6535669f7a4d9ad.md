# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: The template has no dedicated section for infrastructure artifacts (Dockerfiles, CI configs) that are not code-level design elements but still carry architectural significance. The "Key Exports" section is awkward for Dockerfiles -- they have no types/functions/macros, but they do have meaningful structure (base image, installed packages, build args).
- **Wasted effort**: None -- the standard shape fit well for this mixed example/infra/docs commit.
- **Suggested skill change**: None -- the template gap is minor and a "New file" annotation under Key Exports works adequately as a workaround.
