# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The "rename" shape shortcut says "Skip per-file narration" but does not clarify how to handle commits that are rename + behavioral change (as() semantics changed, not just symbol renames). Had to blend rename-style mapping table with standard behavioral analysis. Step 2.2 could add a "rename+semantic" sub-shape.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: In Step 2.2, add a "rename+semantic" shape variant for commits where >50% of the diff is identifier substitution but there is also a meaningful behavioral change. This would signal: produce rename mapping table AND full behavioral analysis, rather than skipping Steps 3-4 entirely.
