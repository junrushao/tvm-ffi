# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The rename shape fast-path says "skip per-file narration" but the procedure still requires full Steps 3.1-3.7 analysis. For a systematic rename, Steps 3.3 (breaking changes) and 3.4 (cross-layer) could have lighter guidance -- the rename itself is the breaking change, and this commit is single-layer C++.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency check was N/A (single-layer C++). The rename shape detection was clear but the procedure does not explicitly state that rename-shape commits with behavioral changes (as<T> semantics changed) should be classified as rename+standard composite rather than pure rename.
- **Suggested skill change**: Add a note under "rename" shape that if the rename is accompanied by behavioral semantic changes (not just name substitution), classify as composite `rename+standard` and require Step 3.3 breaking-change analysis at full depth.
