# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 (breaking changes) and Step 3.4 (cross-layer consistency) added no value for a chore commit that only touches config files and a single Python import. The shape classification "standard" applied full analysis depth, but a "config-only" shape shortcut (similar to "docs-only") would have been more appropriate.
- **Suggested skill change**: Add a `config-only` shape (detection: changes only to `pyproject.toml`, `.pre-commit-config.yaml`, CI YAML, lint scripts, and marker files like `py.typed`) that skips Steps 3.3-3.4 and abbreviates Key Exports to config snippets only.
