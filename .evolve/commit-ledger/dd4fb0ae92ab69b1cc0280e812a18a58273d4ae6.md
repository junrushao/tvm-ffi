# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The core.pyi diff was 458+/56- lines but ~95% was docstring enrichment from stub regeneration. Step 1.1 collects per-file diffs unconditionally; a heuristic to detect "stub regeneration noise" and summarize it rather than reading hundreds of docstring lines would save effort.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency was mostly redundant here -- the C++ changes were only test helper additions, and the Python changes were self-contained in TypeSchema. The check confirmed alignment but added no insight.
- **Suggested skill change**: In Step 1.1, when a `.pyi` file has >200 lines of diff and the commit also touches the corresponding `.pxi` source, flag the `.pyi` as likely stub regeneration and skip detailed diff reading in favor of a summary note ("stub file regenerated with docstring additions").
