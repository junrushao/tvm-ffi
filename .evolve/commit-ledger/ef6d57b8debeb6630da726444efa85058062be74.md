# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None. The standard shape path fit this commit well -- it is neither trivial (14 files, substantial lint/CI scripts) nor large-bringup (<30 files).
- **Template gap**: The template is oriented toward code-level design elements (types, functions, macros). For pure CI/infra commits, the "Key Exports" section has no natural fit for workflows and YAML configs. A "CI/Infra Artifacts" subsection or guidance to list workflow names and their triggers/matrix would help.
- **Wasted effort**: Step 3.3 (breaking changes) and Step 3.4 (cross-layer consistency) added no value for this CI-only commit.
- **Suggested skill change**: In Step 2.2 shape detection, add a `ci-infra` shape for commits touching only `.github/`, `tests/lint/`, `tests/scripts/`, and config files, with guidance to document workflow structure and lint pipeline stages rather than code-level API surface.
