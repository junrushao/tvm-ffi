# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The composite shape classification (standard + rename) is somewhat awkward. Step 2.2 says "pick the primary shape" but this commit is roughly 50/50 between new functionality and renames, making the "primary" call arbitrary. The rename mapping table belongs in TL;DR per the rename fast-path, but the full analysis of new APIs requires the standard path. The guidance could clarify how to blend rename tables into standard-shape output.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: In Step 2.2 composite shapes guidance, add: "For standard + rename composites, include the rename mapping table in TL;DR (as with pure rename shape) and proceed with full standard analysis for new functionality."
