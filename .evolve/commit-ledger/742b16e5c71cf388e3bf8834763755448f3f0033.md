# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The docs-only shape shortcut says to "reframe Step 4 as Mining Key Documentation Artifacts" but the procedure does not specify what that reframing looks like concretely. For this commit, which was 90% docs but had one meaningful code change (adding tensor.h include), the shape classification was ambiguous -- it could be docs-only or standard. A clearer threshold for "docs-only with incidental code changes" would help.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 (breaking changes) and Step 3.4 (cross-layer consistency) added no value for a documentation-focused commit with a single additive include change.
- **Suggested skill change**: Add a note in Step 2.2 docs-only shape: "If the commit also includes trivial code changes that support the documentation (e.g., adding a default include, updating example code to match docs), classify as docs-only and note the code change in Key Exports. Reserve standard shape for commits where the code change is the primary purpose."
