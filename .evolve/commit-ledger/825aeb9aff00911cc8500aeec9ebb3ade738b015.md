# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: The rename mapping table fits naturally into TL;DR but the template does not explicitly designate where it should go. The `{{For rename commits, include a before/after mapping table here}}` placeholder under TL;DR works, but for rename+feature composites, the table duplicates information already in Key Exports (old->new comments). Minor.
- **Wasted effort**: None -- the rename shape shortcut correctly skipped file-by-file narration. Cross-layer check was correctly skipped (single-layer Python commit).
- **Suggested skill change**: None -- skill fit well.
