# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency was borderline -- the commit touches both Python and inline C++ source strings, but the C++ is embedded as a Python string literal, not a separate file. The "single layer" fast-skip doesn't clearly cover this case.
- **Suggested skill change**: Clarify in Step 3.4 that inline C++ source embedded as Python string literals counts as single-layer (Python) unless the commit also modifies standalone C++ header/source files.
