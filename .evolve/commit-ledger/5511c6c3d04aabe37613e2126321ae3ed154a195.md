# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 (breaking changes) and Step 3.4 (cross-layer consistency) were irrelevant for a docs-only commit. The docs-only shape shortcut correctly directs skipping per-file narration, but the procedure still requires running through all Step 3 substeps even when the shape signals no code changes.
- **Suggested skill change**: For docs-only shape, explicitly skip Steps 3.3 and 3.4 (breaking changes, cross-layer consistency) since they only apply to code changes. Add to the shape table: "Skip Steps 3.3-3.4."
