# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification: this is a pure-deletion refactor (0 additions, 144 deletions, 1 file) that removes a key design element. It does not fit "trivial" (it has design impact) but does not need full "standard" depth either. A "removal" shape shortcut -- focused on staleness detection and replacement mapping -- would avoid the wasted effort of Steps 3.1-3.4 invariant analysis on deleted code.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 invariant/extension-point analysis produced little value since the code is being deleted, not modified. The interesting analysis was the staleness check (Step 3.7) and the before/after migration pattern.
- **Suggested skill change**: Add a "removal" shape in Step 2.2 for commits where >80% of diff is deletions and no new code is added. Analysis path: (a) identify what was removed, (b) identify the replacement (from commit message or codebase grep), (c) generate migration mapping, (d) run staleness check. Skip invariant/extension-point analysis.
