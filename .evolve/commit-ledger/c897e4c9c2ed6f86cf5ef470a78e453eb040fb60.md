# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The diff for extension.py was 1061 lines (rename + add). Collecting only the public API signatures via grep was sufficient, but the procedure doesn't explicitly suggest this shortcut for large renamed files.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: In Step 1.1, for large renamed files (>500 LOC), add a note to extract only public function signatures via grep rather than reading the full diff.
