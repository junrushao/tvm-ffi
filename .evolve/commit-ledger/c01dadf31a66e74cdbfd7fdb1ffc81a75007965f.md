# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: The template has no dedicated section for "naming conventions introduced." The `__ffi_init__` convention is arguably as important as the function template itself, but it fits awkwardly under Key Exports (it's not a type or function) and somewhat under Impact. A "Conventions" sub-section in Key Exports would be cleaner.
- **Wasted effort**: None -- standard shape fit well for this 7-file, cross-layer feature commit.
- **Suggested skill change**: Consider adding a "Conventions" bullet type under Key Exports in template-commit-ledger.md, for commits that establish naming or structural conventions (e.g., `__ffi_init__` as the standard init method name). Currently these are captured ad-hoc.
