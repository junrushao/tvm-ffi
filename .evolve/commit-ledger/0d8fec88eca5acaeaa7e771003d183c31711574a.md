# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.5 "Key Exports" asks for public API surface produced, but for a refactor commit the meaningful output is signature changes and new invariants (noexcept contracts), not new types. Had to adapt the section organically rather than following the type/function/macro checklist literally.
- **Template gap**: No dedicated section for "conventions introduced" -- the noexcept-on-move-ops pattern is now a project-wide convention enforced by clang-tidy, but it fits awkwardly under "Key Exports" since it is not a single symbol.
- **Wasted effort**: None -- multi-concern shape correctly triggered abbreviated per-file analysis.
- **Suggested skill change**: For refactor-shaped commits, add guidance under Key Exports to group changes by category (signature changes, convention changes, suppression patterns) rather than by symbol type.
