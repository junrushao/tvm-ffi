# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.4 cross-layer consistency asks about "defaults aligned across layers" which is vague for this commit shape -- the C++ header and Cython extern declarations must agree on struct layout, but the procedure doesn't specifically prompt for ABI struct layout verification between Cython `cdef extern` and C++ struct definitions, which was the actual cross-layer concern here.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: In Step 3.4 (Cross-layer consistency), add a specific check for Cython-C++ commits: "Do Cython `cdef extern` struct declarations match the C++ struct definitions field-by-field (types, order, padding)?" This is the primary cross-layer correctness concern for Python FFI commits and is more actionable than the current "are defaults aligned" phrasing.
