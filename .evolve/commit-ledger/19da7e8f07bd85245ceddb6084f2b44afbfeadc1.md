# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The `docs-only` shape says to reframe Step 4 as "Mining Key Documentation Artifacts" but the template still uses "Key Exports" -- for doc-heavy commits that also carry a non-trivial code fix (the stubgen prefix filtering), the shape classification is ambiguous between `docs-only` and `multi-concern`. Chose `docs-only` since the code change was <10% of total LOC, but the guidance could be clearer on threshold.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: None -- skill fit well.
