# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The `large-bringup` shape says "Skip file-by-file narration" but 80% of this commit's LOC is documentation prose, not code. The shape detection heuristic (>30 files / >3000 LOC) correctly triggered `large-bringup`, but the structured template for bringups (consumed/produced/divergences) was a better fit for the code portions than the docs portions. A `docs-bringup` composite shape would have been cleaner.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 breaking changes analysis was trivial since the rename is confined to example files. The "check for public header signature changes" instruction could short-circuit earlier when the diff touches zero files under `include/`.
- **Suggested skill change**: In Step 2.2 shape detection, add a composite shape `docs-bringup` for commits where >50% of new LOC is under `docs/` but remaining changes include non-trivial code additions. This would allow skipping deep invariant mining for the docs portion while still fully analyzing code additions.
