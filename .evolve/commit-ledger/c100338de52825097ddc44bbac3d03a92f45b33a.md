# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The commit was classified as docs-only but also included non-trivial c_api.h changes (v_char32 removal, DLPackTensorAllocator relocation, forward decl addition) and a version bump. The shape detection heuristic "Only docs/, *.md, *.rst, examples changed" does not account for header changes bundled with doc commits. Had to use docs-only shape but cover the c_api.h changes anyway.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: Refine docs-only shape detection to exclude commits that also modify public C/C++ headers (*.h files under include/). When a doc commit also touches headers, classify as standard with a secondary docs-only note, since header changes can be ABI-impacting even when the primary intent is documentation.
