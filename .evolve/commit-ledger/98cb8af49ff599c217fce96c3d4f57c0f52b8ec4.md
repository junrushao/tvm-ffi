# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The commit has a secondary rename concern (`type_acenstors` -> `type_ancestors`) that spans all layers but is mechanically simple. Step 2.2 composite shape guidance says "note the secondary shape" but doesn't clarify whether the rename mapping table should go in TL;DR or Key Exports. I placed it in TL;DR which felt natural.
- **Template gap**: None.
- **Wasted effort**: None -- the multi-layer check (Step 3.4) was warranted since this commit touches C ABI, C++ source, Cython, and Python simultaneously.
- **Suggested skill change**: None -- skill fit well.
