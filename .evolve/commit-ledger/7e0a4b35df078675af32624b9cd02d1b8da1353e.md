# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: The template has no dedicated section for "ABI layout changes" -- this commit's most important impact is the changed in-memory field layout of container objects. The Impact section covers it, but a structured "ABI Delta" subsection (old layout vs new layout) would make binary-compatibility-breaking commits easier to analyze.
- **Wasted effort**: None.
- **Suggested skill change**: Add an optional "ABI Delta" subsection under Impact for commits that change struct field layouts in public headers. Format: old fields list vs new fields list, with offset annotations if available. Only populated when shape is standard/large-bringup and the commit touches C ABI headers.
