# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The "rename" shape shortcut says "Skip per-file narration" but the commit also contains semantic changes (storage order reversal, new enum, signature change). The multi-concern + rename combination required full analysis despite the rename shortcut. The procedure could clarify that rename-shaped commits with semantic changes should fall through to standard analysis.
- **Template gap**: None.
- **Wasted effort**: None -- the rename mapping table and multi-concern decomposition both contributed to a clear ledger.
- **Suggested skill change**: In Step 2.2, add guidance: "If a commit matches both 'rename' and another shape (e.g., 'multi-concern'), use the rename mapping table but do NOT skip Steps 3-4."
