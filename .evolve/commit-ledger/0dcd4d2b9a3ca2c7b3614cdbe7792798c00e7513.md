# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency check was borderline irrelevant -- the commit touches Python files but only to update embedded C++ string literals (inline compilation snippets), not actual Python API surfaces. The "single-layer fast-skip" heuristic does not cover this edge case where Python files contain embedded C++ code.
- **Suggested skill change**: Refine the cross-layer detection heuristic in Step 3.4: when Python file changes consist entirely of embedded C++ string literals (e.g., `cpp_sources=r"""..."""`), classify as single-layer C++ rather than multi-layer.
