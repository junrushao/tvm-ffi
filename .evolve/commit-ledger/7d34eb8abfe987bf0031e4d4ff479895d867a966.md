# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The large-bringup shape says "skip file-by-file narration" but the Key Exports template still expects per-type detail. For a 59-file root commit, the line between "structured template" and "per-file narration" is blurry. The procedure could specify a maximum Key Exports size (e.g., top-10 types) for large-bringup commits.
- **Template gap**: None.
- **Wasted effort**: Step 3.7 Knowledge Staleness Check is always "None" for the root commit. The procedure could short-circuit this step when no prior knowledge base exists.
- **Suggested skill change**: Add a guard to Step 3.7: "If `$REPO_MAIN/.knowledge/designs/` and `$REPO_MAIN/.knowledge/ADRs/` are empty, write 'None (root commit or empty knowledge base)' and skip the grep."
