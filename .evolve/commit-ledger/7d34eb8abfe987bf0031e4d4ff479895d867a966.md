# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.4 cross-layer consistency check asks to compare C++ and Python layers, but this commit is C++ only (Python bindings are not included in this diff). The single-layer fast-skip handled it, but the step text could explicitly mention that "single-layer" includes commits that only touch one language even if the project is multi-language.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 breaking-change analysis is awkward for an initial bringup commit -- there is no prior API to break. The "additive only" shortcut works but feels like a misnomer when the commit message explicitly discusses ABI incompatibility with a predecessor codebase not present in this repo.
- **Suggested skill change**: For `large-bringup` shape with >10k LOC, consider adding a "Key Exports budget" guideline that caps pseudocode to ~100 lines and explicitly directs the analyst to group related symbols (e.g., all C API functions) rather than listing each individually. The current "~15 most architecturally significant" guidance was helpful but the C ABI layer alone has ~15 functions.
