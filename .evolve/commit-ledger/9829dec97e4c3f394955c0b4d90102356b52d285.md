# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0 does not describe what to do when the input SHA is truncated or has missing characters. Had to fall back to prefix search.
- **Template gap**: None
- **Wasted effort**: None
- **Suggested skill change**: Add a note in Step 0.1 that if `git rev-parse $COMMIT` fails, attempt a prefix match via `git log --all --oneline | grep` before aborting.
