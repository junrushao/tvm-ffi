# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The commit type enum (`feature`, `bugfix`, `refactor`, `performance`, `chore`) has no `test` option, yet this commit is primarily a test addition. Classifying it as `chore` feels imprecise -- `test` would be a more accurate category.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency check was irrelevant -- single-layer Python commit.
- **Suggested skill change**: Add `test` as a commit type option alongside `feature`, `bugfix`, `refactor`, `performance`, `chore`. Test-focused commits are common and distinct from chores.
