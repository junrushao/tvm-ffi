# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.5 Key Exports asks for "macros," "protocols/traits," and "constants/enums" which are C++-centric categories. For a Python-only commit adding a `.pyi` type stub, the guidance did not map well -- the stub declares Python classes and functions, not macros or traits. A "type stub declarations" category would fit better.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency check was not useful here since the commit only touches Python files, and the `.pyi` stub is definitionally a mirror of the Cython module's existing interface.
- **Suggested skill change**: None -- the existing shape classification ("standard") was adequate, and the friction with Key Exports categories was minor enough to work around with a table format.
