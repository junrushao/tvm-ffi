# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None.
- **Upstream skill gap**: None — ledger inputs were sufficient for all 28 commits.
- **Quality bar friction**: Requiring >=1 usage example for docs-only commits (6 of 28) felt forced. The ledgers correctly marked "None" for usage examples on pure doc commits. The skill could explicitly exempt docs-only/chore commit batches from the usage example requirement when the batch is dominated by such commits.
- **Procedure friction**: The add-then-revert pattern (Function.__init__ added in 8786670, reverted in 91fcaa8) required manual reasoning to produce net-zero changes. The planner phase could benefit from explicit "detect reverts within batch" guidance.
- **Suggested skill change**: Add a Step 1.5 "Detect reverts" that identifies commit pairs where one reverts the other within the batch, so the planner can skip net-zero changes automatically.
