# Skill /commit-distill - Self-Evolution
- **Template mismatch**: The stream context (commit 0daaffe) is a small cross-cutting concern -- too small for its own design doc but architecturally significant. It ended up folded into the module system API index. A "micro-design" template for small subsystems (5-10 lines) would avoid this awkwardness.
- **Upstream skill gap**: Commit 6014406 (benchmark chore) had no Key Exports, which is correct. However, its ledger noted a "diff-message discrepancy" but did not capture the actual production change that the benchmark exercises (autodlpack stream integration). This is fine for a benchmark-only commit but means the autodlpack design remains uncaptured.
- **Quality bar friction**: None -- the batch had a clear major feature (module system), a clear ADR (naming convention), and a clear bugfix (JSON strict-aliasing) that mapped cleanly to the templates.
- **Procedure friction**: Step 0 "Resolve $LAST_COMMIT by commit date" fails when all commits share the same commit date (squash-rebased batch). Had to fall back to ancestor relationship checking. The procedure should specify topological ordering as the tiebreaker.
- **Suggested skill change**: In Step 0.1, add: "If multiple commits share the same commit date, resolve $LAST_COMMIT by topological ordering (use `git merge-base --is-ancestor` or `git rev-list --topo-order`) to find the latest descendant."
