---
author: "ctuu"
subject: "Add CMake namespace aliases for source dependency builds"
commit_shape: "trivial"
commit_type: "feature"
potential_duplicate: ""
scope:
  - "packaging"
---
# Add CMake namespace aliases for source dependency builds
## TL;DR
- Adds `tvm_ffi::header`, `tvm_ffi::static`, and `tvm_ffi::shared` CMake ALIAS targets so downstream projects using `add_subdirectory()` can reference TVM FFI via namespaced target names consistent with `find_package()` conventions.

## Key Exports
None

## Impact
- API: New CMake alias targets available when consuming via `add_subdirectory()`.
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- CMake build system -- `.knowledge/designs/0013-packaging.md`

Design elements this commit produces:
None

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
