---
author: "ctuu"
subject: "Add CMake ALIAS targets tvm_ffi::header/shared/static for source dependency builds"
commit_shape: "trivial"
commit_type: "bugfix"
potential_duplicate: ""
has_design_updates: false
scope:
  - "build/cmake"
---
# Add CMake ALIAS targets tvm_ffi::header/shared/static for source dependency builds

## TL;DR
- Adds `tvm_ffi::header`, `tvm_ffi::shared`, `tvm_ffi::static` ALIAS CMake targets in `CMakeLists.txt` and `cmake/Utils/Library.cmake`, so `tvm_ffi_configure_target` works in source-dependency builds (previously only worked with installed package config).

## Key Exports
None

## Impact
- API: none
- Behavioral: `tvm_ffi_configure_target` now usable from source builds without the package config file.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `0013-ffi-packaging-and-distribution`: `tvm_ffi_configure_target`, namespaced CMake targets.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
- `/Users/junrus/Projects/tvm-ffi-knowledge/.knowledge/design-records/0013-ffi-packaging-and-distribution.md`: Note that namespaced ALIAS targets are now created in both source and package builds.

### Stale knowledge references
None
