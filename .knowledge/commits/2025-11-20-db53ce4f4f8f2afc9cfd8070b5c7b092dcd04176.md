---
author: "Yaxing Cai"
subject: "Add kernel library guide documentation"
commit_shape: "docs-only"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/guides"
  - "ffi/containers"
---
# Add kernel library guide documentation
## TL;DR
- Adds `docs/guides/kernel_library_guide.rst` — a comprehensive guide for composing or migrating a kernel library using TVM FFI.
- Adds `GetDataSize` utility function declaration in `container/tensor.h` for computing tensor byte size.
- Registers the guide in `docs/index.rst` and `docs/conf.py`.

## Key Exports

```python
def GetDataSize(tensor: Tensor) -> int:
    """Compute the byte size of a tensor's data buffer."""
    # Interacts with: Tensor.dtype(), Tensor.size(), DLDataType
    # Invariant: tensor must have valid dtype and shape
```

## Impact
- API: new `GetDataSize` C++ utility function (additive)
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Module system (0011-module-system): guide covers `load_module`, function export, stream management.
- Function system (0003-function-system): guide covers `TVM_FFI_DLL_EXPORT_TYPED_FUNC`.
- Container types (0006-containers): `Tensor`, `TensorView`, `GetDataSize`.
- C ABI (0007-c-abi): guide covers `TVMFFIEnvSetStream`/`TVMFFIEnvGetStream`.

Design elements this commit produces:
- Kernel library guide — documentation artifact covering end-to-end kernel library development workflow.

## Usage Examples
N/A — documentation commit.

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
