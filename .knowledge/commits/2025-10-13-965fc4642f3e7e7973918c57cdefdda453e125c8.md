---
author: "Kathryn (Jinqi) Chen"
subject: "Add inline C++ tests for DLPackExchangeAPI protocol and fix dlpack version constant"
commit_shape: "standard"
commit_type: "chore"
scope:
  - "dlpack-interop"
---
# Add inline C++ tests for DLPackExchangeAPI protocol and fix dlpack version constant

## TL;DR
- Adds 206-line `tests/python/test_dlpack_exchange_api.py` with inline C++ tests validating `torch.Tensor.__c_dlpack_exchange_api__` (managed_tensor_from/to, dltensor_from, current_work_stream, managed_tensor_allocator).
- Fixes `__dlpack_version__` in `tensor.pxi` to use `DLPACK_MAJOR_VERSION`/`DLPACK_MINOR_VERSION` constants from `dlpack.h` instead of hardcoded `(1, 1)`.

## Key Exports
None (test-only commit; the version constant fix is an internal correction)

## Impact
- API: none
- Behavioral: `__dlpack_version__` now tracks the actual DLPack header version constants rather than hardcoded values.
- Flags/config: none
- Data formats/schemas: none

## Design Elements

Design elements this commit consumes:
- **DLPackExchangeAPI struct** (introduced in 22a7894) -- tests validate all 5 function pointers in the struct
- **DLPack interop layer** ([0017-dlpack-interop.md](/Users/junrus/Projects/tvm-ffi-knowledge/.knowledge/designs/0017-dlpack-interop.md))

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
