---
author: "tvm-bot"
subject: "Revert hardcoded libbacktrace cmake URL in favor of git submodule at 3rdparty/libbacktrace"
commit_shape: "revert"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "cmake"
  - "3rdparty"
---
# Revert hardcoded libbacktrace cmake URL in favor of git submodule at 3rdparty/libbacktrace

## TL;DR
- Reverts PR #18246 (`b245f1f`) which added `GIT_REPOSITORY` / `GIT_TAG` to `ExternalProject_Add(project_libbacktrace)` in `cmake/Utils/AddLibbacktrace.cmake`.
- Restores the original approach: `ExternalProject_Add` sources from the `3rdparty/libbacktrace` git submodule (no network fetch at configure time).
- Advances the `3rdparty/libbacktrace` submodule pointer from `08f7c7e` to `793921876c` (upstream HEAD), which already incorporates the macOS-compatibility patch that was the motivation for #18246.

## Key Exports

None

## Impact
- API: none
- Behavioral: `cmake/Utils/AddLibbacktrace.cmake` no longer fetches libbacktrace from GitHub at cmake configure time. Builds now consume the pinned submodule at `3rdparty/libbacktrace` (commit `793921876c`), which is upstream HEAD and includes the macOS fix from `tlc-pack:libbacktrace`.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `cmake/Utils/AddLibbacktrace.cmake` — `_libbacktrace_compile()` function controlling `ExternalProject_Add(project_libbacktrace)` source location.
- `3rdparty/libbacktrace` — vendored git submodule used as the build source for the traceback backend referenced in design record `0005-error-system`.

Design elements this commit produces: none

## Usage Examples

None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None — no symbols renamed or removed. The `.knowledge/design-records/0005-error-system.md` references libbacktrace integration but at a behavioral level; the submodule pointer update does not invalidate that description.
