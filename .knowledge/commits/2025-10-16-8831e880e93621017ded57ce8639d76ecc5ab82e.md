---
author: "Junru Shao"
subject: "Fix macOS cp38 skip in wheel build configuration"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "ci-and-lint-pipeline"
---
# Fix macOS cp38 skip in wheel build configuration
## TL;DR
- Moves the macOS cp38 skip from the workflow YAML to `pyproject.toml`'s `[tool.cibuildwheel.macos]` section and removes the redundant workflow-level override, properly skipping Python 3.8 wheels on macOS.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: `test-skip` in `[tool.cibuildwheel.macos]` now includes `cp38-*`
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
