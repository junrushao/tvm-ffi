---
author: "junrushao"
subject: "Move macOS cp38 skip from workflow env to pyproject.toml cibuildwheel config"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "build/ci"
  - "python/packaging"
---
# chore(build): Properly skip macOS cp38 (#152)
## TL;DR
- Removed `CIBW_SKIP: "cp38-macosx_*"` from `.github/workflows/publish_wheel.yml` env vars.
- Added `"cp38-macosx_*"` to the `skip` list in `[tool.cibuildwheel]` section of `pyproject.toml`.
- Consolidates all cibuildwheel skip rules into `pyproject.toml` as the single source of truth.

## Key Exports
None

## Impact
- API: none
- Behavioral: none -- macOS cp38 wheels were already skipped; the skip directive moved from workflow env to pyproject.toml
- Flags/config: `CIBW_SKIP` env var removed from `publish_wheel.yml`; equivalent rule added to `pyproject.toml` `[tool.cibuildwheel].skip`
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
