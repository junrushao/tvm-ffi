---
author: "junrushao"
subject: "Move macOS cp38 skip rule from CI workflow env var into pyproject.toml cibuildwheel config"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "build/packaging"
  - "ci"
---
# chore(build): Properly skip macOS cp38 (#152)
## TL;DR
- Removed `CIBW_SKIP: "cp38-macosx_*"` from `.github/workflows/publish_wheel.yml` env vars.
- Added `"cp38-macosx_*"` to the `skip` list in `pyproject.toml` under `[tool.cibuildwheel]`, consolidating all skip rules in one canonical location.

## Key Exports
None

## Impact
- API: none
- Behavioral: none (same wheels are skipped, just configured in a different place)
- Flags/config: `pyproject.toml` `[tool.cibuildwheel]` `skip` now includes `"cp38-macosx_*"` alongside the existing `"*musllinux*"`
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
