---
author: "TVM FFI team"
subject: "Move cp38-macosx skip rule from CIBW_SKIP env var to pyproject.toml cibuildwheel config"
commit_shape: "ci-infra"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - ".github/workflows"
  - "pyproject.toml"
---
# Move cp38-macosx skip rule from CIBW_SKIP env var to pyproject.toml cibuildwheel config

## TL;DR
- Removes `CIBW_SKIP: "cp38-macosx_*"` from the CI workflow env block (added in d87502a).
- Adds `"cp38-macosx_*"` to the `skip` array under `[tool.cibuildwheel]` in `pyproject.toml` — the canonical location for cibuildwheel skip rules.

## Key Exports
N/A — infrastructure

## Impact
- API: none
- Behavioral: Same net effect as d87502a — Python 3.8 wheels for arm64 macOS are not built. This is a correctness fix for where the skip rule is placed; the `pyproject.toml` config takes effect for all build modes, not just CI.
- Flags/config: `skip` in `[tool.cibuildwheel]` section of `pyproject.toml` now includes `"cp38-macosx_*"`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- CI/CD wheel-publishing pipeline — infrastructure only.

Design elements this commit produces:
- None

## Usage Examples
N/A

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
