---
author: "Junru Shao"
subject: "Suppress two clang-tidy performance-unnecessary-value-param warnings"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/function"
  - "ffi/testing"
---
# Suppress two clang-tidy performance-unnecessary-value-param warnings
## TL;DR
- Added `NOLINT(performance-unnecessary-value-param)` to `TypedFunction<R(Args...)>::operator()` in `function.h` — the variadic parameter pack is intentionally by-value (forwarded via `std::forward`).
- Changed `Optional<TensorView> t` to `const Optional<TensorView>& t` in a testing lambda in `testing.cc` to satisfy clang-tidy.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `TypedFunction<R(Args...)>::operator()` (0004-ffi-function-system) — NOLINT annotation added, no contract change.
- `Optional<TensorView>` (0006-ffi-containers) — const-ref fix in test lambda, no contract change.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
