---
author: "junru"
subject: "Fix broken wheel testing by using test-groups instead of test-extras in cibuildwheel"
commit_shape: "trivial"
commit_type: "bugfix"
potential_duplicate: ""
scope:
  - "packaging"
---
# Fix broken wheel testing by using test-groups instead of test-extras in cibuildwheel
## TL;DR
- Changes `test-extras = ["test"]` to `test-groups = ["test"]` in `[tool.cibuildwheel]` config
- Fixes regression from PR #441 which moved `test` from optional-dependencies to dependency-groups

## Key Exports
None

## Impact
- API: none
- Behavioral: cibuildwheel now correctly installs test dependencies
- Flags/config: `test-groups` replaces `test-extras` in cibuildwheel config
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Python packaging / cibuildwheel config (`.knowledge/designs/0013-packaging.md`)

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
