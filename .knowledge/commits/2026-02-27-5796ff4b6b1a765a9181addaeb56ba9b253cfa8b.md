---
author: "commit-ledger"
subject: "Add comprehensive RecursiveHash test suite and Python binding"
commit_shape: "standard"
commit_type: "feature"
scope:
  - "python-ffi"
  - "structural-equal-hash"
---
# Add comprehensive RecursiveHash test suite and Python binding

## TL;DR
- Exposes `RecursiveHash` to Python via `_ffi_api` typed stub.
- Adds `TestHash` and `TestCustomHash` reflected test fixtures to `tvm_ffi.testing`.
- Adds `test_dataclass_hash.py` with comprehensive test coverage: primitives, NaN canonicalization, containers, field exclusion (`hash(false)`), custom hooks (`__ffi_hash__`), cycle detection, consistency law, DAG scaling.

## Key Exports

**Python `_ffi_api` stub:** `RecursiveHash(value: Any) -> int`

**Testing classes:** `TestHash(v_i64, hash_ignored)` (hash-off field), `TestCustomHash(key, label)` (custom __ffi_hash__)

## Impact
- API: Python-side binding for existing C++ `ffi.RecursiveHash` (additive only).
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `RecursiveHash` from `dataclass.h` (produced by 6b39efb)

Design elements this commit produces: none

## Usage Examples
None (test-only)

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
