---
author: "Junru Shao"
subject: "Add comprehensive RecursiveHash test suite and Python exposure"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/ffi-bindings"
  - "ffi/extra"
---
# Add comprehensive RecursiveHash test suite and Python exposure

## TL;DR
- Exposes `RecursiveHash` to Python via `_ffi_api.py` stub.
- Adds `TestHash` (Hash(false) field exclusion) and `TestCustomHash` (`__ffi_hash__` hook) testing fixtures.
- 971-line test file covering all hash contract invariants including cycle detection, consistency law, DAG scaling, and guard behavior.

## Key Exports

```python
# python/tvm_ffi/_ffi_api.py
def RecursiveHash(value: Any) -> int: ...
    # Interacts with: ffi.RecursiveHash C++ global, __ffi_hash__ TypeAttr

# python/tvm_ffi/testing
class TestHash(Object):
    """Two fields; 'hash_ignored' has Hash(false) exclusion."""
class TestCustomHash(Object):
    """Custom __ffi_hash__ hook — hashes only 'key', ignores 'label'."""
```

## Impact
- API: Additive only.
- Behavioral: none.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `6b39efb` (`dataclass.h`): `RecursiveHash` C++ function.
- `0007-ffi-reflection`: `__ffi_hash__` TypeAttr hook, `Hash(false)` / `Compare(false)` flags.

Design elements this commit produces: none (consumer/test coverage).

## Usage Examples

```python
from tvm_ffi._ffi_api import RecursiveHash, RecursiveEq

a = tvm_ffi.testing.TestIntPair(1, 2)
b = tvm_ffi.testing.TestIntPair(1, 2)
assert RecursiveHash(a) == RecursiveHash(b)  # consistency law
assert RecursiveEq(a, b)
```

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
