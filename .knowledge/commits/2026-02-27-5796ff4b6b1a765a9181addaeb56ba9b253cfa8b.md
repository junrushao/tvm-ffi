---
author: "Junru Shao"
subject: "Add comprehensive RecursiveHash test suite with Python bindings and test fixtures"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - "python/testing"
---
# Add comprehensive RecursiveHash test suite with Python bindings and test fixtures

## TL;DR
- Exposes `RecursiveHash` to Python `_ffi_api` typed stubs.
- Adds `TestHash` (field-level `Hash(false)` exclusion) and `TestCustomHash` (custom `__ffi_hash__` hook) test fixtures to `tvm_ffi.testing`.
- Adds 971-line `test_dataclass_hash.py` covering consistency law, NaN canonicalization, cycle detection, DAG scaling, and hash guard.

## Key Exports

```python
# Python _ffi_api typed stub (new)
def RecursiveHash(_0: Any, /) -> int: ...
    # Interacts with: C++ ffi.RecursiveHash global function

# Testing dataclasses
@register_object("testing.TestHash")
class TestHash(Object):
    key: int
    name: str
    hash_ignored: int  # Hash(false) flag in C++ reflection

@register_object("testing.TestCustomHash")
class TestCustomHash(Object):
    key: int
    label: str
    # Custom __ffi_hash__ hook hashes only key
```

## Impact
- API: None — internal `_ffi_api` symbol and test fixtures only.
- Behavioral: None.
- Flags/config: None.
- Data formats/schemas: None.

## Design Elements
Design elements this commit consumes:
- RecursiveHash API from `6b39efbf` (dataclass.h)
- Reflection system (0008): `refl::hash(false)`, `__ffi_hash__` type attribute hooks
- Python bindings (0012): `_ffi_api` stub pattern, `@register_object`

Design elements this commit produces:
- Testing infrastructure for RecursiveHash (TestHash, TestCustomHash)
- Behavioral contract verification for RecursiveHash consistency law

## Usage Examples

```python
from tvm_ffi import _ffi_api
h1 = _ffi_api.RecursiveHash(obj_a)
h2 = _ffi_api.RecursiveHash(obj_b)
if _ffi_api.RecursiveEq(obj_a, obj_b):
    assert h1 == h2  # consistency law
```

## Reflection

### Design docs to write or update
None.

### Stale knowledge references
None.
