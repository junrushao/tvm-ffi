---
author: "Junru Shao <junrushao@apache.org>"
subject: "Add Taplo TOML formatter to pre-commit lint pipeline"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "ci-and-lint-pipeline"
---
# Add Taplo TOML formatter to pre-commit lint pipeline
## TL;DR
- Adds the `taplo-format` pre-commit hook (v0.9.3 from `ComPWA/taplo-pre-commit`) to `.pre-commit-config.yaml`, enabling automatic TOML formatting (primarily for `pyproject.toml`).

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: TOML files are now auto-formatted by Taplo on pre-commit runs. Developers running `pre-commit run --all-files` will see Taplo applied to `.toml` files.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Pre-commit hook orchestration pattern (`.pre-commit-config.yaml`) -- see [0016-ci-and-lint-pipeline.md](../designs/0016-ci-and-lint-pipeline.md)
- The existing `check-toml` hook (from `pre-commit-hooks`) validates TOML syntax; Taplo complements it by enforcing consistent formatting.

Design elements this commit produces:
- None (extends existing lint pipeline with a new hook entry, no new design element)

## Usage Examples
None

## Reflection

### Design docs to write or update
- `$REPO_MAIN/.knowledge/designs/0016-ci-and-lint-pipeline.md` -- Add `taplo-format v0.9.3` to the pre-commit hook versions table and the flowchart diagram (between `STD` and `RUFF_C`).

### ADRs to write or update
None

### Stale knowledge references
None
