---
author: "dingyaoyao.cs@gmail.com"
subject: "Fix import issue in torch_c_dlpack_ext addon so pre-built wheels are correctly discovered"
commit_shape: "trivial"
commit_type: "bugfix"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "addons/torch_c_dlpack_ext"
  - "python/tvm_ffi"
---
# Fix import issue in torch_c_dlpack_ext addon so pre-built wheels are correctly discovered

## TL;DR
- Fixes import ordering/path issue in `addons/torch_c_dlpack_ext` and `python/tvm_ffi/_optional_torch_c_dlpack.py` so that when `torch_c_dlpack_ext` is installed, its pre-built library path is correctly found.
- Updates `pyproject.toml` dependency spec; tweaks `__init__.py` imports.

## Key Exports
None

## Impact
- API: none
- Behavioral: Fixes the AOT pre-built library discovery path in `_optional_torch_c_dlpack.py`.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: `torch_c_dlpack_ext` package (0016-py-ffi-call-dispatch)
Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
