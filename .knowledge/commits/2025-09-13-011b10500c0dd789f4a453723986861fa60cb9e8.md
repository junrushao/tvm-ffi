---
author: "Tianqi Chen"
subject: "Update documentation URLs and examples for standalone tvm-ffi repo"
commit_shape: "docs-only"
commit_type: "chore"
scope:
  - "docs"
---
# Update documentation URLs and examples for standalone tvm-ffi repo
## TL;DR
- Updates all documentation references from `apache/tvm` (subdir `ffi/`) to `apache/tvm-ffi` (standalone repo).
- Updates code examples in quick_start and packaging guides to use `ffi::Tensor` instead of raw `DLTensor*`.
- Fixes `conf.py` Sphinx config: repository URL and `conf_py_path` now point to the standalone repo layout.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: `docs/conf.py` `html_theme_options["repository_url"]` changed from `https://github.com/apache/tvm` to `https://github.com/apache/tvm-ffi`; `html_context["conf_py_path"]` changed from `/ffi/docs/` to `/docs/`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **Tensor type** (`ffi::Tensor`): doc examples updated to use the higher-level `ffi::Tensor` wrapper instead of raw `DLTensor*`, aligning with the earlier NDArray-to-Tensor rename (see `.knowledge/designs/0008-containers.md`).
- **Module system / `TVM_FFI_DLL_EXPORT_TYPED_FUNC`**: doc examples reference this macro for exporting functions (see `.knowledge/designs/0013-module-system.md`).

Design elements this commit produces:
- None. This is a documentation-only update.

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
