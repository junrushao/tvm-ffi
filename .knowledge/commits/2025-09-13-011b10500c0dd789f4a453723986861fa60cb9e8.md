---
author: "junrushao"
subject: "Update docs URLs and code examples for standalone tvm-ffi repo"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
---
# Update docs URLs and code examples for standalone tvm-ffi repo
## TL;DR
- Repository URLs updated from `apache/tvm` to `apache/tvm-ffi` and clone paths from `tvm/ffi` to `tvm-ffi` across install, quickstart, and packaging docs.
- Sphinx config `conf_py_path` corrected from `/ffi/docs/` to `/docs/` to match the new standalone repo structure.
- Code examples in quickstart and packaging docs updated to use `ffi::Tensor` instead of `DLTensor*`, aligning with the preferred C++ wrapper API.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `Tensor` wrapper type (0006-ffi-containers) -- doc examples updated to show `ffi::Tensor` instead of raw `DLTensor*`
- `TVM_FFI_DLL_EXPORT_TYPED_FUNC` macro (0013-ffi-packaging-and-distribution) -- referenced in quickstart code examples

Design elements this commit produces: None

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
