---
author: "apache/tvm-ffi contributors"
subject: "Update docs to reflect the new standalone tvm-ffi repository"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
---
# Update docs to reflect the new standalone tvm-ffi repository

## TL;DR
- Updates all repository URLs from `https://github.com/apache/tvm` to `https://github.com/apache/tvm-ffi`.
- Fixes clone/cd paths (`cd tvm/ffi` → `cd tvm-ffi`) and the Sphinx `conf_py_path` (`/ffi/docs/` → `/docs/`).
- Updates the quick-start and packaging code examples to use `ffi::Tensor` parameters instead of `DLTensor*` (reflecting the existing NDArray→Tensor rename already captured in 0007-containers).

## Key Exports

None

## Impact
- API: none
- Behavioral: none
- Flags/config: `docs/conf.py` — `repository_url` corrected to `https://github.com/apache/tvm-ffi`; `conf_py_path` corrected to `/docs/`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `0007-containers` (Tensor type) — doc examples updated to reference `ffi::Tensor` instead of `DLTensor*`, consistent with the NDArray→Tensor rename already recorded in the design record.

Design elements this commit produces: none

## Usage Examples

None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
