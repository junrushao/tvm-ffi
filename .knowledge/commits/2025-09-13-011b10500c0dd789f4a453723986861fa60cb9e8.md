---
author: "Tianqi Chen"
subject: "Update docs to reflect standalone tvm-ffi repo (URL, paths, example API)"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "docs"
  - "packaging"
---
# Update docs to reflect standalone tvm-ffi repo (URL, paths, example API)
## TL;DR
- Updates all documentation repo URLs from `apache/tvm` to `apache/tvm-ffi` and clone paths from `tvm/ffi` to `tvm-ffi`.
- Fixes Sphinx `conf.py` to point `repository_url` and `conf_py_path` at the new standalone repo.
- Updates example code snippets in `quick_start.md` and `packaging.md` to use `ffi::Tensor` instead of `DLTensor*`.

Rename mapping:

| Location | Before | After |
|----------|--------|-------|
| `docs/conf.py` repository_url | `https://github.com/apache/tvm` | `https://github.com/apache/tvm-ffi` |
| `docs/conf.py` conf_py_path | `/ffi/docs/` | `/docs/` |
| `docs/get_started/install.md` clone URL | `apache/tvm tvm` | `apache/tvm-ffi` |
| `docs/get_started/install.md` cd path | `cd tvm/ffi` | `cd tvm-ffi` |
| `docs/get_started/quick_start.md` clone URL | `apache/tvm` | `apache/tvm-ffi` |
| `docs/get_started/quick_start.md` cd path | `cd tvm/ffi` | `cd tvm-ffi` |
| `docs/guides/packaging.md` clone URL | `apache/tvm` | `apache/tvm-ffi` |
| `docs/guides/packaging.md` cd path | `cd tvm/ffi` | `cd tvm-ffi` |
| `docs/get_started/quick_start.md` example API | `DLTensor*` | `ffi::Tensor` |
| `docs/guides/packaging.md` example API | `DLTensor*` | `ffi::Tensor` |

## Key Exports
None

## Impact
- API: none
- Behavioral: none -- documentation only
- Flags/config: Sphinx `conf.py` now points at the correct standalone repo for "Edit on GitHub" links
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **Module system** (`.knowledge/designs/0011-module-system.md`) -- `TVM_FFI_DLL_EXPORT_TYPED_FUNC` referenced in updated example code
- **Containers / Tensor** (`.knowledge/designs/0007-containers.md`) -- `ffi::Tensor` replaces `DLTensor*` in doc examples
- **Packaging** (`.knowledge/designs/0013-packaging.md`) -- clone/install instructions updated to match new repo layout

Design elements this commit produces: None

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
