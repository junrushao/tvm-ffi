---
author: "Tianqi Chen"
subject: "Update docs to reflect standalone tvm-ffi repo URL and paths"
commit_shape: "docs-only"
scope:
  - "docs"
  - "packaging"
impact: "trivial"
abi_breaking: false
related_commits: []
---
# Update docs to reflect standalone tvm-ffi repo URL and paths
## TL;DR
- Updates all documentation references from the old monorepo URL (`github.com/apache/tvm`, `cd tvm/ffi`) to the standalone repo (`github.com/apache/tvm-ffi`, `cd tvm-ffi`).
- Updates Sphinx `conf.py` to point `repository_url` and `conf_py_path` at the new standalone repo structure.
- Updates quick-start code examples to use `ffi::Tensor` instead of `DLTensor*` in function signatures shown in docs.

## Key Exports
None

## Impact
- API: none
- ABI: none
- Behavioral: none -- docs-only changes; no runtime code is modified
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **Standalone FFI packaging** (`.knowledge/ADRs/013-standalone-ffi-packaging.md`): The commit is a direct follow-up to the repo split decision, updating user-facing docs to reflect the new standalone `apache/tvm-ffi` repository.
- **Module system / `load_module`** (`.knowledge/designs/0013-module-system.md`): The quick-start and packaging guides describe the module loading workflow; this commit updates the clone/build instructions surrounding those examples.
- **`ffi::Tensor` wrapper** (`.knowledge/designs/0018-dlpack-fast-path.md`): The quick-start example code is updated from raw `DLTensor*` to `ffi::Tensor`, aligning docs with the current API that wraps DLPack tensors in a ref-counted object.

Design elements this commit produces:
- None -- this is a documentation alignment commit; no new design elements are introduced.

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
- `.knowledge/commits/2025-09-01-c695f5f16ab6e54c729fd6338361b555c7daf8b7.md` -- line 29 still references the old `https://github.com/apache/tvm` repository URL for the Sphinx theme configuration. This commit updates that URL to `apache/tvm-ffi`.
