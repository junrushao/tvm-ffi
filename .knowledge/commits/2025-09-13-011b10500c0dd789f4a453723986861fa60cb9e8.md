---
author: "Tianqi Chen"
subject: "Update docs URLs and code snippets to reflect standalone tvm-ffi repo"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
---
# Update docs URLs and code snippets to reflect standalone tvm-ffi repo
## TL;DR
- Migrated all repository URLs from `apache/tvm` to `apache/tvm-ffi` and clone paths from `tvm/ffi` to `tvm-ffi` across install, quick-start, and packaging guides.
- Updated Sphinx config (`conf.py`) repository URL and `conf_py_path` to match the new standalone repo structure.
- Updated code snippets in quick-start and packaging guides to use `ffi::Tensor` instead of `DLTensor*`, aligning docs with the current API.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none — Sphinx theme `repository_url` and `conf_py_path` updated for correct "Edit on GitHub" links
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Tensor type (`ffi::Tensor`) from containers (0006) — doc snippets updated to use `ffi::Tensor` instead of raw `DLTensor*`
- `TVM_FFI_DLL_EXPORT_TYPED_FUNC` macro from function system (0003) — referenced in updated quick-start code snippet

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
