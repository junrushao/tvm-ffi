---
author: "Junru Shao"
subject: "Add comprehensive Any/AnyView tutorial documentation"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/concepts"
  - "python/dataclasses"
---
# Add comprehensive Any/AnyView tutorial documentation
## TL;DR
- New `docs/concepts/any.rst` tutorial (436 lines) covering `Any`/`AnyView` usage patterns, ownership semantics, extraction methods (`cast`/`try_cast`/`as`), ABI boundary rules, `TVMFFIAny` memory layout, atomic vs heap-allocated type tables, and small-string optimization.
- Cross-references added from `docs/concepts/abi_overview.md` and `docs/guides/cpp_lang_guide.md` to the new tutorial via `seealso` directives.
- Minor typographic normalization: em-dashes replaced with ASCII dashes in docstrings of `c_class.py` and `field.py`.

## Key Exports
None -- documentation only, no API changes.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `Any`/`AnyView` ownership and conversion semantics (`0002-ffi-any-value-system`)
- `TVMFFIAny` 16-byte tagged union layout, `TVMFFITypeIndex` enum ranges, atomic vs heap-allocated type dispatch (`0001-ffi-c-abi`)
- `TVMFFIObject` 24-byte heap header and ref-counting (`0003-ffi-object-system`)
- Container types `Map`, `Array`, `String` as documented usage contexts (`0006-ffi-containers`)
- `cast<T>()`, `try_cast<T>()`, `as<T>()` extraction method semantics (`0002-ffi-any-value-system`)

Design elements this commit produces:
- None -- purely documents existing design elements.

## Usage Examples
N/A -- the commit itself is a documentation artifact. The tutorial embedded in `docs/concepts/any.rst` contains extensive inline code examples covering function signatures, container storage, value extraction, nullability checks, ownership transfer, ABI boundary patterns, destruction semantics in C, layout details, and small-string optimization.

## Reflection

### Design docs / ADRs to write or update
None -- this commit is itself a documentation addition that improves coverage of the `Any`/`AnyView` subsystem.

### Stale knowledge references
None
