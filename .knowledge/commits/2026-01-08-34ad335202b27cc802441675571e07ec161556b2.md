---
author: "Junru Shao"
subject: "Add comprehensive Any/AnyView concept documentation with ownership, layout, and extraction patterns"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "any-system"
  - "documentation"
---
# Add comprehensive Any/AnyView concept documentation with ownership, layout, and extraction patterns
## TL;DR
- Adds `docs/concepts/any.rst` (436 lines): a full tutorial on `Any`/`AnyView` covering common usage, ownership semantics, ABI boundary rules, `TVMFFIAny` memory layout, extraction methods (`cast`/`try_cast`/`as`), and small-string optimization.
- Cross-links the new doc from `docs/concepts/abi_overview.md` and `docs/guides/cpp_lang_guide.md` via `seealso` directives; adds it to the Sphinx toctree in `docs/index.rst`.
- Replaces em-dashes with hyphens in two Python docstrings (`c_class.py`, `field.py`) for consistency.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `Any`/`AnyView` type-erased value system ([0003-any-system.md](../designs/0003-any-system.md)) -- the new RST doc comprehensively documents ownership, layout, and extraction patterns already captured in the design doc.
- `TVMFFIAny` C ABI layout ([0001-c-abi.md](../designs/0001-c-abi.md)) -- the layout section reproduces the `TVMFFIAny` struct definition and the atomic/heap type tables.
- `Function` packed calling convention ([0004-function-system.md](../designs/0004-function-system.md)) -- ABI boundary section documents the `AnyView` inputs / `Any` output convention.

Design elements this commit produces:
- New documentation artifact `docs/concepts/any.rst` -- first dedicated concept doc for `Any`/`AnyView`. Covers sections: Common Usage (function signatures, container storage, extracting values, nullability), Ownership (`AnyView` vs `Any`, ABI boundary, C destruction), Layout (tagged union, atomic types table, heap-allocated objects table), Caveats (small-string optimization).

## Usage Examples
N/A -- documentation commit.

## Reflection

### Design docs to write or update
None -- the new `docs/concepts/any.rst` aligns well with the existing knowledge base entry `0003-any-system.md`. The design doc already captures invariants and pseudocode that the user-facing RST tutorial does not need to duplicate.

### ADRs to write or update
None

### Stale knowledge references
None
