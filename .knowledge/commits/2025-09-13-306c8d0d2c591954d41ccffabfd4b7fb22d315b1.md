---
author: "tqchen"
subject: "Add ASF .asf.yaml project configuration for GitHub settings and branch protection"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "ci"
---
# Add ASF .asf.yaml project configuration for GitHub settings and branch protection
## TL;DR
- Adds `.asf.yaml` to configure GitHub repo metadata (description, homepage, labels), branch protection for `dev` (1 required review), and ASF mailing list notifications.
- Adds a trivial blank line in `.github/workflows/ci_test.yml` (no functional change).

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: `.asf.yaml` enables ASF-managed GitHub settings — repo description set to "TVM FFI", homepage to `https://tvm.apache.org/ffi`, labels `ffi`/`gpu`/`machine-learning`; `dev` branch requires 1 approving review; commits notify `commits@tvm.apache.org`, issues notify `discuss-archive@tvm.apache.org`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: None

Design elements this commit produces: None

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
