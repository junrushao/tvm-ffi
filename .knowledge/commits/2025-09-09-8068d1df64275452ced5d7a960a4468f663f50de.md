---
author: "Tianqi Chen"
subject: "Bump version to 0.1.0a9 and xfail Windows load_inline tests"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "python-package"
  - "tests"
---
# Bump version to 0.1.0a9 and xfail Windows load_inline tests
## TL;DR
- Bumps package version from `0.1.0a8` to `0.1.0a9` in `pyproject.toml`.
- Marks four `test_load_inline_cpp*` tests with `@pytest.mark.xfail` on Windows, citing the need to robustify Windows support for the inline C++ compilation path.

## Key Exports
None

## Impact
- API: none
- Behavioral: none (tests still run on Windows but are expected to fail)
- Flags/config: package version string changed from `0.1.0a8` to `0.1.0a9`
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **Module system** (`tvm_ffi.cpp.load_inline`) -- the xfailed tests exercise the inline C++ compilation and module loading path described in `.knowledge/designs/0013-module-system.md`.
- **Python package** -- version metadata in `pyproject.toml`, related to `.knowledge/designs/0014-python-package.md`.

Design elements this commit produces:
- None

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
