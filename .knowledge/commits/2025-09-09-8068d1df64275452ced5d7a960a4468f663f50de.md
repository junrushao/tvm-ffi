---
author: "Tianqi Chen"
subject: "Bump version to 0.1.0a9 and xfail Windows load_inline tests"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/packaging"
  - "python/tvm_ffi/cpp"
---
# Bump version to 0.1.0a9 and xfail Windows load_inline tests
## TL;DR
- Bumps `apache-tvm-ffi` package version from `0.1.0a8` to `0.1.0a9` in `pyproject.toml`.
- Marks all four `test_load_inline_cpp*` tests with `@pytest.mark.xfail` on Windows (`sys.platform.startswith("win")`), with reason "needs to robustify windows support".

## Key Exports
None

## Impact
- API: none
- Behavioral: none — tests now expected-fail on Windows instead of hard-failing
- Flags/config: version string changes from `0.1.0a8` to `0.1.0a9`
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `load_inline` module system (design record `0014-ffi-load-inline`) — tests for this subsystem are marked xfail on Windows

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
