---
author: "TVM FFI team"
subject: "feat: Add __contains__ support to Array via ffi.ArrayContains global function"
commit_shape: "standard"
commit_type: "feature"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: true
scope:
  - "src/ffi"
  - "python/tvm_ffi"
  - "tests"
---
# feat: Add __contains__ support to Array via ffi.ArrayContains global function

## TL;DR
- New C++ global function `ffi.ArrayContains(array, value)` registered via `TVM_FFI_STATIC_INIT_BLOCK` using `AnyEqual` for element comparison
- Python `Array.__contains__(value)` delegates to `_ffi_api.ArrayContains`
- Added 20 C++ test cases and 21 Python tests

## Key Exports

```python
# src/ffi/container.cc (global function registration)
# "ffi.ArrayContains": (ArrayObj*, Any) -> bool
#   Interacts with: AnyEqual (structural equality), Array iteration

# python/tvm_ffi/container.py
class Array:
    def __contains__(self, value: object) -> bool:
        """Check if the array contains a value (via AnyEqual)."""
        # Interacts with: _ffi_api.ArrayContains → ffi.ArrayContains
```

## Impact
- API: Additive — `Array.__contains__` now supported in Python; `ffi.ArrayContains` new global function
- Behavioral: `value in array` now works for Python Array objects
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- 0007-containers — `ArrayObj`, `Array`
- 0009-structural-eq-hash — `AnyEqual` used for element comparison in `ArrayContains`
- 0004-function-system — global function registration via `TVM_FFI_STATIC_INIT_BLOCK`

Design elements this commit produces:
- 0007-containers — adds `__contains__` to Python `Array`

## Usage Examples
```python
from tvm_ffi import Array
arr = Array([1, 2, 3])
assert 2 in arr        # True
assert 5 not in arr    # True
```

## Reflection

### Design docs / ADRs to write or update
- `/Users/junrus/Projects/tvm-ffi-knowledge/.knowledge/design-records/0007-containers.md` — add `Array.__contains__` (uses `AnyEqual`) to the Python API surface

### Stale knowledge references
None
