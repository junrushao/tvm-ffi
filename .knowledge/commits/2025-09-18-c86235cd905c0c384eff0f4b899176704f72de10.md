---
author: "Junru Shao"
subject: "Remove unused OBJECT_INDEX reverse-lookup dict from Cython object registry"
commit_shape: "trivial"
scope:
  - "python-bindings"
impact: "trivial"
abi_breaking: false
related_commits: []
---
# Remove unused OBJECT_INDEX reverse-lookup dict from Cython object registry
## TL;DR
- Removes the `OBJECT_INDEX` module-level `cdef dict` from `python/tvm_ffi/cython/object.pxi` and the line in `_register_object_by_index` that populated it.
- `OBJECT_INDEX` mapped Python class to type index but was never read anywhere in the codebase -- pure dead code removal.

## Key Exports
None

## Impact
- API: none
- ABI: none
- Behavioral: none -- `OBJECT_INDEX` was write-only; no code path ever queried it.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **Cython Object Type Registry** (`OBJECT_TYPE` list, `_register_object_by_index`): the commit modifies the registration function but does not change the forward-lookup path. See `.knowledge/designs/0014-python-bindings.md` and `.knowledge/designs/object-system.md`.

Design elements this commit produces:
- None. This is a pure removal of dead code.

## Usage Examples
None

## Reflection

### Design docs to write or update
- `.knowledge/designs/0014-python-bindings.md` -- Remove the `cdef dict OBJECT_INDEX = {}` line from the Cython registry code snippet (line 219) and update the description of `_register_object_by_index` to note it only populates `OBJECT_TYPE`.
- `.knowledge/designs/object-system.md` -- Remove the mention of `OBJECT_INDEX` from the "Object Type Registry" paragraph (line 208) which currently says "`OBJECT_INDEX` (dict: class -> type_index) map between type indices and Python classes. `_register_object_by_index` populates both."

### ADRs to write or update
None

### Stale knowledge references
- `.knowledge/designs/0014-python-bindings.md` (line 219): references `cdef dict OBJECT_INDEX = {}` which no longer exists.
- `.knowledge/designs/object-system.md` (line 208): references `OBJECT_INDEX` as part of the object type registry, which no longer exists.
