---
author: "Junru Shao"
subject: "Remove unused OBJECT_INDEX dict from Cython object registration"
commit_shape: "trivial"
commit_type: "refactor"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
---
# Remove unused OBJECT_INDEX dict from Cython object registration
## TL;DR
- Removed the `OBJECT_INDEX` cdef dict and its population in `_register_object_by_index` from `python/tvm_ffi/cython/object.pxi`.
- The dict mapped object classes to their type indices but had zero consumers anywhere in the codebase.

## Key Exports
None

## Impact
- API: none — `OBJECT_INDEX` was a cdef module-level variable, not part of any public API.
- Behavioral: none — no code path read from `OBJECT_INDEX`.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Object type registration in Cython (`OBJECT_TYPE` list, `_register_object_by_index`): see [0012-python-bindings](.knowledge/design-records/0012-python-bindings.md). The `OBJECT_TYPE` list (index -> class) remains as the sole registration lookup. The reverse mapping (`OBJECT_INDEX`, class -> index) was populated but never queried.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
