---
author: "TVM FFI contributors"
subject: "Add 'Common Usage' section to Tensor concept documentation"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/concepts"
---
# Add 'Common Usage' section to Tensor concept documentation

## TL;DR
- Adds a new "Common Usage" section to `docs/concepts/tensor.rst` covering kernel signatures, tensor metadata query APIs, and allocation/stream APIs.
- Reorganizes the table of contents: renames "Tensor APIs" → "Common Usage" and reorders sections.
- Net change: 192 additions, 183 deletions (mostly reorganization + new content in tensor.rst).

## Key Exports

None — docs-only commit.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `0007-containers` — documents `Tensor`/`TensorView` allocation and usage APIs.
- `0012-env-api` — documents stream context APIs (`StreamContext`, `use_raw_stream`, `get_raw_stream`).
- `0001-c-abi` — documents `TVMFFITensorCreateUnsafeView` and `TVMFFITensorAllocate` C ABI entry points.

Design elements this commit produces: None.

## Usage Examples

None — docs-only commit.

## Reflection

### Design docs / ADRs to write or update
None — this commit is itself a docs improvement pass.

### Stale knowledge references
None.
