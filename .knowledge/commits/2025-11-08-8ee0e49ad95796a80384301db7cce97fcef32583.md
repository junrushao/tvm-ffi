---
author: "Tianqi Chen"
subject: "Bump torch-c-dlpack-ext to 0.1.2 and switch JIT build to -O3"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "dlpack-interop"
---
# Bump torch-c-dlpack-ext to 0.1.2 and switch JIT build to -O3
## TL;DR
- Changes default JIT compiler optimization flag from `-O2` to `-O3` in `_build_optional_torch_c_dlpack.py`.
- Bumps `torch_c_dlpack_ext` package version from `0.1.1` to `0.1.2`.
- Reorders `CurrentWorkStream` method above `ManagedTensorToPyObjectNoSync` in the C++ template string (no functional change).

## Key Exports
None

## Impact
- API: none
- Behavioral: JIT-compiled torch-c-dlpack extension is now compiled with `-O3` instead of `-O2`
- Flags/config: default cflags changed from `-O2` to `-O3` for non-Windows JIT builds
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Torch C DLPack JIT build pipeline per `.knowledge/designs/0017-dlpack-interop.md`

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
