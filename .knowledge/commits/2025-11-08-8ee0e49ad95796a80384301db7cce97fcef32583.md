---
author: "Tianqi Chen"
subject: "Bump torch_c_dlpack_ext to O3 optimization and version 0.1.2"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "0013-packaging"
  - "0015-dlpack-exchange-api-struct"
---
# Bump torch_c_dlpack_ext to O3 optimization and version 0.1.2
## TL;DR
- Change default compiler optimization flag from `-O2` to `-O3` for the torch_c_dlpack_ext ninja build on non-Windows platforms.
- Bump `torch_c_dlpack_ext` package version from 0.1.1 to 0.1.2.
- Reorder `CurrentWorkStream` method within `TorchDLPackExchangeAPI` struct (cosmetic, no behavioral change).

## Key Exports
None

## Impact
- API: none
- Behavioral: none -- commit message notes evals show no noticeable improvement, but aligns with AOT build flags
- Flags/config: default cflags for non-Windows ninja build changed from `-O2` to `-O3` in `_generate_ninja_build()`
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `TorchDLPackExchangeAPI` struct (see `.knowledge/ADRs/0015-dlpack-exchange-api-struct.md`) -- method reordered within the struct, no contract change
- Ninja build generation in `_build_optional_torch_c_dlpack.py` (see `.knowledge/designs/0013-packaging.md`) -- cflags modified

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
