---
author: "wrongtest"
subject: "Allow tvm_ffi.device() to accept numpy and torch scalar as device id"
commit_shape: "standard"
scope:
  - "python-bindings"
impact: "low"
abi_breaking: false
related_commits: []
---
# Allow tvm_ffi.device() to accept numpy and torch scalar as device id
## TL;DR
- Updates `tvm_ffi.device()` to accept numpy integers and torch scalar tensors as the device id argument.

## Key Exports
None (usability improvement)

## Impact
- API: `tvm_ffi.device(dev_type, dev_id)` now accepts numpy/torch scalar types for `dev_id`.
- ABI: none
- Behavioral: More flexible device id specification.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Device creation API
- Number conversion protocols

Design elements this commit produces: none

## Usage Examples
```python
import numpy as np
dev = tvm_ffi.device("cuda", np.int32(0))
```

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
