---
author: "Tianqi Chen"
subject: "Leverage DLPack exchange API in from_dlpack when available"
commit_shape: "standard"
commit_type: "refactor"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
---
# Leverage DLPack exchange API in from_dlpack when available
## TL;DR
- Updates `from_dlpack` in Cython tensor handling to prefer the `DLPackExchangeAPI` struct when available via `__c_dlpack_exchange_api__`
- Falls back to legacy `__dlpack__`/`__dlpack_device__` protocol when exchange API is not present
- Streamlines the tensor conversion path by using the unified exchange API

## Key Exports
```python
# Cython tensor.pxi: updated from_dlpack path
# Priority: __c_dlpack_exchange_api__ > __dlpack__ protocol
# Interacts with: DLPackExchangeAPI struct (design-record 0007, 0012)
```

## Impact
- API: None
- Behavioral: `from_dlpack()` now uses more efficient exchange path when available
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- `DLPackExchangeAPI` struct / `__c_dlpack_exchange_api__` dunder (design-record 0007, 0012)
- DLPack `_no_sync` convention (design-record 0006)

Design elements this commit produces: None

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
