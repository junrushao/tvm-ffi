---
author: "Junru Shao"
subject: "Extract stub generation into standalone doc (stubgen.rst); update DOC_URL in consts.py"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/packaging"
  - "python/tvm_ffi/stub"
---
# Extract stub generation into standalone doc (stubgen.rst); update DOC_URL in consts.py

## TL;DR
- Extracts 175 lines of stub-gen docs from `docs/packaging/python_packaging.rst` into the new `docs/packaging/stubgen.rst` (400 lines) — a standalone stub generation guide.
- `python/tvm_ffi/stub/consts.py`: `DOC_URL` updated from `python_packaging.html#stub-generation-tool` to `stubgen.html`.

## Key Exports

None — docs-only (with a trivial URL update in `consts.py`).

## Impact
- API: none
- Behavioral: `tvm-ffi-stubgen` CLI now points to `stubgen.html` for its help URL.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `0020-stub-gen`: The stub generation pipeline documented in the new `stubgen.rst`.

Design elements this commit produces: None — docs reorganization only.

## Usage Examples

N/A — docs-only.

## Reflection

### Design docs / ADRs to write or update
- None — the new `docs/packaging/stubgen.rst` is the documentation artifact.

### Stale knowledge references
None.
