---
author: "junru.shao"
subject: "Reorder Python packaging doc sections for user-facing flow emphasis"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/packaging"
  - "docs"
---
# Reorder Python packaging doc sections for user-facing flow emphasis
## TL;DR
- Reorder `python_packaging.rst` sections: "Build a Python wheel" now precedes "Export C++ to Python", emphasizing the packaging workflow over bottom-up technical order.
- Add a new "Load the Library" subsection documenting `load_lib_module` parameters (`package`, `target_name`) and platform-specific library naming.
- Fix cross-reference label typo `sec-subgen` -> `sec-stubgen` in both `python_packaging.rst` and `cpp_tooling.rst`.

## Key Exports
None -- documentation-only commit.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `tvm_ffi_configure_target` CMake helper (design record 0013-ffi-packaging-and-distribution)
- `load_lib_module` Python API (design record 0013-ffi-packaging-and-distribution)
- `tvm-ffi-stubgen` tool and `sec-stubgen` cross-reference label (design record 0018-ffi-stubgen)

Design elements this commit produces: None

## Usage Examples
None -- documentation-only commit. The doc itself now contains a `load_lib_module` usage example in the new "Load the Library" subsection:

```python
from tvm_ffi.libinfo import load_lib_module

LIB = load_lib_module(
    package="my-ffi-extension",
    target_name="my_ffi_extension",
)
```

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
