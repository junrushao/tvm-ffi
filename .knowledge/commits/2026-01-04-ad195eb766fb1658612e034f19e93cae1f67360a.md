---
author: "junrushao"
subject: "Reorder Python packaging doc to lead with wheel building, fix cross-ref label"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "packaging"
  - "docs"
---
# Reorder Python packaging doc to lead with wheel building, fix cross-ref label

## TL;DR
- Reorders `docs/packaging/python_packaging.rst` to place "Build a Python wheel" before "Export C++ to Python", emphasizing the packaging workflow over the bottom-up implementation order.
- Adds `load_lib_module` usage documentation with parameter descriptions to the "Build Python Wheel" section.
- Fixes RST cross-reference label typo: `sec-subgen` -> `sec-stubgen` in `cpp_tooling.rst`.

## Key Exports
None -- documentation-only changes, no code symbols added or changed.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `load_lib_module` (documented in [0013-packaging.md](../designs/0013-packaging.md)) -- the new "Load the Library" subsection documents its `package` and `target_name` parameters with platform-specific library naming rules.
- `tvm_ffi_configure_target` CMake function -- the "Build Python Wheel" section mentions it but defers stub-generation details (`STUB_DIR`, `STUB_INIT`) to the later stub-generation section, simplifying the initial presentation.
- Wheel auditing workflow (`auditwheel`, `delocate-wheel`, `delvewheel`) -- moved as-is within the reordered structure.

Design elements this commit produces:
- None -- no new design elements, only doc reorganization.

## Usage Examples
None -- documentation reordering with no new code.

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
