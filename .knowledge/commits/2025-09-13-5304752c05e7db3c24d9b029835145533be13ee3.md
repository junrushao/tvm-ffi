---
author: "tqchen"
subject: "Run linters (isort, black, ruff) to comply with new CI lint pipeline"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "python-bindings"
  - "ci-infra"
---
# Run linters (isort, black, ruff) to comply with new CI lint pipeline
## TL;DR
- Applies isort, black, and ruff auto-fixes across all Python sources (import ordering, blank lines, unused variables/imports, `is None` style).
- Adds `[tool.ruff]` configuration to `pyproject.toml` (include paths, per-file ignores, max-args).
- Updates `tests/scripts/task_lint.sh` to run isort, ruff, and reorder lint steps.
- Minor test fixes: renames duplicate `test_device` to `test_device_dlpack_device_type`, removes stale `tvm.testing.main()` call in `test_dtype.py`, changes inline build directory to `./build/build_add_one`.

## Key Exports
None

## Impact
- API: none
- Behavioral: none -- all changes are formatting, import ordering, and lint compliance. The build directory change in `test_load_inline.py` (`./build_add_one` -> `./build/build_add_one`) relocates test artifacts but does not affect public API.
- Flags/config: new `[tool.ruff]` section in `pyproject.toml` governs which files ruff checks and per-file lint suppressions (`F401` in `__init__.py`, `E741` in tests, `max-args=10`).
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
