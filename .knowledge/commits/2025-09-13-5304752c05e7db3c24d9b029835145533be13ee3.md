---
author: "tqchen"
subject: "Run isort/black/ruff lint fixes and add ruff config to CI"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - "tests/python"
  - "build/packaging"
---
# Run isort/black/ruff lint fixes and add ruff config to CI
## TL;DR
- Reorders imports across ~25 Python files to comply with isort (alphabetical, grouped by stdlib/third-party/local).
- Adds `[tool.ruff]` configuration to `pyproject.toml` (include patterns, per-file ignores for `F401`/`E741`, `max-args=10`).
- Fixes ruff warnings: removes unused imports/variables, replaces `== None` with `is None`, removes dead code, renames duplicate test function.
- Adds isort and ruff checks to `tests/scripts/task_lint.sh`.

## Key Exports
None

## Impact
- API: none
- Behavioral: none (test function `test_device` renamed to `test_device_dlpack_device_type` to fix duplicate name; test build directory changed from `./build_add_one` to `./build/build_add_one`)
- Flags/config: new `[tool.ruff]` section in `pyproject.toml` — `F401` suppressed in `__init__.py`, `E741` suppressed in tests, max function args set to 10
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
