---
author: "tqchen"
subject: "Run linters (isort, ruff, black) to comply with new CI lint checks"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/ffi-bindings"
  - "python/packaging"
  - "tests"
  - "ci-infra"
---
# Run linters (isort, ruff, black) to comply with new CI lint checks
## TL;DR
- Reorder imports across all Python files to satisfy `isort` (stdlib before third-party before local).
- Fix `ruff` lint warnings: remove unused imports (`subprocess`, `platform`, `pytest`), unused variables (`y`, `z`, `ext`, `dev`), use `is None` instead of `== None`, convert f-string with no placeholders to plain string, remove dead `if __name__ == "__main__"` block, rename duplicate test function `test_device` to `test_device_dlpack_device_type`.
- Add `[tool.ruff]` configuration to `pyproject.toml` and extend `task_lint.sh` with `isort --check` and `ruff check` steps.

## Key Exports
None

## Impact
- API: none
- Behavioral: none (all changes are formatting/lint, no logic changes)
- Flags/config: `pyproject.toml` gains `[tool.ruff]` section scoping ruff to `python/**/*.py` and `tests/**/*.py`, ignoring `F401` in `__init__.py` and `E741` in tests, with `max-args = 10`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
