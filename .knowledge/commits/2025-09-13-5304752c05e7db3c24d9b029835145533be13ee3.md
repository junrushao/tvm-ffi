---
author: "tqchen"
subject: "Run lint fixes across codebase to comply with new CI lint checks"
scope:
  - "packaging-and-build"
impact: "trivial"
---
# Run lint fixes across codebase to comply with new CI lint checks
## TL;DR
- Adds ruff configuration to `pyproject.toml` (include paths, per-file ignores for `__init__.py` F401 and tests E741, max-args=10) and adds isort/ruff checks to `tests/scripts/task_lint.sh`.
- Applies mechanical lint fixes across ~30 Python files: import reordering (isort compliance), removal of unused variables, f-string without placeholder fixes, and list formatting (black compliance).
- Minor lint fixes in `check_asf_header.py` itself (import order, unused variable removal).

## Impact
- API: none
- Flags/config: New `[tool.ruff]` section in `pyproject.toml` configuring ruff for `python/` and `tests/` directories.
- Data formats/schemas: none

## Design Elements
Design elements this commit **consumes**:
- **Packaging and build** (`.knowledge/designs/0016-packaging-and-build.md`): Extends the lint toolchain configuration in `pyproject.toml`.

Design elements this commit **produces**:
- None (purely mechanical lint compliance).

## Reflection

* **Design docs to write**: None

* **ADRs to write**: None

* **Design diagrams to draw**: None

* **Skill evolution (`/commit-ledger`)**: None

## Self-Evolution of Skill /commit-ledger
Trivial chore commits like this are well-handled by the current procedure. The Step 2 "trivial commit" shortcut correctly applies here.
