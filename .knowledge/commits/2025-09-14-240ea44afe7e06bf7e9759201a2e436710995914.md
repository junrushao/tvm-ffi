---
author: "Tianqi Chen"
subject: "Add compiler integration guide and improve module/export documentation"
scope:
  - "documentation-site"
  - "module-export-system"
impact: "low"
---
# Add compiler integration guide and improve module/export documentation
## TL;DR
- Adds `docs/guides/compiler_integration.md` (140 lines) covering how compilers target the TVM FFI ABI: exporting functions via `TVM_FFI_DLL_EXPORT_TYPED_FUNC`, bundling modules with import trees, and loading via `load_module`.
- Improves inline documentation: clarifies `TVM_FFI_DLL_EXPORT_TYPED_FUNC` macro docstring (adds `__tvm_ffi_<ExportName>` note, removes confusing examples), and annotates the `ProcessLibraryBin` binary layout comment with optional value brackets.

## Impact
- API: none (documentation and comment changes only)
- Flags/config: none
- Data formats/schemas: The `ProcessLibraryBin` layout comment now clarifies that `[val: bytes]` is present only for non-`_lib` keys.

## Design Elements
Design elements this commit **consumes**:
- **Module export system** (`.knowledge/designs/0008-module-export-system.md`): The compiler integration guide documents the `TVM_FFI_DLL_EXPORT_TYPED_FUNC` macro and `__tvm_ffi_` symbol prefix convention.
- **Module system** (`.knowledge/designs/0013-module-system.md`): Documents `load_module` and the import tree binary layout in `ProcessLibraryBin`.

Design elements this commit **produces**: none (documentation of existing design elements)

## Reflection

* **Design docs to write**: None
* **ADRs to write**: None
* **Design diagrams to draw**: None
* **Skill evolution (`/commit-ledger`)**: None

## Self-Evolution of Skill /commit-ledger
None.
