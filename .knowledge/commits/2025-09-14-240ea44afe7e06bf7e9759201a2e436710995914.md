---
author: "junru"
subject: "Add compiler integration guide documenting FFI ABI conventions for kernel and graph compilers"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "0011-module-system"
  - "0001-c-abi"
---
# Add compiler integration guide documenting FFI ABI conventions for kernel and graph compilers
## TL;DR
- New `docs/guides/compiler_integration.md` (140 lines) covering three integration scenarios: kernel language compilers, graph compilers, and custom modules with binary serialization.
- Minor documentation fixes: improved Doxygen for `TVM_FFI_DLL_EXPORT_TYPED_FUNC` macro (clarifies symbol name is `__tvm_ffi_<ExportName>`), fixed `library_module.cc` comment to use bracket notation for optional `val`, and corrected lint script error message path.

## Key Exports
None -- documentation-only commit with comment fixes.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **`__tvm_ffi_` symbol prefix convention** ([ADR 0010](../ADRs/0010-ffi-symbol-prefix-convention.md)) -- the guide teaches integrators to prefix symbols with `__tvm_ffi_`
- **`TVM_FFI_DLL_EXPORT_TYPED_FUNC` macro** ([0004-function-system](../designs/0004-function-system.md)) -- referenced as the C++ shortcut for exposing FFI symbols; Doxygen now explicitly notes the final symbol name
- **`TVMFFIAny` packed calling convention** ([0001-c-abi](../designs/0001-c-abi.md), [0003-any-system](../designs/0003-any-system.md)) -- the C code example demonstrates extracting `DLTensor` from `TVMFFIAny*` args
- **`TVMFFIEnvGetStream`** ([0011-module-system](../designs/0011-module-system.md)) -- demonstrated for obtaining the current device stream in generated code
- **`TVMFFIErrorSetRaisedFromCStr`** ([0005-error-system](../designs/0005-error-system.md)) -- used in the C example for error propagation
- **`ModuleObj` subclassing** ([0011-module-system](../designs/0011-module-system.md)) -- "Advanced: Custom Modules" section describes subclassing `ModuleObj` with `kind`, `GetPropertyMask`, `GetFunction`, `SaveToBytes`
- **`__tvm_ffi__library_bin` binary layout** ([0011-module-system](../designs/0011-module-system.md)) -- documented for module export/loading with CSR import tree format

Design elements this commit produces: none

## Usage Examples
None -- the documentation itself is the usage example. The guide contains a complete C code snippet showing `ReadDLTensorPtr` + `__tvm_ffi_add_one` implementation using the packed calling convention, plus prose guidance for graph compiler and custom module integration.

## Reflection

### Design docs to write or update
None -- this commit adds a guide that already references existing design docs. The guide's content is consistent with `0011-module-system.md` and `ADR 0010`.

### ADRs to write or update
None

### Stale knowledge references
None
