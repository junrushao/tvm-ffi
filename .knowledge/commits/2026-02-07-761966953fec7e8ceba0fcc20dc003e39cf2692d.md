---
author: "junru"
subject: "Switch Python type checker from mypy to ty (Astral)"
commit_shape: "standard"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - ".github/workflows"
  - "pyproject.toml"
  - "tests/python"
---
# Switch Python type checker from mypy to ty (Astral)
## TL;DR
- Replaces `mypy` with Astral's `ty` type checker in pre-commit hooks, pyproject.toml dev dependencies, and CI workflow.
- Migrates `[tool.mypy]` configuration to `[tool.ty.*]` sections in pyproject.toml.
- Removes redundant `# type: ignore` comments throughout Python source and test files now unnecessary under ty's stricter/different inference.

## Key Exports
None

## Impact
- API: none — no public Python or C++ API changes.
- Behavioral: none — all Python source changes are annotation/comment cosmetic adjustments (removing `# type: ignore` or restructuring optional-import patterns).
- Flags/config: `[tool.mypy]` → `[tool.ty.environment]`, `[tool.ty.src]`, `[tool.ty.analysis]` in `pyproject.toml`; pre-commit hook changes from `mirrors-mypy` remote to `local` entry using `uvx ty check`.
- Data formats/schemas: none.

## Design Elements
Design elements this commit consumes:
- Python package layout (`python/tvm_ffi/`) — all source files touched only to remove stale `# type: ignore` comments; no logic changes.

Design elements this commit produces:
- None — tooling migration only.

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
