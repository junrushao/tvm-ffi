---
author: "junru"
subject: "Migrate type checker from mypy to ty (Astral)"
commit_shape: "ci-infra"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "tooling"
  - "python-bindings"
---
# Migrate type checker from mypy to ty (Astral)
## TL;DR
- Replaces `mypy` with Astral's `ty` type checker across pre-commit config, `pyproject.toml`, and CI workflows
- Adds `# type: ignore` / comment adjustments across ~37 Python files to satisfy `ty`'s stricter checking
- Moves `[tool.mypy]` config section to `[tool.ty.*]` sections in `pyproject.toml`

## Key Exports
N/A -- infrastructure

## Impact
- API: none
- Behavioral: none -- type checking only
- Flags/config: `[tool.ty.environment]`, `[tool.ty.src]`, `[tool.ty.analysis]` replace `[tool.mypy]` in `pyproject.toml`
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Pre-commit hook infrastructure
- CI workflow definitions (`.github/workflows/ci_test.yml`)

Design elements this commit produces: none

## Usage Examples
N/A

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
