---
author: "tqchen"
subject: "Remove --no-as-needed linker flag from the optional Torch C DLPack extension build script"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/packaging"
  - "build/cmake"
---
# Remove --no-as-needed linker flag from the optional Torch C DLPack extension build script

## TL;DR

- Drops `-Wl,--no-as-needed` from the Linux `default_ldflags` in `_build_optional_torch_c_dlpack.py`.
- Updates the macOS comment to remove the stale reference to `--no-as-needed`.
- No functional change to macOS; Linux now omits the flag (allowing the linker to use default as-needed semantics).

## Key Exports

None

## Impact

- API: none
- Behavioral: Linux linker now uses default as-needed semantics when building the optional Torch C DLPack extension; libraries no longer forced into the link output.
- Flags/config: `-Wl,--no-as-needed` removed from `default_ldflags` for Linux in `python/tvm_ffi/utils/_build_optional_torch_c_dlpack.py`.
- Data formats/schemas: none

## Design Elements

Design elements this commit consumes:
- `0012-ffi-python-package` — the Torch DLPack addon build script lives under `python/tvm_ffi/utils/`.
- `0013-ffi-packaging-and-distribution` — Linux RPATH and linker flag conventions for shared library builds.

Design elements this commit produces: none

## Usage Examples

None

## Reflection

### Design docs / ADRs to write or update

None

### Stale knowledge references

None
