---
author: "Junru Shao"
subject: "Add tvm_ffi.Function.__init__ for wrapping Python callables as FFI Functions"
commit_shape: standard
commit_type: feature
potential_duplicate: "91fcaa8bd261d55255fe658ce026ce6840547234"
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - "ffi/function"
---
# Add tvm_ffi.Function.__init__ for wrapping Python callables as FFI Functions
## TL;DR
- Adds `Function.__init__(self, func)` in Python/Cython to wrap arbitrary Python callables as FFI Functions
- Adds `_PackedFunc_Init` C++ helper to create a Function from a Python callback handle
- Note: This commit was reverted by 91fcaa8

## Key Exports
```python
class Function:
    def __init__(self, func: Callable) -> None: ...
    # Interacts with: _PackedFunc_Init C++ helper, TVMFFIPyCallManager callback mechanism
```

## Impact
- API: adds `Function(callable)` constructor in Python (later reverted)
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Function system (0003-function-system)
- Python bindings (0012-python-bindings)

Design elements this commit produces:
- `Function.__init__` Python constructor (reverted in 91fcaa8)

## Usage Examples
```python
import tvm_ffi
f = tvm_ffi.Function(lambda x, y: x + y)
assert f(1, 2) == 3
```

## Reflection

### Design docs to write or update
None — reverted

### Stale knowledge references
None
