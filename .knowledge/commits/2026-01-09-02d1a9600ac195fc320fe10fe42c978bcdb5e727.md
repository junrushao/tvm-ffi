---
author: "Guan-Ming (Wesley) Chiu"
subject: "Add starts_with and ends_with methods to String"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
scope:
  - "0007-containers"
---
# Add starts_with and ends_with methods to String
## TL;DR
- Adds `starts_with()` and `ends_with()` methods to `tvm::ffi::String` with overloads for `String`, `const char*`, `std::string_view`, and raw pointer+length.
- Adds comprehensive C++ tests for both methods.

## Key Exports

```python
class String:
    def starts_with(self, prefix: Union[str, String]) -> bool:
        """Check if string starts with prefix."""
        # Invariant: returns True for empty prefix
        # Interacts with: std::memcmp for byte-level comparison
        ...

    def ends_with(self, suffix: Union[str, String]) -> bool:
        """Check if string ends with suffix."""
        # Invariant: returns True for empty suffix
        # Interacts with: std::memcmp for byte-level comparison
        ...
```

## Impact
- API: additive -- two new methods on `tvm::ffi::String`
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- String container (`0007-containers.md`)

Design elements this commit produces:
- `String::starts_with()` and `String::ends_with()` -- new public API on the String container

## Usage Examples

```cpp
tvm::ffi::String s("hello world");
assert(s.starts_with("hello"));    // true
assert(s.ends_with("world"));      // true
assert(!s.starts_with("World"));   // false (case-sensitive)
```

## Reflection

### Design docs to write or update
- `.knowledge/designs/0007-containers.md` -- add `starts_with`/`ends_with` to String API surface listing

### ADRs to write or update
None

### Stale knowledge references
None
