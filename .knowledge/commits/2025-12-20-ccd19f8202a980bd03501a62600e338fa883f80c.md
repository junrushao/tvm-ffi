---
author: "Junru Shao"
subject: "Introduce tvm_ffi_configure_target and tvm_ffi_install CMake helpers"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "build/packaging"
---
# Introduce tvm_ffi_configure_target and tvm_ffi_install CMake helpers

## TL;DR
- Adds `tvm_ffi_configure_target()` and `tvm_ffi_install()` CMake functions (259 LOC) for simplified C++ extension packaging
- Simplifies example CMakeLists.txt from 49 to 3 lines

## Key Exports
```cmake
# cmake/Utils/Library.cmake
tvm_ffi_configure_target(TARGET my_lib SOURCES src/lib.cc)
tvm_ffi_install(TARGET my_lib DESTINATION lib)
# Interacts with: tvm-ffi-config, CMake install system
```

## Impact
- API: New CMake helper functions for extension packaging
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Build system: CMake library infrastructure

Design elements this commit produces:
- `tvm_ffi_configure_target` and `tvm_ffi_install` CMake helpers

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
