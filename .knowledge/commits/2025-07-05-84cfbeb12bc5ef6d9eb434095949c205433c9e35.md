---
author: "Twice <twice@apache.org>"
subject: "Replace recursive for_each_dispatcher with C++17 fold expression"
commit_shape: "trivial"
commit_type: "refactor"
potential_duplicate: ""
scope:
  - "ffi/base-details"
---
# Replace recursive for_each_dispatcher with C++17 fold expression
## TL;DR
- Replaced the recursive two-specialization `for_each_dispatcher` template in `base_details.h` with a single struct using `std::index_sequence` and a C++17 fold expression.
- Net effect: -7 LOC, simpler template instantiation, identical runtime behavior.

## Key Exports
None -- internal implementation detail only. Public interface of `details::for_each` is unchanged.

## Impact
- API: none
- Behavioral: none -- identical calling convention and semantics
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `details::for_each` / `details::for_each_dispatcher` -- internal variadic argument iteration utility in `include/tvm/ffi/base_details.h`, used by `PackedArgs::Fill` (`include/tvm/ffi/function.h:283`) to set packed FFI call arguments.
- C++17 fold expressions and `std::index_sequence` from the standard library.

Design elements this commit produces:
- None -- no new design elements. Internal simplification only.

## Usage Examples
None -- trivial refactor with no API change.

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
