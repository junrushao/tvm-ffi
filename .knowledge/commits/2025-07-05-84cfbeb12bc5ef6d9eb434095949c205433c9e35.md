---
author: "Twice"
subject: "Replace recursive template for_each with C++17 fold expression"
commit_shape: "trivial"
commit_type: "refactor"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/base-details"
---
# Replace recursive template for_each with C++17 fold expression
## TL;DR
- Replaces recursive `for_each_dispatcher` template specialization (two structs, recursive + base case) with a single struct using `std::index_sequence` and a C++17 fold expression.
- Net -7 LOC in `include/tvm/ffi/base_details.h`. No behavioral change; the public `details::for_each(f, args...)` signature is preserved.

## Key Exports
None

## Impact
- API: none -- `details::for_each` is an internal utility in `tvm::ffi::details` namespace; its signature is unchanged.
- Behavioral: none -- fold expression evaluates in left-to-right order, matching the previous recursive instantiation order.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `tvm::ffi::details::for_each` -- internal variadic-pack iteration utility used by `Function::operator()` via `PackedArgsSetter` (`function.h:283`). Part of `0004-ffi-function-system` function call infrastructure.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
