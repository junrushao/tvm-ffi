---
author: "Junru Shao"
subject: "Docs: tweak ABI overview heading, add global function retrieval note, fix example code block markers"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/concepts"
  - "docs/packaging"
  - "examples/python_packaging"
---
# Docs: tweak ABI overview heading, add global function retrieval note, fix example code block markers
## TL;DR
- Rename `docs/concepts/abi_overview.md` heading from "ABI Specification" to "ABI Overview".
- Add a `.. note::` block to `docs/packaging/python_packaging.rst` documenting how to retrieve global functions via `tvm_ffi.get_global_func` (Python), `TVMFFIFunctionGetGlobal` (C), and `tvm::ffi::Function::GetGlobal` (C++), with a Python usage snippet.
- Refactor `examples/python_packaging/src/extension.cc` to properly bracket the `[global_function.begin]`/`[global_function.end]` code markers around only the `AddOne` registration, separating it from the `RaiseError` registration block.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `refl::GlobalDef().def(name, fn)` pattern for global function registration (design record `0007-ffi-reflection`, `0004-ffi-function-system`)
- `tvm_ffi.get_global_func` Python API (design record `0012-ffi-python-package`)
- `TVMFFIFunctionGetGlobal` C ABI entry (design record `0001-ffi-c-abi`)
- `tvm::ffi::Function::GetGlobal` C++ API (design record `0004-ffi-function-system`)

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
