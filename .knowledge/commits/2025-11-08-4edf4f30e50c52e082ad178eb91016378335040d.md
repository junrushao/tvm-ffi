---
author: "Junru Shao"
subject: "Expose TypeAttr lookup via _lookup_type_attr Cython API"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/c-api"
  - "python/tvm_ffi"
---
# Expose TypeAttr lookup via _lookup_type_attr Cython API
## TL;DR
- Adds `_lookup_type_attr(type_index, attr_key)` Cython function exposing C++ `TypeAttrDef` columns to Python
- Adds corresponding C API `TVMFFIObjectTypeLookupAttr` (renamed from prior internal usage)
- Anticipates usecases for `__repr__`, `__metadata__` in stubgen and dataclass workflows

## Key Exports
```python
# python/tvm_ffi/cython/object.pxi
def _lookup_type_attr(type_index: int, attr_key: str) -> Any:
    """Look up a type attribute by type index and attribute key."""
    # Interacts with: TypeAttrDef<T> C++ registration (design-record 0008)
    # Returns None if attribute not found for the type
```

## Impact
- API: New `_lookup_type_attr` function in Cython core; C API signature refined
- Behavioral: None (no type attributes defined yet)
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Reflection / `TypeAttrDef` (design-record 0008)
- C ABI (design-record 0007)

Design elements this commit produces:
- `_lookup_type_attr` Python-side accessor for C++ type attributes

## Usage Examples
```python
from tvm_ffi.core import _lookup_type_attr
result = _lookup_type_attr(type_index, "__repr__")  # returns registered attr or None
```

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
