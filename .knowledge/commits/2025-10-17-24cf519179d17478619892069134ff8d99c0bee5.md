---
author: "TVM FFI team"
subject: "Add StreamContext, get_raw_stream, use_raw_stream, use_torch_stream to Python public API and expand Sphinx reference docs"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
  - "python/tvm_ffi"
---
# Add StreamContext, get_raw_stream, use_raw_stream, use_torch_stream to Python public API and expand Sphinx reference docs

## TL;DR
- Adds `StreamContext`, `get_raw_stream`, `use_raw_stream`, `use_torch_stream` to `tvm_ffi.__all__` and removes duplicate `Device` / `Object` entries.
- Expands `docs/reference/python/index.rst` with more module members documented via Sphinx `autofunction`/`autoclass`.
- Minor doc-string and formatting fixes in `_tensor.py` and `device.pxi`.

## Key Exports
None — no new public symbols introduced; existing symbols made explicitly public via `__all__`.

## Impact
- API: `StreamContext`, `get_raw_stream`, `use_raw_stream`, `use_torch_stream` are now in `tvm_ffi.__all__` (previously accessible but not listed).
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `0012-env-api` — `StreamContext`, `get_raw_stream`, `use_raw_stream`, `use_torch_stream` are stream-context APIs from the env layer now surfaced in `__all__`.
- `0013-python-package` — `__all__` is the public surface of the `tvm_ffi` package.

Design elements this commit produces:
- None

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
