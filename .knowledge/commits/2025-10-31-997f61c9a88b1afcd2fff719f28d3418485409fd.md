---
author: "yifeif@nvidia.com"
subject: "Add CUDA C++ loading example and docs on ABI/glibc compatibility for kernel distributors"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
  - "examples"
  - "CONTRIBUTING.md"
---
# Add CUDA C++ loading example and docs on ABI/glibc compatibility for kernel distributors

## TL;DR
- New `examples/quickstart/load/load_cuda.cc`: C++ example loading/executing a CUDA FFI function with a custom DLPack allocator.
- New `docs/guides/cpp_packaging.md`: Documents glibc versioning challenges, manylinux approach, and Docker images for cross-platform binary distribution.
- `CONTRIBUTING.md`: Adds "Setting Up Pre-commit Hooks" section with hook versions and troubleshooting.

## Key Exports
None (documentation and examples only)

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `load_module()` C++ API (0011-module-system) — used in `load_cuda.cc` example
- `Tensor::FromEnvAlloc` (0012-env-api) — used in CUDA allocator example

Design elements this commit produces: none

## Usage Examples

From `examples/quickstart/load/load_cuda.cc`:
```cpp
// Load a CUDA FFI module
ffi::Module mod = ffi::load_module("my_kernel.so");
ffi::Function my_fn = mod.GetFunction("my_func");

// Set up DLPack tensor with custom allocator
TVMFFISetDLPackManagedTensorAllocator(my_allocator);
ffi::Tensor t = ffi::Tensor::FromEnvAlloc(shape, dtype, device);
my_fn(t);
```

## Reflection

### Design docs / ADRs to write or update
None — documentation produced in this commit.

### Stale knowledge references
None
