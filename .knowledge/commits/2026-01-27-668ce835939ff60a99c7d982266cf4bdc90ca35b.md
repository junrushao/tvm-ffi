---
author: "Guan-Ming (Wesley) Chiu"
subject: "Fix handle empty metadata in get_global_func_metadata"
commit_shape: standard
commit_type: bugfix
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - "ffi/reflection"
---
# Fix handle empty metadata in get_global_func_metadata
## TL;DR
- Fixes `get_global_func_metadata` to handle `None` metadata gracefully instead of crashing
- Returns empty dict when metadata is None rather than attempting to access attributes on None

## Key Exports
```python
def get_global_func_metadata(name: str) -> dict:
    # Invariant: returns {} when function has no metadata (metadata is None)
    ...
```

## Impact
- API: none
- Behavioral: `get_global_func_metadata` no longer raises when metadata is None
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Reflection metadata access (0008-reflection)
- Python registry (0012-python-bindings)

Design elements this commit produces: none

## Usage Examples
Before: `get_global_func_metadata("func_without_metadata")` raised an error.
After: returns `{}`.

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
