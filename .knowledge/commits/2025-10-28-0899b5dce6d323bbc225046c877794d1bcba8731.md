---
author: "Junru Shao"
subject: "Add clang-tidy CI job to mainline-only workflow"
commit_shape: "ci-infra"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "ci"
---
# Add clang-tidy CI job to mainline-only workflow

## TL;DR
- New `.github/workflows/ci_mainline_only.yml` (62 lines) adds a clang-tidy job that runs on push to `main`/`main-staged` and on pull requests targeting those branches.
- Uses `uv run --no-project --with "clang-tidy==21.1.1"` to invoke `tests/lint/clang_tidy_precommit.py`.

## Key Exports
N/A — infrastructure

## Impact
- API: none
- Behavioral: clang-tidy checks (`clang-analyzer-security.ArrayBound`, `modernize-macro-to-enum`, `bugprone-*`, etc.) are now enforced in CI.
- Flags/config: `clang-tidy==21.1.1` pinned.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `tests/lint/clang_tidy_precommit.py` lint script (see `CLAUDE.md`).

Design elements this commit produces: none — CI infrastructure.

## Usage Examples
N/A

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
