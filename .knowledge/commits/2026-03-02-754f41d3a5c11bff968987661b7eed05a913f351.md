---
author: "Junru Shao"
subject: "Add TypeSchema type converter with function-pointer dispatch"
commit_shape: "large-bringup"
commit_type: "feature"
potential_duplicate: ""
scope:
  - "python-bindings"
---
# Add TypeSchema type converter with function-pointer dispatch
## TL;DR
- Introduces `type_check.pxi` (1032 lines) implementing a TypeSchema-based type converter system in Cython with function-pointer dispatch for converting Python values to FFI types.
- Updates `type_info.pxi` with TypeSchema construction from reflection metadata.
- Adds 3399-line test suite (`test_type_check.py`) covering all type conversion scenarios.

## Key Exports

```python
class TypeSchema:
    """Describes how to convert a Python value to an FFI type."""
    # Contains function pointers for type checking and conversion
    # Interacts with: TVMFFIFieldInfo, reflection type metadata
    # Extension: new type converters added by registering TypeSchema entries

def convert_value(schema: TypeSchema, value: Any) -> CAny: ...
    # Dispatches to schema's function pointer for type conversion
```

## Impact
- API: New internal TypeSchema system for Python-to-FFI type conversion.
- Behavioral: Enables precise type checking and conversion at field assignment time.
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Reflection type metadata -- `.knowledge/designs/0008-reflection.md`
- Cython bindings -- `.knowledge/designs/0012-python-bindings.md`

Design elements this commit produces:
- TypeSchema type converter system

## Usage Examples
None -- internal API used by field setters.

## Reflection

### Design docs to write or update
- `.knowledge/designs/0012-python-bindings.md` -- document TypeSchema type converter system.

### ADRs to write or update
None

### Stale knowledge references
None
