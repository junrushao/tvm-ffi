---
author: "unknown"
subject: "Enable comprehensive Ruff rule coverage and apply auto-fixes across Python codebase"
commit_shape: "standard"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - "build/packaging"
  - "tests/lint"
---
# chore: Basic Ruff rule coverage (#20)
## TL;DR
- Expands `pyproject.toml` Ruff config with rules UP (pyupgrade), PL (pylint), I (isort), RUF, NPY, F (pyflakes), PTH (pathlib), D (pydocstyle); removes obsolete `[tool.black]` and `[tool.isort]` sections.
- Applies auto-fixes across all Python files: adds missing docstrings, terminates docstrings with periods, migrates `os.path`/`glob` to `pathlib.Path`, replaces `typing.List`/`Mapping`/`Sequence` with `collections.abc`/builtins, sorts `__all__` lists alphabetically.
- Deletes obsolete `tests/lint/git-clang-format.sh` and `tests/scripts/task_lint.sh` (superseded by pre-commit).

## Key Exports
None

## Impact
- API: none -- all changes are internal style/docstring fixes, no public signatures changed
- Behavioral: none -- `os.path` -> `pathlib.Path` migration preserves semantics (return types coerced back to `str` at boundaries)
- Flags/config: Ruff rule set expanded in `pyproject.toml`; `[tool.black]` and `[tool.isort]` sections removed (tooling migrated to ruff)
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Python bindings layer (0012-python-bindings): touched nearly every `.py` file in `python/tvm_ffi/` for style conformance
- Module system (0011-module-system): `module.py` docstring and style updates
- Containers (0006-containers): `container.py` docstring and import modernization

Design elements this commit produces: none -- purely cosmetic/tooling changes

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
- `/Users/junrus/Projects/tvm-ffi-knowledge/.knowledge/design-records/0012-python-bindings.md` line 280: uses `os.path.join(os.path.dirname(__file__), ...)` in a usage example, which now diverges from the codebase convention of `pathlib.Path`. Severity: `low` (cosmetic example update, 1 file).
