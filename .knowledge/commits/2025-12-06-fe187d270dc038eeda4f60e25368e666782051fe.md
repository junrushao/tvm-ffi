---
author: "Junru Shao <junrushao@apache.org>"
subject: "Fix clang-tidy warnings in test_c_env_api.cc: C-style casts, missing reserve, lock_guard"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "ci-and-lint-pipeline"
---
# Fix clang-tidy warnings in test_c_env_api.cc: C-style casts, missing reserve, lock_guard
## TL;DR
- Replaces C-style casts with `static_cast<>` in `test_c_env_api.cc` to fix clang-tidy `cppcoreguidelines-pro-type-cstyle-cast` warnings.
- Adds `threads.reserve(num_threads)` to avoid reallocations.
- Replaces `std::lock_guard` with `std::scoped_lock` for consistency.

## Key Exports
None

## Impact
- API: None.
- Behavioral: None (test-only changes).
- Flags/config: None.
- Data formats/schemas: None.

## Design Elements
Design elements this commit consumes:
- **CI lint pipeline** ([0016-ci-and-lint-pipeline.md](../designs/0016-ci-and-lint-pipeline.md)): Fixes clang-tidy warnings from the CI run.

Design elements this commit produces:
- None.

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
