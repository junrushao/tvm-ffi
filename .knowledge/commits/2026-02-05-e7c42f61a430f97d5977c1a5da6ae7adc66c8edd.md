---
author: "Junru Shao"
subject: "Extract exception handling into standalone doc page with cross-reference updates"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/concepts"
  - "ffi/error"
---
# Extract exception handling into standalone doc page with cross-reference updates
## TL;DR
- New standalone `docs/concepts/exception_handling.rst` created by extracting exception handling content from `func_module.rst` (194 lines added, 68 removed from source).
- Cross-references updated in 5 other concept docs (`abi_overview`, `any`, `object_and_class`, `tensor`, `func_module`) to point to the new page.
- Documentation for error code `-2` (frontend errors / `EnvErrorAlreadySet`) was removed from public docs; only error code `-1` is now documented.

## Key Exports
None — documentation-only change.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Error protocol (`ErrorObj`, `Error`, `TVM_FFI_THROW`, `TVM_FFI_CHECK`, `TVMFFISafeCallType` return codes) — see `0005-ffi-error-protocol`
- Function calling convention (`TVMFFISafeCallType`) — see `0004-ffi-function-system`
- Object system (reference counting, `Object`/`ObjectRef`) — see `0003-ffi-object-system`

Design elements this commit produces:
- None (documentation reorganization only)

## Usage Examples
None — documentation change.

## Reflection

### Design docs / ADRs to write or update
None.

### Stale knowledge references
- `$REPO_MAIN/.knowledge/design-records/0001-ffi-c-abi.md` — references error code `-2` and `EnvErrorAlreadySet` in return code semantics (lines 9, 50, 163-164). The public docs now only document `-1`. The code still supports `-2` so the design record is technically accurate, but there is a docs-vs-knowledge divergence. Severity: `low`.
- `$REPO_MAIN/.knowledge/design-records/0005-ffi-error-protocol.md` — documents `EnvErrorAlreadySet` and return code `-2` (lines 20, 108, 160, 165, 185, 193). Same divergence as above. Severity: `low`.
- `$REPO_MAIN/.knowledge/design-records/0004-ffi-function-system.md` — references `EnvErrorAlreadySet` and `-2` return code in safe call macro description (lines 347-348). Severity: `low`.
