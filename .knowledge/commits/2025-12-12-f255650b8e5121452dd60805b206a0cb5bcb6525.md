---
author: "Junru Shao"
subject: "Introduce tvm_ffi.libinfo.load_lib_module for library info access"
commit_shape: "standard"
scope:
  - "module-system"
  - "python-bindings"
impact: "low"
abi_breaking: false
related_commits: ["8dcaec1f"]
---
# Introduce tvm_ffi.libinfo.load_lib_module for library info access
## TL;DR
- Adds `tvm_ffi.libinfo.load_lib_module` as a convenience function for loading shared library modules with library info metadata.

## Key Exports

**Python Functions:**
- `tvm_ffi.libinfo.load_lib_module(name)` -- Load a library module by name, resolving paths via library info.

## Impact
- API: New `load_lib_module` function.
- ABI: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Module system (`load_module`)

Design elements this commit produces:
- `load_lib_module` convenience API.

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
