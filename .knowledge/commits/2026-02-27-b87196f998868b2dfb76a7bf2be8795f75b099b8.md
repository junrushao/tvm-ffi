---
author: "Junru Shao"
subject: "Expose recursive compare to Python with comprehensive tests"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
scope:
  - "python-bindings"
  - "reflection"
---
# Expose recursive compare to Python with comprehensive tests
## TL;DR
- Exposes `ffi.RecursiveCompare` C++ function to Python via `_ffi_api`.
- Adds Python testing helpers for recursive compare.
- Adds 1272-line comprehensive test suite (`test_dataclass_compare.py`).

## Key Exports

```python
def recursive_compare(lhs: Object, rhs: Object) -> bool: ...
    # Interacts with: ffi.RecursiveCompare, reflection field iteration
```

## Impact
- API: `ffi.RecursiveCompare` exposed to Python.
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Recursive compare from dataclass.cc (6b39efb)
- Python FFI API wiring

Design elements this commit produces:
- Python-accessible recursive compare

## Usage Examples
```python
import tvm_ffi
a = tvm_ffi.testing.TestIntPair(1, 2)
b = tvm_ffi.testing.TestIntPair(1, 2)
assert tvm_ffi.testing.recursive_compare(a, b)
```

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
