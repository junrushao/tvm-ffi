---
author: "unknown"
subject: "Merge Windows torch_c_dlpack CI job into unified workflow"
commit_shape: "ci-infra"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ci/torch-c-dlpack"
---
# Merge Windows torch_c_dlpack CI job into unified workflow
## TL;DR
- Consolidates the standalone `.github/workflows/torch_c_dlpack_windows.yml` into the main `.github/workflows/torch_c_dlpack.yml` as a new `build_wheels_windows` job.
- Updates the `upload_pypi` job dependency list to include `build_wheels_windows` alongside Linux, macOS, and source jobs.
- Deletes the now-redundant `torch_c_dlpack_windows.yml` workflow file.

## Key Exports
N/A — infrastructure.

## Impact
- API: none
- Behavioral: Windows wheel builds now run as part of the unified `torch_c_dlpack` workflow dispatch rather than a separate workflow. PyPI upload now gates on all three platforms (Linux, Windows, macOS) plus source.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Torch DLPack addon build infrastructure (`.github/workflows/torch_c_dlpack.yml`, `addons/torch_c_dlpack_ext/build_aot_wheels.bat`). Relates to [0012-ffi-python-package](../design-records/0012-ffi-python-package.md) (torch DLPack addon section).

Design elements this commit produces: none — purely consolidating existing CI jobs.

## Usage Examples
N/A

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
- `.knowledge/commits/2025-11-10-d1595c459f5c5a17ed80ab4b152f24d2c8a65a49.md` — references `.github/workflows/torch_c_dlpack_windows.yml` which is deleted by this commit. Severity: `low` (filename mention only, no semantic impact).
