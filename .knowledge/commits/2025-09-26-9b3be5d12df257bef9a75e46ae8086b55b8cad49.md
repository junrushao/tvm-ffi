---
author: "Junru Shao"
subject: "Add test for field `__doc__` being None when not explicitly set"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tests"
  - "ffi/reflection"
---
# Add test for field `__doc__` being None when not explicitly set
## TL;DR
- Adds `test_attribute_no_doc()` to `tests/python/test_object.py`, verifying that reflection-registered fields without explicit docstrings expose `__doc__` as `None`.
- Follow-up test for PR #60 (`43ffe57`), which fixed docstring attachment to only set `__doc__` when explicitly defined.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Reflection field descriptor system (`ObjectDef<T>::def_ro`/`def_rw` with optional docstring parameter) — `0007-ffi-reflection`
- Python type system field descriptors (`TypeField.__doc__`) — `0015-ffi-python-type-system`
- Testing infrastructure (`tvm_ffi.testing.TestObjectBase` registered via `src/ffi/extra/testing.cc`)

Design elements this commit produces: None

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
