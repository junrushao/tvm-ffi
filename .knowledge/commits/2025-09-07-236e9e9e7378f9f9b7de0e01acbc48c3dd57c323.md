---
author: "Tianqi Chen"
subject: "Temporarily skip load_inline tests on non-Linux and add ninja dependency"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/packaging"
  - "tests/python"
---
# Temporarily skip load_inline tests on non-Linux and add ninja dependency
## TL;DR
- Mark four `test_load_inline_cpp*` tests with `@pytest.mark.xfail` on non-Linux platforms pending cross-platform support.
- Add `ninja` as a dependency to the `torch`, `cpp`, and `test` optional-dependency groups in `pyproject.toml`.

## Key Exports
None

## Impact
- API: none
- Behavioral: `load_inline` tests now report `xfail` instead of hard failure on macOS/Windows.
- Flags/config: new `cpp` optional-dependency group (`ninja`) in `pyproject.toml`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Module system (`tvm_ffi.cpp.load_inline`, `tvm_ffi.module.Module`) -- test-only usage, no changes to the API.
- Packaging configuration (`pyproject.toml` optional-dependencies) from design record `0013-ffi-packaging-and-distribution`.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
