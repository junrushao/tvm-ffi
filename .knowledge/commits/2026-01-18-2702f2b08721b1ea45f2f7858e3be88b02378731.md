---
author: "junchi"
subject: "Add PaddlePaddle quickstart docs and load example"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/quickstart"
  - "docs/concepts"
  - "examples/quickstart"
---
# Add PaddlePaddle quickstart docs and load example
## TL;DR
- Adds PaddlePaddle 3.3.0 as a supported ML framework across quickstart, tensor, stable C ABI, and README docs.
- Adds new example script `examples/quickstart/load/load_paddle.py` demonstrating `tvm_ffi.load_module` with PaddlePaddle tensors.
- Documents PaddlePaddle's DLPack 1.2/1.3 support and C DLPack exchange API (`__c_dlpack_exchange_api__` / `__dlpack_c_exchange_api__`).

## Key Exports
None -- documentation and example additions only.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `tvm_ffi.load_module` (0011-ffi-module-system) -- used in the new PaddlePaddle example to load a compiled shared library.
- DLPack tensor interop (0006-ffi-containers) -- PaddlePaddle tensors exchanged via DLPack protocol.
- `__dlpack_c_exchange_api__` / `__c_dlpack_exchange_api__` (0012-ffi-python-package) -- PaddlePaddle 3.3.0 implements the C DLPack exchange API for zero-copy tensor sharing.

Design elements this commit produces: none.

## Usage Examples
PaddlePaddle load example (`examples/quickstart/load/load_paddle.py`):

```python
import tvm_ffi
mod = tvm_ffi.load_module("build/add_one_cuda.so")

import paddle
x = paddle.tensor([1, 2, 3, 4, 5], dtype=paddle.float32, device="cuda")
y = paddle.empty_like(x)
mod.add_one_cuda(x, y)
print(y)
```

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
