---
author: "Tianqi Chen"
subject: "Improve torch DLPack fallback behavior and dtype conversion"
commit_shape: "standard"
commit_type: "bugfix"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - "addons/torch_c_dlpack"
---
# Improve torch DLPack fallback behavior and dtype conversion
## TL;DR
- Ensures `tvm_ffi.convert()` always returns correct `tvm_ffi.dtype` when passed a dtype argument
- Improves torch DLPack fallback behavior when prebuilt extension is not available
- Fixes dtype dispatch in Cython `dtype.pxi` to properly handle torch dtype objects via `__dlpack_data_type__` protocol

## Key Exports
```python
# In _convert.py: enhanced convert() path for dtype objects
# In cython/dtype.pxi: improved dispatch for torch.dtype -> DLDataType
# In cython/function.pxi: fallback path improvements
# Interacts with: __dlpack_data_type__ protocol (design-record 0012)
```

## Impact
- API: None
- Behavioral: `tvm_ffi.convert(torch.float32)` now correctly returns `tvm_ffi.DataType` instead of failing
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- `__dlpack_data_type__` protocol (design-record 0012)
- `TVMFFIPyArgSetter` dispatch (design-record 0012)

Design elements this commit produces: None

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
