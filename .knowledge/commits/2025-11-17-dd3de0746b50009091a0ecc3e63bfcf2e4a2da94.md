---
author: "Tianqi Chen"
subject: "Add tests for Optional<TensorView> to ensure correct behavior"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/containers"
  - "tests"
---
# Add tests for Optional<TensorView> to ensure correct behavior
## TL;DR
- Adds C++ test registration (`TestOptionalTensorView`) and Python test for `Optional<TensorView>` round-trip through FFI.
- Validates that `None` and valid tensors are correctly handled by `Optional<TensorView>`.

## Key Exports
None

## Impact
- API: none
- Behavioral: none — test-only
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `Optional<TensorView>` (0005-type-traits-protocol, 0006-containers): validates the `TensorView` TypeTraits specialization with `Optional` wrapper.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
