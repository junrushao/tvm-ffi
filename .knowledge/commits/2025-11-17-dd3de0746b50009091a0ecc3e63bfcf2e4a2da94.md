---
author: "Tianqi Chen"
subject: "Add test for Optional<TensorView> pass-by-value in C++ and Python"
commit_shape: "test-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "tests/python"
  - "src/ffi/testing"
---
# Add test for `Optional<TensorView>` pass-by-value

## TL;DR
- Registers `testing.optional_tensor_view_has_value` in `testing.cc` as a global function taking `Optional<TensorView>` by value.
- Adds `test_optional_tensor_view()` Python test that verifies `None` returns false and a numpy array (with `__dlpack__`) returns true.

## Key Exports
None

## Impact
- API: none
- Behavioral: none (test-only)
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `0007-containers`: `TensorView` with `kTVMFFIDLTensorPtr=7` and `Optional<T>` container.
- `0008-type-traits`: `storage_enabled=false` for `TensorView` — the test verifies pass-through via AnyView conversion.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
