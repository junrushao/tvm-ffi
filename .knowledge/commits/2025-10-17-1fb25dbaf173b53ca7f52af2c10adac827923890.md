---
author: "junru"
subject: "Polish quickstart docs: rename example files/symbols, fix CMake config, add JAX example"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/get-started"
  - "examples/quick-start"
---
# Polish quickstart docs: rename example files/symbols, fix CMake config, add JAX example

## TL;DR
- Renamed example files (`main.cc` -> `add_one_cpu.cc`, `main.cu` -> `add_one_cuda.cu`) and libraries (`libmain.so` -> `add_one_cpu.so`/`add_one_cuda.so`) for clarity; updated all references in the quickstart guide.
- Fixed CMake config invocation from `tvm-ffi-config --cmakedir` to `tvm_ffi.config --cmakedir` (Python module form) and added `set_target_properties` for clean output names.
- Added JAX framework usage example, expanded C++ bundle example with `Function::FromExternC`/`Function::InvokeExternC` hint, and improved prose throughout.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none -- the CMakeLists.txt fix in `examples/quick_start/` adds the `execute_process` block to resolve `tvm_ffi_ROOT` via `tvm_ffi.config --cmakedir`, matching the documented pattern.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **Module system** (`0011-ffi-module-system`): `tvm_ffi.load_module`, `Module::LoadFromFile`, Rust `Module::load_from_file`
- **Function system** (`0004-ffi-function-system`): `TVM_FFI_DLL_EXPORT_TYPED_FUNC` (export macro), `Function::FromExternC`, `Function::InvokeExternC` (new in hint block)
- **Containers** (`0006-ffi-containers`): `TensorView` (kernel parameter type), `Tensor` (C++ host-side allocation)
- **Packaging** (`0013-ffi-packaging-and-distribution`): `tvm-ffi-config` CLI, `tvm_ffi.config --cmakedir`, CMake `find_package(tvm_ffi)`
- **load_inline** (`0014-ffi-load-inline`): mentioned in hint as single-file convenience

Design elements this commit produces: none

## Usage Examples
None -- this commit is itself a documentation improvement to usage examples.

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
