---
author: "Tianqi Chen"
subject: "Polish quickstart docs: rename examples for clarity, add JAX/C++ bundle/Rust hints, fix CMake invocation"
commit_shape: "docs-only"
commit_type: "DOCS"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/get_started"
  - "examples/quick_start"
---
# Polish quickstart docs: rename examples for clarity, add JAX/C++ bundle/Rust hints, fix CMake invocation
## TL;DR
- Renamed example artifacts throughout quickstart: `main.cc` -> `add_one_cpu.cc`, `main.cu` -> `add_one_cuda.cu`, `libmain.so` -> `add_one_cpu.so`/`add_one_cuda.so`, exported function `add_one` -> `add_one_cpu`/`add_one_cuda`.
- Added new framework/language examples: JAX integration via `jax-tvm-ffi`, C++ bundling with `Function::InvokeExternC`, expanded Rust snippet with `into_typed_fn!`.
- Fixed CMake examples to use `tvm_ffi.config` module path (was `tvm-ffi-config`) and added `set_target_properties` for proper output naming; fixed the actual `examples/quick_start/CMakeLists.txt` to match.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Module system (0011): `load_module`, `Module::LoadFromFile`, `GetFunction` -- used in quickstart examples
- Function system (0003): `TVM_FFI_DLL_EXPORT_TYPED_FUNC`, `Function::FromExternC`, `Function::InvokeExternC` -- documented in new C++ bundle hint
- C ABI (0007): `__tvm_ffi_` symbol prefix convention -- shown in new `extern "C"` bundle example
- Containers (0006): `TensorView`, `Tensor` -- used in quickstart C++ code snippets
- Python bindings (0012): `tvm_ffi.load_module`, `tvm_ffi.Module` -- used in Python framework examples
- Rust bindings (0014): `tvm_ffi::Module::load_from_file`, `into_typed_fn!` -- expanded in Rust snippet

Design elements this commit produces: None

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
