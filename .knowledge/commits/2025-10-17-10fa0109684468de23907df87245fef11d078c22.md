---
author: "TVM FFI team"
subject: "Re-enable Python 3.8 wheel builds on macOS arm64 after reverting the skip rule"
commit_shape: "ci-infra"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - ".github/workflows"
  - "pyproject.toml"
---
# Re-enable Python 3.8 wheel builds on macOS arm64 after reverting the skip rule

## TL;DR
- Removes `"cp38-macosx_*"` from the `skip` list in `pyproject.toml` `[tool.cibuildwheel]` (previously added in 8831e88).
- Updates `publish_wheel.yml` to handle Python 3.8 on macOS properly with conditional logic rather than a blanket skip.

## Key Exports
N/A — infrastructure

## Impact
- API: none
- Behavioral: Python 3.8 wheels are again built for macOS arm64, with the CI workflow handling compatibility constraints at the job level rather than a blanket skip.
- Flags/config: `skip` in `[tool.cibuildwheel]` no longer contains `"cp38-macosx_*"`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- CI/CD wheel-publishing pipeline — infrastructure only.

Design elements this commit produces:
- None

## Usage Examples
N/A

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
