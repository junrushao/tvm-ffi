---
author: "Junru Shao"
subject: "Convert ABI overview doc from Markdown to RST; add region markers in c_api.h; refactor concept docs"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/concepts"
  - "examples/abi_overview"
  - "include/tvm/ffi"
---
# Convert ABI overview doc from Markdown to RST; add region markers in c_api.h; refactor concept docs

## TL;DR
- `docs/concepts/abi_overview.md` removed; replaced by `docs/concepts/abi_overview.rst` (RST format, 551 lines).
- `examples/abi_overview/example_code.c` added (292 lines) — a standalone C example illustrating the stable C ABI.
- Region markers (`// [TVMFFITypeIndex.begin]`, `// [TVMFFIObject.begin]`, `// [TVMFFIAny.begin]`, etc.) added to `include/tvm/ffi/c_api.h` to enable doc cross-referencing.
- Existing concept RST docs (`any.rst`, `func_module.rst`, `object_and_class.rst`, `tensor.rst`) significantly trimmed (combined -411 lines).

## Key Exports

N/A — docs-only.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: `c_api.h` gains region markers (`// [TVMFFITypeIndex.begin/end]`, `// [TVMFFIObject.begin/end]`, `// [TVMFFIAny.begin/end]`) used for doc cross-referencing.

## Design Elements
Design elements this commit consumes:
- `0001-c-abi`: The stable C ABI types (`TVMFFITypeIndex`, `TVMFFIObject`, `TVMFFIAny`) are documented in the new RST file.

Design elements this commit produces: None — documentation only.

## Usage Examples

N/A — docs-only commit.

## Reflection

### Design docs / ADRs to write or update
- The new `docs/concepts/abi_overview.rst` is itself the key documentation artifact. The `.knowledge/design-records/0001-c-abi.md` may want to reference it as the canonical ABI overview.

### Stale knowledge references
None.
