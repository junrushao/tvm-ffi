---
author: "Junru Shao"
subject: "Add example coverage and polish docstrings across Python public API"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/ffi-bindings"
  - "python/documentation"
---
# Add example coverage and polish docstrings across Python public API
## TL;DR
- Added copy-pastable `Examples` sections to nearly every public Python API docstring: `convert`, `dtype`, `Module`, `system_lib`, `load_module`, `register_global_func`, `get_global_func`, `remove_global_func`, `get_global_func_metadata`, `AccessPath`, `serialization`, `stream`, `error.register_error`, `Array`, `Map`, `Shape`, `FileLock`, `Device`, `DLDeviceType`, `Object`, `Tensor`, `from_dlpack`, `Function`.
- Polished existing prose: fixed grammar, standardized `Returns` blocks (removed redundant parameter names), added `See Also` cross-references, and fixed a typo (`"stram"` -> `"stream"` in `use_raw_stream` error message).
- Expanded Sphinx autodoc index (`docs/reference/python/index.rst`) with `get_raw_stream` and explicit entries for `serialization`/`access_path` sub-items.

## Key Exports
None

## Impact
- API: none
- Behavioral: Trivial bugfix -- error message in `use_raw_stream` now says `"stream"` instead of `"stram"`.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `convert()` function (0012-ffi-python-package) -- documented conversion rules for list, dict, str, bytes, callable, numpy array
- `dtype` class (0006-ffi-containers) -- documented constructor, `from_dlpack_data_type`, `with_lanes`, property accessors
- `Module`/`system_lib`/`load_module` (0011-ffi-module-system) -- clarified `system_lib` symbol prefix convention with cross-layer C++/Python example; registered `__tvm_ffi_testing.add_one` in `testing.cc` to support the example
- `register_global_func`/`get_global_func`/`remove_global_func`/`get_global_func_metadata` (0004-ffi-function-system, 0015-ffi-python-type-system) -- added usage examples
- `AccessPath` (0009-ffi-structural-eq-hash) -- added class-level docstring and usage example
- `serialization` module (0010-ffi-json-serialization) -- added round-trip example
- `use_raw_stream`/`get_raw_stream` (0011-ffi-module-system) -- added usage examples
- `error.register_error` (0005-ffi-error-protocol) -- updated example to current API
- `Array`/`Map` (0006-ffi-containers) -- updated examples to use direct constructors
- `Function` class (0004-ffi-function-system) -- updated `See Also` to use Sphinx cross-references

Design elements this commit produces: none

## Usage Examples
None -- the commit itself is a collection of usage examples embedded in docstrings.

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
