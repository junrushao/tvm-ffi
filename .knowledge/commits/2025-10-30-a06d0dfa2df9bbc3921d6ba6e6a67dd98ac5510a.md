---
author: "dingyaoyao.cs@gmail.com"
subject: "Remove python lib link flags on Linux when building torch C DLPack ext to fix CI failure"
commit_shape: "trivial"
commit_type: "bugfix"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
---
# Remove python lib link flags on Linux when building torch C DLPack ext to fix CI failure

## TL;DR
- Removes `-L <python-lib-dir> -lpython3` from Linux linker flags in `_build_optional_torch_c_dlpack.py`; keeps them for macOS and Windows where `libtorch_python` requires Python's shared library.

## Key Exports
None

## Impact
- API: none
- Behavioral: Fixes Linux CI build failure where linking python shared lib was unnecessary and caused errors.
- Flags/config: Platform-conditional ldflags: Linux omits python lib link; macOS/Windows retain it.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: `_build_optional_torch_c_dlpack` build utility (0016-py-ffi-call-dispatch)
Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
- `.knowledge/design-records/0016-py-ffi-call-dispatch.md` — document platform-conditional linker flag logic: Linux: no `-lpython3`; macOS/Windows: include because `libtorch_python` requires it.

### Stale knowledge references
None
