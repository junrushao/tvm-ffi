---
author: "Junru Shao"
subject: "Remove redundant forward declaration of Function in function_details.h"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "function-system"
---
# Remove redundant forward declaration of Function in function_details.h
## TL;DR
- Removes a two-line forward declaration `class Function;` from `include/tvm/ffi/function_details.h` that was unused within the file.
- Follow-up cleanup from PR #36 code review comment.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **Function class** ([0004-function-system.md](../designs/0004-function-system.md)): The removed forward declaration referenced `Function`, but `function_details.h` never uses `Function` directly -- it only defines signature reflection helpers (`FuncFunctorImpl`, `FunctionInfo`, `ArgValueWithContext`, `unpack_call`) that operate on template parameters and `AnyView`/`Any`.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
