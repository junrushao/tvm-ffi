---
author: "Junru Shao"
subject: "Reimplement c_class as register_object plus structural dunders"
commit_shape: "standard"
commit_type: "refactor"
potential_duplicate: ""
scope:
  - "python-dataclasses"
---
# Reimplement c_class as register_object plus structural dunders
## TL;DR
- Reimplements `c_class` decorator as a thin layer over `register_object` that additionally installs structural `__eq__`/`__hash__`/`__repr__`/`__copy__`/`__deepcopy__` dunders.
- Consolidates `_install_structural_dunders` in `registry.py`.
- Renames test files for clarity (`test_copy.py` -> `test_dataclass_copy.py`).

## Key Exports

```python
def c_class(type_key: str) -> Callable[[type], type]: ...
    # Now equivalent to: register_object(type_key) + _install_structural_dunders
    # Interacts with: register_object, _install_structural_dunders
    # Extension: additional dunders can be added to _install_structural_dunders

def _install_structural_dunders(cls: type, type_key: str) -> None: ...
    # Installs __eq__, __hash__, __repr__, __copy__, __deepcopy__
    # Interacts with: ffi.RecursiveCompare, ffi.RecursiveHash, ffi.ReprPrint, ffi.DeepCopy
```

## Impact
- API: `c_class` behavior unchanged externally; internal implementation simplified.
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- `register_object` with auto-init -- from b1abaee
- Recursive compare/hash/repr/copy from dataclass.cc

Design elements this commit produces:
- `_install_structural_dunders` pattern

## Usage Examples
None -- same external API as before.

## Reflection

### Design docs to write or update
- `.knowledge/designs/0015-python-dataclasses.md` -- document c_class reimplementation as register_object + structural dunders.

### ADRs to write or update
None

### Stale knowledge references
None
