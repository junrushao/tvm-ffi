---
author: "Tianqi Chen"
subject: "Bump TVM_FFI_VERSION_PATCH from 1 to 2 after release"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/c-api"
---
# Bump TVM_FFI_VERSION_PATCH from 1 to 2 after release
## TL;DR
- Bumps `TVM_FFI_VERSION_PATCH` macro from `1` to `2` in `include/tvm/ffi/c_api.h`, moving the compile-time version to 0.1.2 for the next development cycle.

## Key Exports
None

## Impact
- API: none
- Behavioral: none — the only observable change is `TVMFFIGetVersion()` now returns patch=2 instead of patch=1.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `TVM_FFI_VERSION_PATCH` / `TVMFFIGetVersion` version query protocol (see `0001-ffi-c-abi` in `.knowledge/design-records/0001-ffi-c-abi.md`)

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
- `.knowledge/design-records/0001-ffi-c-abi.md`: update the pseudocode showing `TVM_FFI_VERSION_PATCH: int = 0` to reflect current value `2` (low severity — cosmetic only, does not affect contract understanding).

### Stale knowledge references
- `.knowledge/design-records/0001-ffi-c-abi.md` line ~290: shows `TVM_FFI_VERSION_PATCH: int = 0`, should be `2`. Severity: `low` (simple constant update).
