---
author: "Kathryn (Jinqi) Chen"
subject: "Fix clang-tidy warnings in metadata testing code"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "ci-and-lint-pipeline"
---
# Fix clang-tidy warnings in metadata testing code
## TL;DR
- Fixes two categories of clang-tidy warnings introduced by `ac7bf68` (metadata export feature): pass-by-value of container types changed to const-ref in `schema_arr_map_opt`, and `NOLINTNEXTLINE(bugprone-reserved-identifier)` annotations added to `extern "C"` declarations with double-underscore `__tvm_ffi__` prefixes.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **CI and lint pipeline** (`0016-ci-and-lint-pipeline.md`): clang-tidy `performance-unnecessary-value-param` and `bugprone-reserved-identifier` rules.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
