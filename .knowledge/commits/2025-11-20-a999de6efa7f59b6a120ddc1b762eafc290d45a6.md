---
author: "Kathryn (Jinqi) Chen"
subject: "Fix clang-tidy warnings in testing.cc and test_metadata.cc for reserved identifier usage"
commit_shape: "trivial"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "src/ffi/testing"
  - "tests/cpp"
---
# Fix clang-tidy warnings for reserved identifiers and pass-by-value

## TL;DR
- Adds `// NOLINTNEXTLINE(bugprone-reserved-identifier)` before forward declarations of `__tvm_ffi__metadata_*` and `__tvm_ffi__doc_*` symbols in `test_metadata.cc` (identifiers starting with `__` in global scope trigger this clang-tidy check).
- Changes `Array<Optional<int64_t>> arr` and `Optional<String> os` parameters to const-ref in `schema_arr_map_opt` in `testing.cc` to silence pass-by-value warnings.

## Key Exports
None

## Impact
- API: none
- Behavioral: none (lint annotation and const-ref are ABI-transparent)
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
