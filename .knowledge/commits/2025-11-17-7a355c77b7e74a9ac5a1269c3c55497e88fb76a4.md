---
author: "Tianqi Chen"
subject: "Remove ninja dependency on non-Windows when JIT-building optional torch-c-dlpack"
commit_shape: "standard"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi/torch-integration"
  - "build/packaging"
---
# Remove ninja dependency on non-Windows when JIT-building optional torch-c-dlpack
## TL;DR
- Removes the hard `ninja` dependency for the optional torch-c-dlpack JIT build on non-Windows platforms, falling back to `make` when `ninja` is unavailable.
- Refactors `_build_optional_torch_c_dlpack.py` to conditionally use ninja vs make as the CMake generator.

## Key Exports

```python
# No new public API. Internal build logic change:
# _build_optional_torch_c_dlpack.py now checks for ninja availability
# and falls back to Unix Makefiles on non-Windows.
# Interacts with: CMake build system, torch JIT compilation path
```

## Impact
- API: none
- Behavioral: torch-c-dlpack extension can now build without ninja on Linux/macOS
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Optional torch C DLPack extension build pipeline (0013-load-inline): modifies JIT build generator selection.

Design elements this commit produces: none (internal build improvement)

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
