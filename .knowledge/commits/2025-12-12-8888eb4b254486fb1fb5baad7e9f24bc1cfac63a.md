---
author: "Tianqi Chen"
subject: "Add API to create strided tensor views"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/containers"
  - "ffi/c-api"
---
# Add API to create strided tensor views

## TL;DR
- Adds `Tensor::CreateView` for creating strided tensor views with different shapes/strides/offsets
- Adds `TVMFFITensorCreateView` C API function
- Adds comprehensive C++ test coverage (169 LOC)

## Key Exports
```python
class Tensor:
    def CreateView(
        self,
        shape: Sequence[int],
        strides: Sequence[int] | None = None,
        byte_offset: int = 0,
    ) -> Tensor:
        """Create a strided view of the tensor.
        # Interacts with: DLTensor layout, TVMFFITensorCreateView C API
        # Invariant: view shares data pointer with original
        """
        ...
```

## Impact
- API: New `Tensor::CreateView` and `TVMFFITensorCreateView` APIs
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Containers (`0006-containers`): Tensor
- C ABI (`0007-c-abi`)

Design elements this commit produces:
- Strided tensor view creation API

## Usage Examples
```cpp
Tensor t = Tensor::Empty({6}, DLDataType{kDLFloat, 32, 1}, DLDevice{kDLCPU, 0});
Tensor view = t.CreateView({2, 3}, {3, 1}, 0);
```

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
