---
author: "junru"
subject: "Move ty and test dependencies to dependency-groups; pin ty version"
commit_shape: "ci-infra"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "tooling"
  - "packaging"
---
# Move ty and test dependencies to dependency-groups; pin ty version
## TL;DR
- Restructures `pyproject.toml`: moves `test` from `[project.optional-dependencies]` to `[dependency-groups]` using `include-group` for composition
- Pins `ty==0.0.15` and adds `--error-on-warning` flag in pre-commit hook
- Reorders pre-commit hooks for cleaner grouping; removes `python = ".venv"` from `[tool.ty.environment]`

## Key Exports
N/A -- infrastructure

## Impact
- API: none
- Behavioral: CI test installs now use `--group test` instead of `--extras test`
- Flags/config: `[tool.ty.rules] unused-ignore-comment = "ignore"` added
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Python packaging configuration (`.knowledge/designs/0013-packaging.md`)

Design elements this commit produces: none

## Usage Examples
N/A

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
