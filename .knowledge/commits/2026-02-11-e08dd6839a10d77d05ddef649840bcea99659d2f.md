---
author: ""
subject: "Run ty type checker in dev environment instead of system-wide"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "ci-and-lint"
---
# Run ty type checker in dev environment instead of system-wide
## TL;DR
- Changes `ty` type checker invocation from `uvx ty check` to running within the dev environment across pre-commit hooks, CI workflows, and `pyproject.toml` configuration.
- Minor type annotation fixes across ~12 test/example files to satisfy stricter checking.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: Pre-commit hooks and CI now run `ty` within the project's virtualenv rather than via `uvx`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **CI and lint pipeline** (`.knowledge/designs/0016-ci-and-lint-pipeline.md`): Adjusts type checker execution environment.

Design elements this commit produces:
- None.

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
