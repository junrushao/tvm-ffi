---
author: "tianqi.tchen@gmail.com"
subject: "Setup initial GitHub Actions CI with lint, test, and wheel publish workflows"
commit_shape: "ci-infra"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - ".github"
  - "tests/lint"
---
# Setup initial GitHub Actions CI with lint, test, and wheel publish workflows

## TL;DR
- Adds `.github/workflows/ci_test.yml` (CI test pipeline) and `.github/workflows/publish_wheel.yml` (wheel publish pipeline).
- Adds `.pre-commit-config.yaml` with hooks for ruff, clang-format, cython-lint, cmake-format, etc.
- Adds `tests/lint/` scripts: `check_asf_header.py`, `check_file_type.py`, `git-clang-format.sh`, `task_lint.sh`.

## Key Exports
N/A — infrastructure

## Impact
- API: None.
- Behavioral: CI now runs on PRs and pushes to `main`/`dev`. Lint enforced via pre-commit.
- Flags/config: Workflow uses `detect-skip-ci` and `detect-env-vars` composite actions.
- Data formats/schemas: None.

## Design Elements
Design elements this commit consumes: None.
Design elements this commit produces: None.

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
