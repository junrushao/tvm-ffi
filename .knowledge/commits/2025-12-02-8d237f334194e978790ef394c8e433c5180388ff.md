---
author: "junrushao"
subject: "Change JIT-compile log message from info to debug level to reduce server startup noise"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/ffi-bindings"
---
# Change JIT-compile log message from info to debug level to reduce server startup noise
## TL;DR
- Downgraded the JIT-compilation log message in `_optional_torch_c_dlpack.py` from `logger.info` to `logger.debug` to suppress noisy output during server startup.

## Key Exports
None

## Impact
- API: none
- Behavioral: JIT-compilation log message for torch-c-dlpack-ext no longer appears at default (INFO) log level; only visible at DEBUG level.
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Torch DLPack addon JIT build flow (`load_torch_c_dlpack_extension()` in `python/tvm_ffi/_optional_torch_c_dlpack.py`) — see [0012-ffi-python-package](../design-records/0012-ffi-python-package.md)

Design elements this commit produces: None

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
