---
author: "Tianqi Chen"
subject: "Fix error propagation for tensor arguments in safe_call"
commit_shape: standard
commit_type: bugfix
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/c-api"
  - "python/tvm_ffi"
  - "ffi/extra"
---
# Fix error propagation for tensor arguments in safe_call
## TL;DR
- Fixes error propagation when tensor arguments fail conversion in the safe_call path
- Updates `c_api.h` to clarify tensor argument handling semantics
- Fixes Python helper `tvm_ffi_python_helpers.h` to properly handle tensor argument errors
- Updates `env_context.cc` for correct error code propagation

## Key Exports
```python
# Fix: TVMFFIEnvTensorAlloc now properly propagates errors through safe_call convention
# Invariant: tensor argument conversion errors must be caught and returned as error code -1
```

## Impact
- API: minor C API clarification
- Behavioral: fixes silent error swallowing when tensor arguments fail to convert
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Error propagation (0004-error-propagation)
- Function safe_call convention (0003-function-system)
- C ABI (0007-c-abi)

Design elements this commit produces: none

## Usage Examples
Before: tensor argument conversion failures could be silently swallowed.
After: errors are properly propagated through the safe_call return code.

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
