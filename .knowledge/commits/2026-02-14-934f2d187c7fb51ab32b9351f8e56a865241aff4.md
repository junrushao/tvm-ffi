---
author: "feat: Claude Skill for TVM-FFI Development (#445)"
subject: "Add a condensed developer-reference Claude skill for TVM-FFI build, lint, test, and docs workflows."
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - ".claude"
---
# Add a condensed developer-reference Claude skill for TVM-FFI build, lint, test, and docs workflows.
## TL;DR
- Adds `.claude/skills/devtools/SKILL.md`: a 257-line condensed reference for contributors covering linting (pre-commit + clang-tidy), C++ build/test (CMake options), Python build/test (`uv` workflows), and docs build (Sphinx, Doxygen, Rust).
- Adds `ignores: [".claude/**"]` to `.markdownlint-cli2.yaml` so markdownlint skips `.claude/` skill files (which use non-standard YAML frontmatter that triggers lint failures).

## Key Exports
N/A — developer tooling only; no public API surface introduced.

## Impact
- API: none
- Behavioral: `.claude/**` files are now ignored by markdownlint-cli2 (prevents false-positive lint failures on skill frontmatter).
- Flags/config: `.markdownlint-cli2.yaml` gains an `ignores` block for `.claude/**`.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `0021-build-packaging` — skill documents the CMake options table (`TVM_FFI_BUILD_TESTS`, `TVM_FFI_ATTACH_DEBUG_SYMBOLS`, etc.) and the `scikit-build-core`-driven `uv pip install -e .` workflow.
- `0013-python-package` — references the four-step import protocol and editable-install rebuild requirement.
- `0020-stub-gen` — not referenced directly, but stub-gen is part of the dev workflow documented in CLAUDE.md from which this skill is derived.

Design elements this commit produces:
- None — no new C++ types, Python APIs, or system protocols are introduced.

## Usage Examples
None — docs-only commit; no new callable API.

## Reflection

### Design docs / ADRs to write or update
None — the new skill file is a user-facing developer tool, not a design record. All referenced design elements (build system, Python package) are already documented in `.knowledge/design-records/`.

### Stale knowledge references
None — no symbols were renamed or removed.
