---
author: "Junru Shao"
subject: "Update pre-commit hook versions and enable ruff docstring-code-format"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "lint"
  - "tooling"
---
# Update pre-commit hook versions and enable ruff docstring-code-format
## TL;DR
- Bumped 9 pre-commit hook versions: ruff 0.12.3->0.14.9, clang-format v20->v21, mypy 1.18.2->1.19.0, pre-commit-hooks v5->v6, yamllint, cython-lint, shellcheck, markdownlint-cli2, rstcheck.
- Enabled `docstring-code-format = true` and set `docstring-code-line-length = 80` in `pyproject.toml` ruff config.
- All Python file changes are mechanical reformatting of docstring code examples (blank line insertion, quote normalization, line wrapping) applied by the newly enabled formatter.

## Key Exports
None

## Impact
- API: none
- Behavioral: none -- all changes are formatting/tooling; no runtime behavior changes.
- Flags/config: `docstring-code-format` changed from `false` to `true` in `pyproject.toml`; `docstring-code-line-length` changed from `"dynamic"` to `80`. Future ruff runs will auto-format Python code blocks inside docstrings to 80-col width.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
