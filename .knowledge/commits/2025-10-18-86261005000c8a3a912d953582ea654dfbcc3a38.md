---
author: "Junru Shao"
subject: "Polish quickstart doc and reorganize examples into examples/quickstart/"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
  - "examples"
---
# Polish quickstart doc and reorganize examples into examples/quickstart/

## TL;DR
- Reorganized example files from `examples/quick_start/` into `examples/quickstart/` with a clearer directory split (`compile/`, `load/`).
- Quickstart RST switched from inline code blocks to `literalinclude` directives referencing the new example files.
- Added CPU and CUDA load examples in Python (`load_numpy.py`, `load_pytorch.py`, `load_cupy.py`) and C++ (`load_cpp.cc`).

## Key Exports
None — documentation and example reorganization only.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `tvm_ffi.load_module` (see `.knowledge/design-records/0011-module-system.md`)
- `TVM_FFI_REGISTER_GLOBAL` / `TVM_FFI_STATIC_INIT_BLOCK` registration pattern (see `.knowledge/design-records/0004-function-system.md`)
- Stable C ABI calling convention (see `.knowledge/design-records/0001-c-abi.md`)

Design elements this commit produces: none

## Usage Examples
None — all content is documentation/example files.

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
