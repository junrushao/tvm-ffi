---
author: "Junru Shao"
subject: "Remove Python-side field descriptor infrastructure from dataclasses"
commit_shape: standard
commit_type: refactor
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
---
# Remove Python-side field descriptor infrastructure from dataclasses
## TL;DR
- Removes 727 lines of Python-side field descriptor infrastructure from `tvm_ffi.dataclasses`
- Simplifies `@c_class` by relying entirely on C++ reflection for field information
- Breaking change: removes Python `field()` descriptor that duplicated C++ reflection metadata

## Key Exports
```python
# Removed: Python-side Field class and fill_dataclass_field infrastructure
# Now: @c_class relies solely on C++ reflection (ObjectDef<T>.def_field) for field metadata
# This is a breaking refactor that simplifies the Python dataclass layer
```

## Impact
- API: removes Python `field()` descriptor infrastructure (breaking)
- Behavioral: `@c_class` now uses C++ reflection exclusively for field info
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: Reflection (0008), Python dataclasses (0012)
Design elements this commit produces: none (simplification)
## Usage Examples
None
## Reflection
### Design docs to write or update
- `.knowledge/design-records/0012-python-bindings.md` — update @c_class field infrastructure
### Stale knowledge references
None
