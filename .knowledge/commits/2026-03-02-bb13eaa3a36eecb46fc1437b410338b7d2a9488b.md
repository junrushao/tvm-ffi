---
author: "doc: add dataclass reflection guide and update related docs (#494)"
subject: "Add a 467-line comprehensive guide for the dataclass-style reflection system and fix cross-reference issues in existing docs"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
---
# Add dataclass reflection guide and update related docs

## TL;DR
- New `docs/guides/dataclass_reflection.rst` (467 lines) covering quick start, auto-init, field traits (`refl::default_`, `kw_only`, `init`, `repr`, `hash`, `compare`, `Metadata`), deep copy/hash/eq/repr operations, custom hooks via `TypeAttrDef`, `@c_class` / `@register_object`, and inheritance semantics.
- Sphinx `conf.py` updated: exclude `reflection::default_`/`default_factory` from Doxygen, fix `.CObject` → `.Object` in `_link_inherited_members`, add `_move`/`__move_handle_from__`/`__init_handle_by_constructor__` to `_autodoc_always_show`.
- Cross-reference fixes: `refl::DefaultValue(0)` → `refl::default_(0)` in `object_and_class.rst`; broken Sphinx link fixed in `export_func_cls.rst`.

## Key Exports
N/A — documentation-only change.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `0006-reflection`: ObjectDef, field traits, TypeAttrDef — all documented in the new guide
- `0017-c-class-dataclasses`: `@c_class` decorator documented
- `0009-structural-eq-hash`: `RecursiveEq`, `RecursiveHash`, `RecursiveLt/Le/Gt/Ge` documented

Design elements this commit produces:
- None (docs only)

## Usage Examples
N/A

## Reflection

### Design docs / ADRs to write or update
- The new `docs/guides/dataclass_reflection.rst` is itself the documentation artifact; it should be cross-referenced from `0006-reflection` and `0017-c-class-dataclasses` design records.

### Stale knowledge references
- `0006-reflection` (design-index.md): mentions `refl::DefaultValue` — should be `refl::default_`. Staleness: `low`.
