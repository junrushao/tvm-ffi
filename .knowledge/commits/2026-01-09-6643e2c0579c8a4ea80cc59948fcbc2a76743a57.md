---
author: "Guan-Ming (Wesley) Chiu"
subject: "Add docstring notes pointing to structural_equal/structural_hash for Array and Map"
commit_shape: "docs-only"
commit_type: "docs"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/containers"
  - "ffi/extra"
---
# Add docstring notes pointing to structural_equal/structural_hash for Array and Map
## TL;DR
- Adds `Notes` sections to `Array` and `Map` class docstrings in `python/tvm_ffi/container.py` directing users to `structural_equal` and `structural_hash` APIs for container comparison/hashing.

## Key Exports
None -- documentation-only change, no new or modified API surface.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `Array` and `Map` container classes (design record `0006-ffi-containers`) -- docstrings augmented with cross-references.
- `structural_equal` and `structural_hash` APIs (design record `0009-ffi-structural-eq-hash`) -- referenced as the recommended approach for container equality/hashing.

Design elements this commit produces:
- None -- purely additive documentation.

## Usage Examples
None -- docs-only change. The added notes direct users to:
```python
from tvm_ffi import structural_equal, structural_hash

a1 = tvm_ffi.Array([1, 2, 3])
a2 = tvm_ffi.Array([1, 2, 3])
assert structural_equal(a1, a2)
assert structural_hash(a1) == structural_hash(a2)
```

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
