---
author: "Junru Shao"
subject: "Remove obsolete `?sanitize=true` query parameter from SVG image URLs in docs"
commit_shape: "chore"
commit_type: "fix"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
---
# Remove obsolete `?sanitize=true` query parameter from SVG image URLs in docs
## TL;DR
- Removed `?sanitize=true` from two raw GitHub SVG URLs in `docs/get_started/stable_c_abi.rst`.
- Aligns with upstream change in `apache/tvm-site` (commit `81a8c2e`), where the sanitize parameter is no longer needed.

## Key Exports
None

## Impact
- API: none
- Behavioral: none — SVG images render identically (GitHub raw URLs no longer require `?sanitize=true`)
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- [0001-any-anyview-value-system](.knowledge/design-records/0001-any-anyview-value-system.md) — the `stable-c-abi-layout-any.svg` figure illustrates the TVMFFIAny layout
- [0007-c-abi](.knowledge/design-records/0007-c-abi.md) — the `stable-c-abi-layout-func.svg` figure illustrates the function calling convention

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
