---
author: "Junru Shao"
subject: "Remove `?sanitize=true` query parameter from SVG URLs in stable C ABI docs"
commit_shape: "docs-only"
scope:
  - "docs"
impact: "trivial"
abi_breaking: false
related_commits: []
---
# Remove `?sanitize=true` query parameter from SVG URLs in stable C ABI docs
## TL;DR
- Removed the obsolete `?sanitize=true` query parameter from two raw GitHub SVG image URLs in `docs/get_started/stable_c_abi.rst`.
- Aligns with a corresponding change on the tvm-site repository (commit `81a8c2e94ea1e32d5bfb5c1335cd5a8565e66f46`).

## Key Exports
None

## Impact
- API: none
- ABI: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- C ABI layout documentation (`docs/get_started/stable_c_abi.rst`), specifically the SVG figures for the `TVMFFIAny` layout and the packed function calling convention layout.

Design elements this commit produces:
- None. This is a cosmetic URL cleanup with no new design content.

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
