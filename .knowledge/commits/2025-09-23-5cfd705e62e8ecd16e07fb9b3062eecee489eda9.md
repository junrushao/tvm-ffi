---
author: "TVM FFI team"
subject: "Revamp mypy config and publish PEP 561 typing metadata"
scope:
  - "0016-packaging-and-build"
  - "0014-python-bindings"
impact: "low"
---
# Revamp mypy config and publish PEP 561 typing metadata
## TL;DR
- Consolidates mypy settings into `pyproject.toml` with project-wide targets, namespace support, and per-directory overrides for tests/examples/optional deps.
- Replaces multiple per-path mypy pre-commit hooks with a single project-wide hook pinned to mypy v1.18.2.
- Adds `py.typed` marker file and bundles it in wheels/sdists for PEP 561 compliance, enabling downstream consumers to use the package's type stubs.

## Impact
- API: none
- Flags/config: Consolidated `[tool.mypy]` in `pyproject.toml`; single mypy pre-commit hook
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **Packaging and Build** (`.knowledge/designs/0016-packaging-and-build.md`): `pyproject.toml` configuration, pre-commit hooks.

Design elements this commit produces:
- **PEP 561 typing marker**: `py.typed` file enables downstream type checking for `tvm_ffi` imports.

## Reflection

### Design docs to write
- None.

### ADRs to write
- None.

### Design diagrams to draw
- None.

### Skill evolution (`/commit-ledger`)
- None.

## Self-Evolution of Skill /commit-ledger
No issues encountered.
