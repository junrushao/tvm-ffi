---
author: "GitHub Actions / PR #468"
subject: "Add CLAUDE.md project guide and .claude/launch.json Sphinx dev server config"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
  - "claude-config"
---
# Add CLAUDE.md project guide and .claude/launch.json Sphinx dev server config

## TL;DR
- Adds `CLAUDE.md` at repo root: a Claude Code project guide covering repo layout, build/test/lint workflows, C++ and Python code conventions, and key architecture concepts (`Any/AnyView`, `Object`, `Function`, global registry, containers, reflection, module system).
- Adds `.claude/launch.json` with a named `"docs"` configuration that launches `sphinx-autobuild` via `uv run --group docs`, serving on port 8000 with `BUILD_CPP_DOCS=1` and `BUILD_RUST_DOCS=1` env vars.
- Both files are purely additive (194 LOC total, 0 deletions).

## Key Exports
N/A — docs-only commit; no code symbols introduced.

## Impact
- API: none
- Behavioral: none
- Flags/config: `.claude/launch.json` defines `BUILD_CPP_DOCS=1` and `BUILD_RUST_DOCS=1` env flags used by the Sphinx autobuild dev server.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `Any`/`AnyView` (0003-any-anyview) — mentioned in architecture overview
- Object system `FooObj`/`ObjectRef` / `make_object<T>` (0002-object-system) — mentioned in architecture overview and C++ conventions
- `Function` packed calling convention (0004-function-system) — mentioned in architecture overview
- Global function registry `register_global_func`/`get_global_func` (0004-function-system) — mentioned in architecture overview
- Containers: `Array<T>`, `List<T>`, `Map<K,V>`, `Dict`, `String`, `Tensor`, `Shape`, `Tuple`, `Variant<T...>` (0007-containers) — mentioned in architecture overview
- `ObjectDef<T>` reflection with `def_field`/`def_method` (0006-reflection) — mentioned as "Reflection" concept
- `@c_class` Python decorator (0017-c-class-dataclasses) — mentioned as Python exposure of reflection
- Module system `load_module` (0011-module-system) — mentioned in architecture overview
- TVM FFI macro pair `TVM_FFI_DECLARE_OBJECT_INFO` / `TVM_FFI_DEFINE_OBJECT_REF_METHODS` (0002-object-system) — documented in C++ code conventions
- `TVM_FFI_THROW` error macro (0005-error-system) — documented in C++ code conventions
- `@register_object("type.Key")` / `register_global_func` Python conventions (0013-python-package, 0004-function-system) — documented in Python code conventions

Design elements this commit produces: none

## Usage Examples
N/A — docs-only commit.

## Reflection

### Design docs / ADRs to write or update
None — `CLAUDE.md` is itself a developer-facing orientation doc. It accurately reflects the existing design records. No new doc targets created.

### Stale knowledge references
None — all referenced symbols (`Any`, `AnyView`, `Object`, `ObjectRef`, `Function`, `TVM_FFI_DECLARE_OBJECT_INFO`, `TVM_FFI_DEFINE_OBJECT_REF_METHODS`, `TVM_FFI_THROW`, `ObjectDef<T>`, `@c_class`, `load_module`) are current per existing design records 0001–0030.
