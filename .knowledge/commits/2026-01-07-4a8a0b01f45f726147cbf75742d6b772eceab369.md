---
author: "unknown"
subject: "Replace C-style cast with static_cast in String::substr default parameter"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "containers"
---
# Replace C-style cast with static_cast in String::substr default parameter
## TL;DR
- Replace `size_t(-1)` with the existing `npos` constant in `String::substr` default parameter to fix a clang-tidy `google-readability-casting` warning.

## Key Exports
None

## Impact
- API: none (signature is unchanged; `npos` is `static constexpr size_t npos = static_cast<size_t>(-1)`)
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `String::npos` constant (defined at line 615 of `include/tvm/ffi/string.h`; see `.knowledge/designs/0007-containers.md`)

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
