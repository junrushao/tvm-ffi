---
author: "Junru Shao"
subject: "Specify --module-name for Cython build, remove __init__.py module patching"
commit_shape: "standard"
commit_type: "refactor"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - "build/packaging"
---
# Specify --module-name for Cython build, remove __init__.py module patching
## TL;DR
- Removes `__module__` patching logic from `python/tvm_ffi/__init__.py` (introduced in PR #177)
- Replaces with `--module-name tvm_ffi.core` flag at Cython build time in `CMakeLists.txt` and `pyproject.toml`
- Updates Cython class references to use `tvm_ffi.core` module path consistently

## Key Exports
None

## Impact
- API: None
- Behavioral: Cython classes now have correct `__module__` attribute at build time rather than runtime patching
- Flags/config: Build: `--module-name tvm_ffi.core` passed to Cython compiler
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Python bindings Cython build (design-record 0012)

Design elements this commit produces: None

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
