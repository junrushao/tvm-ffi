---
author: "Tianqi Chen <tqchen@users.noreply.github.com>"
subject: "Fix lint errors and improve docs with Sphinx cross-references"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
  - "build/cmake"
  - "ci-config"
---
# Fix lint errors and improve docs with Sphinx cross-references
## TL;DR
- Adds missing Apache 2.0 license header to `docs/reference/python/cpp/index.rst`.
- Replaces inline backtick code references with Sphinx cross-reference roles (`{cpp:func}`, `{cpp:class}`, `{c:macro}`) in `docs/guides/compiler_integration.md`, enabling hyperlinked API docs.
- Adds `TEST_DISCOVERY_TIMEOUT 300` to the GoogleTest CMake helper to prevent test discovery timeouts.
- Updates `.asf.yaml` branch protection comment.

## Key Exports
None

## Impact
- API: none
- Behavioral: none (GoogleTest discovery timeout increased from default to 300s, preventing flaky CI failures)
- Flags/config: `TEST_DISCOVERY_TIMEOUT 300` added in `cmake/Utils/AddGoogleTest.cmake`
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Module system (0011-module-system): docs reference `ModuleObj`, `GetFunction`, `GetPropertyMask`, `SaveToBytes`, `kind` in the compiler integration guide.
- Function system (0003-function-system): docs reference `TVM_FFI_DLL_EXPORT_TYPED_FUNC`, `tvm::ffi::Function`, `TVMFFIEnvGetStream`, `TVMFFIErrorSetRaisedFromCStr`.
- C ABI (0007-c-abi): docs describe `__tvm_ffi_` symbol prefix convention and library binary format.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
