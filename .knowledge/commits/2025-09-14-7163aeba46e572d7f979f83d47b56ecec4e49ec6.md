---
author: "tqchen"
subject: "Fix lint errors and improve docs with Sphinx cross-references"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs"
  - "build/cmake"
  - "ci-infra"
---
# Fix lint errors and improve docs with Sphinx cross-references
## TL;DR
- Added missing Apache 2.0 license header to `docs/reference/python/cpp/index.rst`.
- Replaced inline backtick code references with Sphinx cross-reference roles (`{cpp:func}`, `{cpp:class}`, `{c:macro}`) in `docs/guides/compiler_integration.md`.
- Bumped `TEST_DISCOVERY_TIMEOUT` to 300s in `cmake/Utils/AddGoogleTest.cmake`.
- Updated `.asf.yaml` branch protection comment to clarify committer CI check policy.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: `TEST_DISCOVERY_TIMEOUT 300` added to GoogleTest discovery in CMake (prevents test discovery timeouts on slow CI).
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `ModuleObj` / `Module` interface (`kind`, `GetPropertyMask`, `GetFunction`, `SaveToBytes`) -- referenced in compiler integration guide via new Sphinx roles. See `0011-ffi-module-system`.
- `tvm::ffi::Function` -- referenced in compiler integration guide. See `0004-ffi-function-system`.
- `TVM_FFI_DLL_EXPORT_TYPED_FUNC` macro -- referenced in compiler integration guide. See `0013-ffi-packaging-and-distribution`.
- `TVMFFIEnvGetStream`, `TVMFFIErrorSetRaisedFromCStr` -- C ABI functions referenced in guide. See `0001-ffi-c-abi`.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs / ADRs to write or update
None

### Stale knowledge references
None
