---
author: "Tianqi Chen"
subject: "Fix lint errors and improve docs with Sphinx cross-references and license header"
commit_shape: "trivial"
commit_type: "chore"
potential_duplicate: ""
scope:
  - "docs"
  - "ci-infra"
---
# Fix lint errors and improve docs with Sphinx cross-references and license header
## TL;DR
- Replaced inline code references in `docs/guides/compiler_integration.md` with Sphinx cross-reference roles (`{c:macro}`, `{cpp:func}`, `{cpp:class}`) for proper doc linking.
- Added Apache 2.0 license header to `docs/reference/python/cpp/index.rst` (lint fix).
- Minor infra tweaks: updated `.asf.yaml` PR review comment wording; added `TEST_DISCOVERY_TIMEOUT 300` to `AddGoogleTest.cmake`.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: `TEST_DISCOVERY_TIMEOUT 300` added to `tvm_ffi_add_googletest` macro in `cmake/Utils/AddGoogleTest.cmake`, preventing test discovery timeouts on slow CI runners.
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- Module system docs (`docs/guides/compiler_integration.md`) reference `ModuleObj`, `__tvm_ffi_` symbol prefix, `TVMFFIEnvGetStream`, `TVMFFIErrorSetRaisedFromCStr`, `TVM_FFI_DLL_EXPORT_TYPED_FUNC`, and `tvm::ffi::Function`. No new design elements consumed.
- See [0011-module-system](../../.knowledge/designs/0011-module-system.md), [ADR 0010](../../.knowledge/ADRs/0010-ffi-symbol-prefix-convention.md)

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
