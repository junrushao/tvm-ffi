---
author: "Yaoyao Ding"
subject: "Add runtime and state management section to compiler integration guide"
scope:
  - "documentation-site"
impact: "low"
---
# Add runtime and state management section to compiler integration guide
## TL;DR
- Adds "Runtime and State Management for Compilers" section to `docs/guides/compiler_integration.md`, documenting the recommended pattern for compiler-specific state via global `tvm::ffi::Function` registration using `TVM_FFI_STATIC_INIT_BLOCK()` and singleton state classes.
- Adds `docs/_static/custom.css` to fix sidebar scrollbar issue in sphinx-book-theme, and allows `.css` file type in `check_file_type.py`.

## Impact
- API: none (documentation only)
- Flags/config: `docs/conf.py` now includes `html_static_path` and `html_css_files` for custom CSS.
- Data formats/schemas: none

## Design Elements
Design elements this commit **consumes**:
- **Function system** (`.knowledge/designs/0004-function-system.md`): Documents the pattern of registering global functions for compiler runtime state access via `TVM_FFI_STATIC_INIT_BLOCK` and `GetGlobalRequired`.
- **Module export system** (`.knowledge/designs/0008-module-export-system.md`): The guide extends the compiler integration documentation with runtime library packaging advice.

Design elements this commit **produces**: none (documentation of existing patterns)

## Reflection

* **Design docs to write**: None
* **ADRs to write**: None
* **Design diagrams to draw**: None
* **Skill evolution (`/commit-ledger`)**: None

## Self-Evolution of Skill /commit-ledger
None.
