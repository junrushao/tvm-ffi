---
author: "Junru Shao <junrushao@apache.org>"
subject: "Add Tensor and DLPack concept doc and polish existing quickstart/ABI/packaging guides"
commit_shape: "docs-only"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/concepts"
  - "docs/get_started"
  - "docs/packaging"
---
# Add Tensor and DLPack concept doc and polish existing quickstart/ABI/packaging guides
## TL;DR
- Adds `docs/concepts/tensor.rst` (483 lines): a comprehensive tutorial covering Tensor classes (DLPack vs TVM-FFI), `Any`/`AnyView` conversion protocols, C-level layout diagrams, allocation APIs, and stream handling.
- Editorial pass on `quickstart.rst`, `stable_c_abi.rst`, and `python_packaging.rst`: tightens prose, fixes grammar, standardizes phrasing ("TVM-FFI" with hyphen, active voice).
- Minor config: adds `external+dlpack:doc` to `.rstcheck.cfg` ignore roles; adds `(object-storage-format)=` cross-reference target in `abi_overview.md`; registers `concepts/tensor.rst` in `docs/index.rst` toctree.

## Key Exports
None -- docs-only commit with no code changes.

## Impact
- API: none
- Behavioral: none
- Flags/config: `.rstcheck.cfg` now ignores `external+dlpack:doc` roles (prevents false lint warnings when referencing DLPack external docs)
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `Tensor`/`TensorObj`/`TensorView` (0006-ffi-containers): documented in new `tensor.rst` with layout diagrams, ownership semantics, and API reference
- `Any`/`AnyView` conversion (0002-ffi-any-value-system, 0001-ffi-c-abi): documented with C99-compatible conversion code examples showing `kTVMFFIDLTensorPtr` vs `kTVMFFITensor` dispatch
- `TVMFFIObject` header layout (0003-ffi-object-system): referenced in tensor layout figure showing object header prefix
- `TVMFFIEnvTensorAlloc`/`TVMFFIEnvGetStream` (0011-ffi-module-system): documented allocation and stream APIs
- `Tensor::FromEnvAlloc`/`Tensor::FromNDAlloc`/`Tensor::FromNDAllocStrided` (0006-ffi-containers): documented allocation entry points with usage examples
- `tvm_ffi.from_dlpack` (0012-ffi-python-package): documented Python DLPack import pattern

Design elements this commit produces:
- None -- purely documentary

## Usage Examples
None -- docs-only commit. The new `tensor.rst` itself contains extensive code examples (C ABI conversion, C++ kernel patterns, Python import, allocation, stream handling) that serve as the canonical usage reference.

## Reflection

### Design docs / ADRs to write or update
None -- this commit itself is a substantial documentation addition.

### Stale knowledge references
None -- no symbols were renamed or removed.
