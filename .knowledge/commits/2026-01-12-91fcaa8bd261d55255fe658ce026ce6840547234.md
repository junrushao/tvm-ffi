---
author: "Junru Shao"
subject: "Revert Function.__init__ addition due to downstream breakage"
commit_shape: "revert"
commit_type: "bugfix"
scope:
  - "function-system"
  - "python-ffi-call-dispatch"
---
# Revert Function.__init__ addition due to downstream breakage
## TL;DR
- Reverts commit 8786670 ("feat: Add `tvm_ffi.Function.__init__`" #395) which added `Function.__init__(func)` constructor. The feature broke a downstream (incorrect) usage pattern.
- Removes `Function.__init__` from Cython `function.pxi`, the `.pyi` stub, and the test.
- Also reverts the bundled Doxygen comment cleanup in `base_details.h`.

## Key Exports
None (reverts previously added API).

## Impact
- API: `tvm_ffi.Function.__init__(func)` constructor removed (was added in #395, now reverted)
- Behavioral: none (restores pre-#395 behavior)
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- **Function system** (`.knowledge/designs/0004-function-system.md`) — reverts extension to `Function` class.

Design elements this commit produces:
- None (revert only).

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
