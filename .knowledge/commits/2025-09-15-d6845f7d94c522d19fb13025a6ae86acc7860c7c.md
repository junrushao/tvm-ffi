---
author: "(unknown from commit metadata)"
subject: "Fix MyST-style cross-references to reST-style in container docstrings"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "python-package"
---
# Fix MyST-style cross-references to reST-style in container docstrings
## TL;DR
- Replaces `{py:func}` (MyST/Markdown cross-reference syntax) with `:py:func:` (reStructuredText cross-reference syntax) in `Array` and `Map` docstrings in `python/tvm_ffi/container.py`.
- Fixes 4 broken Sphinx hyperlinks to `tvm_ffi.convert` in the rendered Python API documentation.

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `Array` and `Map` container classes (`python/tvm_ffi/container.py`) -- docstrings only, no code changes. See `.knowledge/designs/0008-containers.md`.
- `tvm_ffi.convert` function referenced in docstrings.

Design elements this commit produces: none

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
