---
author: "Bohan Hou"
subject: "Add Trainium (kDLTrn=17) device type to Python bindings"
commit_shape: "trivial"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
---
# Add Trainium (kDLTrn=17) device type to Python bindings
## TL;DR
- Adds `kDLTrn = 17` to `DLDeviceType` enum in both `device.pxi` and `core.pyi`.
- Adds `"trn"` string mapping in both `_DEVICE_TYPE_TO_NAME` and `_DEVICE_NAME_TO_TYPE` dictionaries.

## Key Exports

```python
class DLDeviceType(IntEnum):
    kDLTrn = 17  # AWS Trainium device
    # Interacts with: Device.__init__, Device.__str__
```

## Impact
- API: New enum value `DLDeviceType.kDLTrn = 17`.
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- DLPack device type enum (DLPack spec)
- Python Device bindings (`.knowledge/design-records/0012-python-bindings.md`)

Design elements this commit produces: None (additive only)

## Usage Examples
```python
dev = tvm_ffi.Device("trn", 0)
```

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
