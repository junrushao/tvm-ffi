---
author: "TVM FFI contributors"
subject: "Add Object and Class concept documentation"
commit_shape: "docs-only"
commit_type: "chore"
secondary_commit_type: ""
potential_duplicate: ""
has_design_updates: false
scope:
  - "docs/concepts"
---
# Add Object and Class concept documentation

## TL;DR
- Creates `docs/concepts/object_and_class.rst` (478 lines) covering `ObjectRef`/`ObjectObj` pattern, ref-counting, type hierarchy, `c_class` decorator for Python, and cross-layer interop.
- Adds a minor cross-reference from `docs/concepts/any.rst` and updates `docs/index.rst` to include the new page.
- Minor update to `docs/concepts/tensor.rst` (2 additions, 2 deletions) for cross-reference consistency.

## Key Exports

None — docs-only commit.

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes:
- `0002-object-system` — documents `FooObj`/`Foo`/`ObjectPtr<T>` three-layer pattern, `make_object<T>()`, `TVM_FFI_DECLARE_OBJECT_INFO`, `TVM_FFI_DEFINE_OBJECT_REF_METHODS`.
- `0017-c-class-dataclasses` — documents `@c_class` Python decorator and `field()` protocol.
- `0006-reflection` — documents `ObjectDef<T>` builder, `def_field`, `def_method`.

Design elements this commit produces: None.

## Usage Examples

None — docs-only commit.

## Reflection

### Design docs / ADRs to write or update
None — this commit is a docs addition.

### Stale knowledge references
None.
