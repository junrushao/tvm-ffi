---
author: "tqchen <tianqi.tchen@gmail.com>"
subject: "Establish tvm-ffi repo with license, gitignore, submodules, and NOTICE"
commit_shape: "trivial"
commit_type: "chore"
scope:
  - "repo-setup"
---
# Establish tvm-ffi repo with license, gitignore, submodules, and NOTICE
## TL;DR
- Adds `.gitignore` (286 lines of standard ignores for Python, C++, IDE, OS artifacts).
- Adds `.gitmodules` referencing `3rdparty/dlpack` and `3rdparty/libbacktrace` submodules; updates dlpack submodule pointer from `3ea601b` to `addbc8b`.
- Adds Apache 2.0 `LICENSE` and `NOTICE` file identifying the project as "Apache TVM FFI."

## Key Exports
None

## Impact
- API: none
- Behavioral: none
- Flags/config: none
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces:
- **Repo scaffolding**: `.gitignore`, `LICENSE`, `NOTICE`, `.gitmodules` -- foundational files for the standalone tvm-ffi repository. Not design elements in the architectural sense.
- **Third-party dependency declarations**: dlpack (`3rdparty/dlpack`) and libbacktrace (`3rdparty/libbacktrace`) pinned as git submodules.

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### ADRs to write or update
None

### Stale knowledge references
None
