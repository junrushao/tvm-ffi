# 8e471b01 -- 31 commits distilled
- **Template mismatch**: Needed a "cross-cutting rename" artifact type for the traceback->backtrace rename that touches C ABI, C++, Python, and file names simultaneously. The design doc template works but the evidence matrix format is poorly suited for documenting 20+ symbol renames -- a dedicated rename mapping table is more natural. Worked around by inlining the mapping in the ADR.
- **Upstream skill gap**: Several ledgers for tooling/CI commits (taplo, yamllint, markdownlint, Docker, CI matrix) produced no useful design elements. The `/commit-ledger` correctly classified them as `trivial` but they still required reading and triaging. A bulk-skip mechanism for `impact: trivial` + `scope: lint|ci|docs` commits would save time.
- **Quality bar friction**: Requiring >=1 usage example for the backtrace ADR felt forced -- the ADR is about a storage-order decision, not an API usage pattern. Included C API examples to satisfy the requirement but they added marginal value.
- **Procedure friction**: Phase 2 (Planner) says to "look into the codebase to comprehensively understand" but with 31 commits spanning 15+ scopes, the scout phase reading all ledger files already took substantial effort. The planner phase would benefit from explicit guidance on batching: "group commits by scope, plan one artifact per scope cluster."
- **Suggested skill change**: Add a "Rename Artifact" template or a structured rename table section to the design doc template, for commits that perform systematic symbol renames across multiple layers. The current approach of scattering rename notes across multiple sections is fragile and hard to review.
