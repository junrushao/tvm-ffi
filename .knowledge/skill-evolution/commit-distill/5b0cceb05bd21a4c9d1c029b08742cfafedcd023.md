# 5b0cceb05bd21a4c9d1c029b08742cfafedcd023 — 31 commits distilled
- **Template mismatch**: None. The design doc, ADR, and API index templates all fit well for this batch. The reflection system naturally spans design doc + ADR + API index.
- **Upstream skill gap**: Several ledgers for minor commits (eda2b71 clang20 fix, 1c9b17a dsymutil fix, fa5e2ac attribute refactor) listed "Design docs: None" but these commits did contribute to the overall picture (compiler compatibility, build system). The ledger shape classification correctly marked them as low-impact, which was sufficient.
- **Quality bar friction**: None. The 31 commits had enough substance across 8+ design areas to naturally produce >=1 usage example, evolution timelines, and cross-layer examples for every design doc.
- **Procedure friction**: Phase 2 (Planner) could benefit from explicit guidance on how to handle "superseding chains" -- e.g., commits 84cfbeb and 5a4038f introduced a for_each enhancement that was then reverted by 9a1d41a. The skill says nothing about how to handle reverted work; I chose to omit the reverted changes from design docs entirely but it would be cleaner to have explicit guidance.
- **Suggested skill change**: Add guidance in Phase 2 for handling reverted commits: "When a commit in the batch is later reverted by another commit in the same batch, omit both from design docs and evolution timelines. Note the revert only if the reversion itself reveals a design constraint worth capturing."
