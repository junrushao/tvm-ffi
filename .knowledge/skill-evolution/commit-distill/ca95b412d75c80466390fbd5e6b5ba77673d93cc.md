# ca95b41 -- 16 commits distilled
- **Template mismatch**: Needed a lightweight "behavioral change" note type for commits that change semantics without adding new API surface (e.g., NDArray always-populated strides, convert() no longer raising TypeError). These don't warrant a full design doc but are too significant for just an evidence matrix entry. Ended up folding into container doc updates.
- **Upstream skill gap**: None -- ledger inputs were sufficient. All 16 ledgers provided adequate Key Exports and Design Elements.
- **Quality bar friction**: Requiring >=1 usage example for packaging robustification updates (cibuildwheel config changes, RPATH fixes) felt forced -- these are build system concerns, not user-facing API. Applied examples only where meaningful.
- **Procedure friction**: Phase 2.5 (Usage Example Curation) had nothing to deduplicate since examples were already distinct across ledgers. The phase added no value for this batch. The "reuse before create" criterion was the most useful planning guide for this group's mix of many small fixes + a few big features.
- **Suggested skill change**: Add an optional "Behavioral Changes" section to the design doc template for capturing semantics changes that don't add new API surface (e.g., "strides always populated", "convert() wraps instead of raising"). These are important for reconstructibility but don't fit neatly into Key Classes/Interfaces.
