# 2025-05-06 -- 7d34eb8abfe987bf0031e4d4ff479895d867a966 (shape: large-bringup)
- **Procedure friction**: Step 1 assumes `git diff` against a parent commit, but this is the root commit with no parent. The empty tree hash (`4b825dc...`) was unavailable in the repo. Had to fall back to `git show --numstat`. The procedure should explicitly handle root commits.
- **Template gap**: For a large-bringup root commit that establishes an entire architecture, the template's "Design Elements consumed/produced" section is awkward -- nearly everything is "produced." A "Founding Architecture" variant of the template would better capture the bootstrapping nature.
- **Wasted effort**: Step 5 (Knowledge Staleness Check) is definitionally empty for the root commit since the knowledge base does not yet exist. Step 3.1 (Consumers) is also thin since there is no prior codebase to consume from.
- **Suggested skill change**: Add a "root commit" detection in Step 1 (e.g., `git rev-parse HEAD~1` fails) and provide an alternate diff command (`git show --numstat` or `git diff-tree --root`). This avoids the empty-tree-hash pitfall.
