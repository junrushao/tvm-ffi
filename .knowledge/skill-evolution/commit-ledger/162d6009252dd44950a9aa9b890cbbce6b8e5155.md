# 2025-07-22 — 162d6009252dd44950a9aa9b890cbbce6b8e5155 (shape: standard)
- **Procedure friction**: Step 2 shape classification doesn't have a "rename + feature" hybrid shape. This commit is roughly half rename and half new feature, but neither the "rename" nor "standard" shape perfectly fits. Ended up using "standard" and including a rename mapping table inline.
- **Template gap**: None.
- **Wasted effort**: None — all steps contributed. The cross-layer consistency check (Step 3.4) was light since this commit is C/C++ only, but the check was still valid to confirm no Python/Rust bindings were affected.
- **Suggested skill change**: Consider adding a "hybrid" shape or allowing shape to be a list (e.g., `["rename", "feature"]`) for commits that combine substantial renames with new functionality.
