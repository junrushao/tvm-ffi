# 2025-09-22 — 6f020c11c304ef11ac5d0dad904d41ebe42a3ffd (shape: rename)
- **Procedure friction**: This commit is rename + behavioral change + API signature change + docs/examples cleanup -- a hybrid between rename and multi-concern. The rename shape shortcut says "skip per-file narration" but the behavioral changes (reversed storage order, TVMFFIErrorCreate return type change) are substantive and non-trivial. I ended up doing deeper analysis than the rename fast path suggests.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: Add guidance for "rename + semantic change" hybrid shape. When a rename commit also changes contracts (e.g., parameter additions, return type changes, storage order reversal), the rename shortcut should explicitly note that the contract changes need full Step 4 analysis even if the identifier substitution portion can be summarized as a mapping table.
