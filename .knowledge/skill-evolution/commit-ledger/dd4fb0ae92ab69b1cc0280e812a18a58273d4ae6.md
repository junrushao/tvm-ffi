# Self-Evolution Reflection: dd4fb0ae

## What went well
- The commit message and PR title clearly described the core feature (`TypeSchema.repr(ty_map)`), making classification straightforward.
- The diff was moderate in size but dominated by docstring additions in `core.pyi` (~400 lines of doc vs ~45 lines of logic), so correctly identifying the "real" change required reading the Cython `.pxi` diff first.
- Cross-referencing the knowledge base for `TypeSchema` references confirmed that `reflection.md` and `0014-python-bindings.md` were the relevant design docs.

## What could be improved
- For commits that are mostly docstring/stub enrichment with a small functional change, the template's "Key Exports" section could benefit from a convention to separate "new API" from "documentation-only changes" more explicitly. Currently I folded both into the same section.
- The `core.pyi` diff was very large (458 insertions) but almost entirely docstrings. A heuristic to quickly classify "stub-only" vs "logic" changes by file extension (`.pyi` = stubs) would speed up analysis.

## Template observations
- The template handles this mixed commit shape (feature + docs) adequately under "standard".
- No new shape category needed.
