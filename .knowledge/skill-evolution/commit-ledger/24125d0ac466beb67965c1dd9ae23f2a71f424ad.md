# 2025-09-07 — 24125d0ac466beb67965c1dd9ae23f2a71f424ad (shape: multi-concern)
- **Procedure friction**: Step 4.5 Key Exports is awkward for commits that are primarily adding documentation comments -- the "exports" are the same pre-existing API surface, just now documented. The section ends up being mostly "None introduced" which feels like wasted template space for docs-heavy commits.
- **Template gap**: No good place to capture the Doxygen suppression convention (`/// \cond`) as a produced design element -- it is a coding convention rather than a type, function, or macro, but it is an important pattern for future contributors. Had to shoehorn it into "Design elements this commit produces."
- **Wasted effort**: Step 3.3 breaking changes analysis was useful here since there were actual ABI changes buried in a docs commit, so no wasted effort despite the docs-heavy shape. Step 3.4 cross-layer consistency was irrelevant -- this is C++ headers + Sphinx config only.
- **Suggested skill change**: For multi-concern commits where one concern is docs-only and another is code change, the shape detection should allow hybrid classification (e.g., "multi-concern: docs+standard") so the docs portion can use the abbreviated docs-only path while the code portion gets full analysis.
