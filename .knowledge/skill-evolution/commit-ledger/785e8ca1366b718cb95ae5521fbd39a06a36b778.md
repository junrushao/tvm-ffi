# 2025-09-18 — 785e8ca1366b718cb95ae5521fbd39a06a36b778 (shape: standard)
- **Procedure friction**: Step 4.5 Key Exports asks for macro expansions and protocol definitions, but this commit's main artifact is a `.pyi` type stub — essentially a declaration-only file. The guidance didn't distinguish between "new runtime API" and "new type annotation surface" well.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 (Breaking changes) and Step 3.4 (Cross-layer consistency) were irrelevant for a pure type-annotation commit with no runtime changes.
- **Suggested skill change**: Add a `typestub` or `annotation-only` shape for commits that only add `.pyi`/type annotation files with no runtime behavior change, allowing Steps 3.3/3.4 to be skipped and Key Exports to focus on the declared interface surface rather than runtime contracts.
