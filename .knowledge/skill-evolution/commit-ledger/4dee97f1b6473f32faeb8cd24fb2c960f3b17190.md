# 2025-09-12 — 4dee97f1b6473f32faeb8cd24fb2c960f3b17190 (shape: rename)
- **Procedure friction**: Step 4.5 Key Exports asks for "macro expansions" and "protocols/traits" but for a rename commit the real value is the mapping table (already in TL;DR). Key Exports largely restates the mapping with type signatures, which is useful but overlaps. The template could explicitly say "For rename commits, Key Exports should focus on the final signatures of renamed symbols, not re-list the mapping."
- **Template gap**: None.
- **Wasted effort**: Step 3.3 breaking changes and Step 3.4 cross-layer consistency are subsumed by the rename mapping table — the rename shape shortcut correctly skips per-file narration but the procedure still asks for these sub-steps explicitly.
- **Suggested skill change**: None — the rename shape shortcut worked well overall. The minor friction in Key Exports vs mapping table overlap is not worth a procedure change.
