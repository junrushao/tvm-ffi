# 2025-09-09 — a08fa6eb0d7a67de9f5a4354d9a1fe41c0dfa806 (shape: rename)
- **Procedure friction**: Step 4.5 Key Exports asks for "Types/Classes, Functions, Macros, Protocols/Traits, Constants/Enums" as separate sub-headings, but a macro-rename commit like this is better served by a single before/after mapping table plus a behavioral-change note. The structured sub-heading format doesn't fit well.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 breaking-changes check was somewhat redundant for a rename commit — the rename mapping table already communicates what changed. Step 3.4 cross-layer consistency was irrelevant since this commit is C++-only.
- **Suggested skill change**: For rename-shaped commits, the template guidance for Key Exports should explicitly say "Use a mapping table as the primary format; add behavioral notes only for macros/functions whose semantics changed beyond the rename."
