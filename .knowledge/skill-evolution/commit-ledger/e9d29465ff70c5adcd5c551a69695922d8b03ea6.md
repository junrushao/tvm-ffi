# 2025-09-08 — e9d29465ff70c5adcd5c551a69695922d8b03ea6 (shape: standard)
- **Procedure friction**: Step 2 shape classification has no shape for "alias removal" or "convention termination" commits. This commit is not quite "rename" (no identifier substitution in call sites), not "trivial" (it has meaningful API impact), and not "chore" (it changes caller contracts). Classified as "standard" but Steps 3-4 mining felt over-scoped for a deletion-only diff.
- **Template gap**: None.
- **Wasted effort**: Step 4 "Mining Key Implementations" asks to build a mental model of invariants and extension points, but the commit only deletes `using` lines. The entire step reduced to "nothing new to model." Step 4.5 Key Exports similarly produced only a removal table.
- **Suggested skill change**: Add a "deletion" or "removal" shape for commits that purely remove API surface (aliases, deprecated symbols, compatibility shims) with no new code. The analysis path would focus on: (a) what was removed, (b) migration path for callers, (c) stale knowledge references. Skip invariant/extension-point analysis.
