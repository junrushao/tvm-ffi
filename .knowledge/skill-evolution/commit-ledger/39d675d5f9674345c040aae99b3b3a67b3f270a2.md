## Observations
- This commit directly reverses a design decision from a very recent commit (9d547555, same day). The `related_commits` field is essential for these rapid-iteration CI config changes to maintain traceability.
- The diff is small (7 lines changed, 1 file) but has three distinct sub-changes: (1) trigger mechanism swap, (2) removal of conditional `if:` gating, (3) removal of the `name:` field. The `if:` removal is a meaningful behavioral change (manylinux2014 now builds unconditionally) that could be overlooked if only focusing on the cron change.
- Staleness detection worked well here: the same-day commit 9d547555 had a ledger documenting the "push on main" pattern that is now invalidated.

## Template feedback
- No template changes needed. The current structure handles CI config churn adequately.
