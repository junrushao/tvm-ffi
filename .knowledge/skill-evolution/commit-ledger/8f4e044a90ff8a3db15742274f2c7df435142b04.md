# 2025-09-17 — 8f4e044a90ff8a3db15742274f2c7df435142b04 (shape: standard)
- **Procedure friction**: Step 4.5 Key Exports asks for "Types/Classes, Functions, Macros" produced or mutated, but this commit only adds annotations to existing signatures without changing runtime behavior. The guidance doesn't distinguish between "added annotations to existing API" vs "introduced new API." Had to improvise a mapping table format.
- **Template gap**: No section for "linting/tooling configuration changes." The `pyproject.toml` `ANN` rule enablement is the most consequential change (it gates all future PRs) but doesn't fit neatly into Key Exports, Impact, or Design Elements.
- **Wasted effort**: Step 3.3 breaking changes and Step 3.4 cross-layer consistency were irrelevant — this is a Python-only annotation commit with no ABI or cross-layer dimension.
- **Suggested skill change**: Add a `chore-lint` shape for commits that enable/configure linting rules and fix violations. These commits have a distinctive pattern: one config change + many mechanical fixups. The shape should skip per-file narration and focus on "what rule was enabled, what convention it enforces, any non-mechanical fixes discovered."
