# 2025-06-25 — 69f2484f915d95886502a1f620ea69aeed623c49 (shape: standard)
- **Procedure friction**: Step 3.3 Breaking Changes asks to check public header signature changes, but the static_assert addition is a subtler kind of source-breaking change (rejects previously-compiling code) that doesn't fit neatly into "signature changes" or "enum renumbering." The checklist could mention "new compile-time constraints on existing APIs."
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency check was irrelevant -- this is a single-file, single-layer (C++ header-only) change.
- **Suggested skill change**: In Step 3.3, add a bullet for "New static_assert / concept constraints that narrow accepted inputs on existing templates" as a category of source-breaking change.
