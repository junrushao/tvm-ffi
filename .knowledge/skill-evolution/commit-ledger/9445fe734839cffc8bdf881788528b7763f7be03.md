# 2025-07-19 -- 9445fe734839cffc8bdf881788528b7763f7be03 (shape: standard)
- **Procedure friction**: Step 4.5 Key Exports requires extracting macro expansions, but for this commit the only macro (`TVM_FFI_EXTRA_CXX_API`) is a trivial `#define` alias -- the guidance was adequate but the "expansion summary" phrasing suggests more depth than needed for simple defines.
- **Template gap**: The commit introduces a rich dispatch protocol (tree/DAG/freevar/const-tree) with specific ordering of checks (pointer equality fast-paths, equal_map lookup, field traversal). The template's "Key Exports" captures signatures but not algorithmic invariants like "DAG nodes are memoized after successful comparison" or "free-var mapping is bidirectional." A "Protocol Dispatch Logic" subsection would help.
- **Wasted effort**: None -- all steps contributed. The cross-layer consistency check (Step 3.4) was light since this is a C++-only commit, but it correctly identified the `Bytes::memncmp` visibility change and the global function registrations.
- **Suggested skill change**: Add an optional "Dispatch / Algorithm Invariants" subsection under Key Exports for commits that introduce stateful comparison/traversal protocols with multiple code paths. This would capture memoization semantics, bidirectional mapping, and early-termination conditions that are critical for correctness but don't fit as type signatures.
