# 2025-08-09 — 03e8a6b8c995259d379358ab50ad81609a99083e (shape: standard)
- **Procedure friction**: Step 3.3 (Breaking changes) asks to check public header signature changes, but this commit's ABI break is purely semantic (same field, different encoding). The guidance focuses on signature/enum changes and doesn't explicitly prompt for in-field encoding changes. Had to reason about it independently.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 (Cross-layer consistency) was irrelevant -- this commit is C++ only, single file. The check added no value.
- **Suggested skill change**: In Step 3.3, add a bullet for "semantic ABI breaks: field encoding or interpretation changes that don't alter the type signature but change runtime meaning."
