# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The commit type taxonomy (`feature`, `bugfix`, `refactor`, `performance`, `chore`) does not include `test`. This commit is tagged `[TEST]` and adds only test code. Had to map it to `chore`, which loses signal about what the commit actually is.
- **Template gap**: None.
- **Wasted effort**: None -- trivial shape correctly short-circuited Steps 3-4.
- **Suggested skill change**: Add `test` as a commit type in Step 2.1, distinct from `chore`. Test-only commits are common and downstream consumers may want to filter for them.
