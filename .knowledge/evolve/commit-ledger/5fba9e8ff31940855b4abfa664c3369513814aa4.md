# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The "bugfix" type guidance asks to document "what invariant was violated before the fix" — for a compile error, there is no runtime invariant, making this framing slightly awkward.
- **Template gap**: No dedicated section for "compiler compatibility fix" — had to use the bugfix pattern which doesn't map cleanly.
- **Wasted effort**: Step 3.4 cross-layer consistency was N/A (single C++ header). Step 3.6 usage examples required a contrived before/after since there are no test files in the commit.
- **Suggested skill change**: For `bugfix` commits that are purely compile fixes (no runtime behavior change), allow "None" in Usage Examples with a note "compile-time fix only."
