# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The `test-only` shape skip table correctly guided skipping Steps 3.3 and 3.4, but Step 3.5 still directs writing Key Exports "for test utilities/fixtures." In this case the new test fixtures (`TestHash`, `TestCustomHash`) existed in C++ before this commit — only their Python exposure was added. The guidance does not distinguish "test fixture exposure" from "net-new test utilities," leading to mild confusion.
- **Template gap**: None.
- **Wasted effort**: Step 3.1 (Consumers) required reading the full design record for 0009-structural-eq-hash even though the commit only adds tests. For `test-only` shape, a lighter "which design record does this test validate?" question would suffice.
- **Suggested skill change**: For `test-only` shape, Step 3.1 guidance should be narrowed to "identify which design record(s) this test validates" rather than a full consumer analysis. This matches the actual useful output for test-only commits.
