# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.3 breaking changes — the "public header signature changes" checklist is correct but the `at()` return-type change (value → reference) is a subtlety that matters for C++ callers even though it's not a C ABI change. The guidance could explicitly mention "return type changes (value vs. reference)" as a breaking-change signal.
- **Template gap**: None. The Key Exports section handled the old→new annotation well.
- **Wasted effort**: Step 3.4 cross-layer consistency — this commit is single-layer (C++ headers only), so the check was trivially N/A. The fast-skip worked correctly.
- **Suggested skill change**: None — skill fit well.
