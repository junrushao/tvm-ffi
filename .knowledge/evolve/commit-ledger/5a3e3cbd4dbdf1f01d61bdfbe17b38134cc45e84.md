# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The `docs-only` shape skip table says "Reframe Step 4 as 'Mining Key Documentation Artifacts'", but the example files added here are runnable C++/Python code, not documentation prose. The `docs-only` shape guidance was slightly mismatched — the files are closer to a `scaffolding` or `standard` mini-example than traditional docs. A note distinguishing "examples with runnable code" from "docs with prose" would help.
- **Template gap**: None — the existing sections covered the commit well.
- **Wasted effort**: Steps 3.3 (breaking changes) and 3.4 (cross-layer consistency) were formally skipped per the docs-only shape table, which was correct and saved time.
- **Suggested skill change**: In the `docs-only` shape description, add a parenthetical: "(For `examples/` with runnable C++/Python code, treat as `docs-only` but apply Step 3.6 Usage Examples normally, since the example code itself is the documentation artifact.)"
