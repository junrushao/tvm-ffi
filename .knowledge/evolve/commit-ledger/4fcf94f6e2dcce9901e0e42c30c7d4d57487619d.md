# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None. The standard shape path fit this commit well. The "mutates" vs "produces" distinction in Step 3.2 was useful — `load_inline` is mutated (same API, new implementation) while `build_inline` is produced.
- **Template gap**: The Design Elements section only has "consumes" and "produces" slots; "mutates" is a concept in the procedure (Step 3.2) but not a labeled section in the template. A "mutates" bullet alongside "produces" would make mutation explicit without prose.
- **Wasted effort**: Step 3.4 cross-layer check was quick to dismiss (single-layer fast-skip), but the procedure still required reading it. For `python/` only commits, the fast-skip could be surfaced earlier in Step 2 to avoid the context-switch.
- **Suggested skill change**: Add a `Design elements this commit mutates:` bullet in `template-commit-ledger.md` under the Design Elements section, parallel to "consumes" and "produces". This captures contract-stable refactors where the element identity is preserved but implementation changes.
