# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.3 Breaking Changes — the "additive-only fast-skip" check is misleading for refactor commits that rename symbols in the public C ABI. The commit had zero `new file mode` entries and substantial deletions, so the fast-skip didn't apply, but the guidance focuses on "new additions." A hint like "for rename/refactor commits, check renamed public symbols in c_api.h and enums" would reduce back-and-forth.
- **Template gap**: None — the `# RENAMED from` comment annotation in Key Exports captured the before/after cleanly.
- **Wasted effort**: None — all steps produced useful content for this shape.
- **Suggested skill change**: In Step 3.3, add a bullet: "For refactor commits touching `c_api.h`, check enum constant prefix changes and struct field renames as breaking changes, even when net LOC is small."
