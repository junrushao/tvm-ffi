# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 checkout failed with a worktree lock error when another agent process holds the index.lock. The procedure doesn't address this fallback scenario; it would benefit from a note that diff analysis may proceed via `git -C "$REPO_MAIN" show` when the worktree checkout is unavailable.
- **Template gap**: None.
- **Wasted effort**: Steps 3.3–3.6 were fully skipped per ci-infra fast-path, which was appropriate. The ci-infra shape path worked cleanly.
- **Suggested skill change**: Add a fallback note in Step 0.3: "If checkout fails due to `index.lock`, read commit diff via `git -C "$REPO_MAIN" show $COMMIT` and proceed; note the fallback in the log."
