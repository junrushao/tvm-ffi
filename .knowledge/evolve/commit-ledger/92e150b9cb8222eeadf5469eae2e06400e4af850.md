# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 (checkout in worktree) was blocked by sandbox preventing new worktree creation. The skill should document a fallback: if `git worktree add` fails, proceed with `git -C $REPO_MAIN show` for diff analysis — read-only and sufficient.
- **Template gap**: None.
- **Wasted effort**: None for this commit shape.
- **Suggested skill change**: Add a note in Step 0.3 that when sandbox restricts worktree creation, `git -C $REPO_MAIN show $COMMIT` is an acceptable fallback for diff-only analysis.
