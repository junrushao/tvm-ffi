# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The `ci-infra` shape correctly skips Steps 3.3–3.6, but Step 3.1 (Consumers) still yields value even for ci-infra when the CI script calls specific library discovery patterns (e.g., `tvm_ffi.libinfo`) — the per-shape skip table could clarify that 3.1 is optional-but-useful for ci-infra rather than fully skipped.
- **Template gap**: None.
- **Wasted effort**: None — shape was clear early, fast-path applied cleanly.
- **Suggested skill change**: None — skill fit well.
