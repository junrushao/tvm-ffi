# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 checkout is blocked by the filesystem sandbox (cannot write index.lock). The procedure should note that when running as a proxy-agent (not a spawned subagent), all diff data is still fully accessible via `git -C $REPO_MAIN show` without checkout.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: Add a fallback note in Step 0.3: "If `git checkout` is blocked by filesystem restrictions, fall back to `git -C $REPO_MAIN show $COMMIT` for all diff reads — no checkout is required for analysis-only workflows."
