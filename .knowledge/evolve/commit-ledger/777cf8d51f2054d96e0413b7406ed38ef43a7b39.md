# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.3 (breaking changes) worked well for this ABI-enum-reordering commit, but the "additive-only fast-skip" heuristic (zero deletions = skip) would have incorrectly skipped this — there were deletions, but the real breaking change was enum *renumbering*, not removal. The heuristic could note enum value changes explicitly.
- **Template gap**: None — the template handled the enum-before/after and parameter-rename patterns well via the "old → new as comments" annotation.
- **Wasted effort**: None significant.
- **Suggested skill change**: In Step 3.3, add "enum value renumbering" explicitly to the breaking-change checklist bullets (it is listed but can be missed when the values shift without removals). E.g., "Enum value renumbering — even if no enum *members* are removed, reordering numeric values breaks ABI for all compiled consumers."
