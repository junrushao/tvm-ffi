# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 diff-message alignment check (under Step 2.2) is useful but easy to miss — it appears as a note at the end of the shape classification table rather than as its own step. For commits where the message scope substantially exceeds the diff, this caused me to spend time on Step 1.1 before realizing the mismatch. A dedicated early check (before shape classification) would save re-reading effort.
- **Template gap**: None — the "Diff-message alignment note" inline annotation in the TL;DR section was a natural fit.
- **Wasted effort**: Step 3.3 breaking-change analysis triggered the full path before I realized the `def_ro`/`def_rw` change is purely additive (widens template parameters; existing callsites still compile). An explicit "template parameter widening is additive" heuristic in the additive-only fast-skip rule would help.
- **Suggested skill change**: In Step 2.2, add a "diff-message alignment" sub-check as Step 2.1a (before shape classification) with explicit instruction to narrow analysis scope when message names components absent from the diff. This prevents spending Steps 1.x reading for breadth that isn't there.
