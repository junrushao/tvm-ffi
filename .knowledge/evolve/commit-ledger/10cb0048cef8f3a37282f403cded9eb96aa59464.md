# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 checkout via `cd "$REPO_WORKTREE" && git checkout` failed with index.lock Operation not permitted in sandbox; used `git show` from main repo instead. The skill should clarify that `git show $COMMIT` from $REPO_MAIN is a valid fallback when checkout is blocked.
- **Template gap**: None.
- **Wasted effort**: None — the refactor shape was correctly identified quickly.
- **Suggested skill change**: Add a fallback note in Step 0.3: "If `git checkout` fails due to index.lock or sandbox restrictions, read diff via `git show $COMMIT` from $REPO_MAIN without checking out."
