# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None — the `ci-infra` shape fast-path (skip Steps 3.3, 3.4, 3.5, 3.6) matched perfectly for a single-file YAML config change.
- **Template gap**: None.
- **Wasted effort**: Reading the full design-index.md was unnecessary for a pure `ci-infra` commit with no code changes; the shape classification at Step 2.2 could short-circuit the knowledge-base read at Step 1.3 for `ci-infra`/`trivial`/`formatting` shapes.
- **Suggested skill change**: Consider adding a guard in Step 1.3: "For `ci-infra`, `trivial`, and `formatting` shapes (classifiable from the numstat alone), skip the design-index read entirely."
