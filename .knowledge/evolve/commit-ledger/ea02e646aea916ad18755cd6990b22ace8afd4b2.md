# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 (checkout) is frequently blocked by the sandbox file lock. The procedure could note that `git show` on `$REPO_MAIN` is a valid fallback when checkout is blocked — currently the procedure only says "retry the checkout," which doesn't help in a sandbox-blocked case.
- **Template gap**: None. The template covered this feature commit well.
- **Wasted effort**: Step 3.4 cross-layer consistency check was lightweight here (Python-only changes). The single-layer fast-skip applied cleanly.
- **Suggested skill change**: In Step 0.3/0.4, add a fallback note: "If checkout fails due to file lock (sandbox restriction), proceed using `git -C $REPO_MAIN show $COMMIT` for all diff reads — the checkout is only needed to walk the tree at HEAD, which is not required for ledger generation."
