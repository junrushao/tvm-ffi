---
author: "Tianqi Chen"
subject: "Add kwargs_wrapper utility for positional-to-keyword argument conversion"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
---
# Add kwargs_wrapper utility for positional-to-keyword argument conversion

## TL;DR
- Adds `tvm_ffi.utils.kwargs_wrapper` (360 LOC) for wrapping positional FFI functions with Python keyword argument support
- Includes comprehensive tests and benchmarks

## Key Exports
```python
class KwargsWrapper:
    """Wraps a positional-args FFI function with keyword argument support.
    # Interacts with: Function (0003), TypeSchema (0012)
    """
    def __init__(self, func: Function, schema: TypeSchema): ...
    def __call__(self, *args, **kwargs) -> Any: ...
```

## Impact
- API: New `KwargsWrapper` utility class
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Function system (`0003-function-system`)
- Python bindings (`0012-python-bindings`): TypeSchema

Design elements this commit produces:
- `KwargsWrapper` utility for keyword argument support

## Usage Examples
```python
from tvm_ffi.utils.kwargs_wrapper import KwargsWrapper
wrapped = KwargsWrapper(ffi_func, schema)
result = wrapped(x=1, y=2)  # calls ffi_func(1, 2)
```

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
