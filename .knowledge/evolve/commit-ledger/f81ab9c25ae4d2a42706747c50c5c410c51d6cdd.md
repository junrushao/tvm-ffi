# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The multi-concern shape requires analyzing 4 independent concerns but the template has a single flat Key Exports section. Grouping exports by concern (subsection headings) would improve readability for multi-concern commits but the procedure says not to add new headings.
- **Template gap**: None.
- **Wasted effort**: None. All steps were relevant for a multi-concern feature commit touching C ABI, C++ internals, Cython bindings, and Python packaging.
- **Suggested skill change**: For multi-concern shape, consider allowing optional `### Concern N: <name>` sub-headings within Key Exports to group related symbols, rather than flattening all concerns into one code block with only comments for separation.
