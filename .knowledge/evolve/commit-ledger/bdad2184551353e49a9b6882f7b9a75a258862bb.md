# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None — the standard bugfix path (Steps 3.1–3.7) fit well. Cross-layer section (3.4) was relevant since both C++ and Python were touched.
- **Template gap**: None.
- **Wasted effort**: The `TVM_FFI_LOG_EXCEPTION_CALL_BEGIN/END` vs `TVM_FFI_SAFE_CALL_BEGIN` distinction was important for understanding the abort root cause but required two extra lookups (macro definition + which functions use which wrapper). The design index has no entry for this pattern. This is a knowledge gap, not a skill gap.
- **Suggested skill change**: None — skill fit well.
