# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 requires `cd $REPO_WORKTREE && git checkout $COMMIT`, but sandbox restrictions block `index.lock` creation when using a subshell `cd`. Using `git -C $REPO_WORKTREE checkout` consistently avoids this. The procedure could note `git -C` as the preferred form.
- **Template gap**: None.
- **Wasted effort**: Steps 3.1–3.7 were all skipped for `trivial` shape as specified. The skip table worked correctly.
- **Suggested skill change**: In Step 0.3, prefer `git -C "$REPO_WORKTREE" checkout "$COMMIT"` over `cd "$REPO_WORKTREE" && git checkout "$COMMIT"` to avoid sandbox index.lock failures in sandboxed execution environments.
