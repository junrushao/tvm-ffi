# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.4 cross-layer consistency check was relevant here (Python/Cython/C++ boundaries all touched) but the analysis naturally emerged from the diff review. The separate step felt slightly redundant for a commit where the cross-layer nature was obvious from the file list.
- **Template gap**: The rename mapping table is large (16 rows) and combines API surface renames with internal struct field renames. A structured distinction between "public API renames" and "internal implementation renames" would help readers prioritize what matters for their own code.
- **Wasted effort**: None — all steps contributed meaningful information for this commit shape.
- **Suggested skill change**: For rename-heavy commits that are classified as `standard` (not `rename` shape), consider adding guidance to split the rename table into "public/dunder API changes" vs "internal struct/function changes" subsections, since the former has direct impact on external consumers while the latter is implementation detail.
