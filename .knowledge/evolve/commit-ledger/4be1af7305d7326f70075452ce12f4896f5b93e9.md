# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None. The standard shape path applied cleanly to this removal commit.
- **Template gap**: None. The before/after pattern in Usage Examples handled the removal well.
- **Wasted effort**: Step 3.3 (breaking changes) required manually confirming `Downcast` removal is indeed breaking — the procedure could benefit from a fast-path note: "removal of a `using` alias in a public header always counts as a breaking change; no need to scan for subtleties."
- **Suggested skill change**: Add to Step 3.3 a note: "Removal of `using <name>` re-exports from public headers is always a breaking API change — mark immediately without further analysis."
