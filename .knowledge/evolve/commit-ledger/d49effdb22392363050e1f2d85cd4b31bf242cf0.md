# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Large-bringup shape with examples/ and docs/ directories inflates the numstat significantly. The large-bringup classification correctly caps Key Exports at ~15 symbols, which was helpful.
- **Template gap**: No section for "CMake API surface" — CMake utility functions like `tvm_ffi_generate_cubin` are first-class API but pseudocode representation feels awkward in Python-style pseudo.
- **Wasted effort**: Reading full diff for examples/ (311+112+122+... lines) would have been wasted; numstat-first approach was correct.
- **Suggested skill change**: Add a note that for large-bringup commits with CMake utilities, pseudocode for CMake functions should use a comment-only form (e.g., `# tvm_ffi_generate_cubin(OUTPUT, SOURCE, ARCH)`) rather than Python-style class/def.
