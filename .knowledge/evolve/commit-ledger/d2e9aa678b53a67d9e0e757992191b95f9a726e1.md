# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The shape classification table does not have a clean category for "packaging + compat fix" commits: the diff is neither pure `ci-infra` (Python source changed meaningfully) nor `standard` (no new design elements). Settling on `standard` with type `chore` worked but felt imprecise.
- **Template gap**: None — the Impact section adequately captured the behavioral nuance of `include_extras` being silently dropped on 3.8.
- **Wasted effort**: Step 3.3 breaking-change analysis required checking all public header signatures even though this is clearly a packaging/compat commit; the additive-only fast-skip check was clean but the surrounding analysis steps were still walked through mentally.
- **Suggested skill change**: Add a `compat-fix` shape entry in the shape table for commits whose primary change is adapting existing code to run on an older language/platform version without changing public API — skip Step 3.3 (breaking changes) and Step 3.5 (Key Exports) for this shape, since no API surface changes.
