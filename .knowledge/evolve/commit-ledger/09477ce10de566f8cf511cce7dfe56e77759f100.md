# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.5 Key Exports cap at ~15 symbols was helpful for the large-bringup shape. However, the "Python-style pseudocode" for Rust types is somewhat awkward — Rust traits and lifetime parameters don't map cleanly to Python class syntax.
- **Template gap**: No section for noting cross-language ABI mirroring (e.g., "Rust struct is `#[repr(C)]` mirror of C++ struct X"). The Impact section partially covers this but lacks a dedicated "ABI compatibility notes" field.
- **Wasted effort**: None — shape detection and fast-path worked well for this large-bringup.
- **Suggested skill change**: For `large-bringup` commits that introduce a new language binding, add a "Language Binding Correspondence" table in Key Exports: C++ type → Rust type → Python type, showing which layer each symbol lives in.
