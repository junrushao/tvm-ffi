# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The shape classification table has no explicit entry for "code extraction" (split one file into two, moving existing code with minimal changes). This commit landed as `standard` but the bulk of `map_base.h` is a verbatim copy of what was in `map.h`. A `large-bringup` heuristic based solely on LOC would have misfired; the actual complexity was the hierarchy change and templatization.
- **Template gap**: None — the Key Exports section handled the before/after pattern for ArrayObj and the new class hierarchy well.
- **Wasted effort**: Step 3.6 Usage Examples required fabricating pseudocode since the commit has no accompanying tests or docs. The result is useful but speculative.
- **Suggested skill change**: Add a `code-extraction` shape (or a note under `refactor`) for commits that predominantly move code between files with minor edits. Key signal: >70% of new-file LOC already existed in an old file. Analysis should focus on the structural/hierarchy changes rather than the extracted code itself.
