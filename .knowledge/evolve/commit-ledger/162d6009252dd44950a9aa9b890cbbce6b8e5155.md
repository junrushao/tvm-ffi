# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The "composite shapes" guidance in Step 2.2 is helpful but the per-shape skip table doesn't have a row for composite shapes — the analyst must mentally combine two rows. A short note like "for rename+standard, apply standard depth but include rename mapping table" would save re-reading the prose description.
- **Template gap**: None — the rename mapping table fits cleanly as a freeform table in TL;DR, which the template already allows.
- **Wasted effort**: Step 3.3 (breaking changes) required non-trivial analysis because the rename is ABI-breaking (struct field rename), but the standard fast-skip criteria ("additive-only") didn't fire. The result was correctly flagged, so no wasted effort, just slower than a rename-shape fast path would be.
- **Suggested skill change**: None — skill fit well.
