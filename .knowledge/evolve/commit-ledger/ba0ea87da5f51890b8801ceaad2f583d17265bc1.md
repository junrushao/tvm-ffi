# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: The bugfix-within-performance-commit case (latent correctness bug fixed incidentally) isn't well-called-out by the `performance` type. The `commit_type` field is a single value, but this commit is both `performance` and `bugfix`. A tuple or secondary-type field would be cleaner.
- **Wasted effort**: Step 3.4 cross-layer consistency check was immediately skippable (single C++ layer) — the single-sentence fast-skip helped, no wasted effort.
- **Suggested skill change**: Add a `secondary_commit_type` optional frontmatter field for commits that combine types (e.g., performance + bugfix). The primary type drives analysis depth; secondary is informational for downstream distill.
