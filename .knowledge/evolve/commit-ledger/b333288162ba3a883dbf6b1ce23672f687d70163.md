# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None. The standard shape path fit well. Step 3.4 cross-layer check correctly resolved to "N/A — single layer" (all changes are C++ only).
- **Template gap**: The `def_method` API in `GlobalDef` wraps both `ObjectRef`-by-value and `Object*`-by-pointer dispatch behind a single name, but the Key Exports section has no natural slot for "one method, two dispatch paths." Had to fold both into a single pseudocode entry with inline comments. A small "Dispatch Variants" sub-annotation in the Key Exports guide would help.
- **Wasted effort**: None.
- **Suggested skill change**: None — skill fit well.
