# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The shape classification table doesn't cleanly handle commits that are simultaneously `test-only` (by file type) and `bugfix` (by intent + docstring addition to production code). The `secondary_commit_type` field accommodated this, but the shape decision required judgment not covered by the table.
- **Template gap**: None — the before/after Usage Examples section handled the lifecycle invariant well.
- **Wasted effort**: Step 3.4 cross-layer consistency check was not applicable (single-layer Python commit), but the skip table required reading through it to confirm. The "Single-layer fast-skip" instruction is helpful but buries the skip instruction inside the step text rather than the skip table.
- **Suggested skill change**: In Step 2.2 shape table, add a note under `test-only`: "If a test-only commit also touches a production `.py` docstring, classify as `standard` with `secondary_commit_type: docs`." This avoids the shape ambiguity for this common pattern.
