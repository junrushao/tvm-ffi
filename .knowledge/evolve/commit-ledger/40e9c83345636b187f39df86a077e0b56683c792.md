# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification — a commit that's primarily `chore` (adding mypy hooks) but involves substantive `refactor` across 25+ source files doesn't fit cleanly into `ci-infra` (which would skip too much) or `standard` (which asks for deep invariant mining of functionally unchanged code). The guidance could benefit from a `tooling-enablement` shape for type-checking/linting bring-up PRs.
- **Template gap**: None — the Impact section adequately captured the behavioral change in `_find_cuda_home`.
- **Wasted effort**: Step 3.3 breaking-change analysis was borderline — the commit is largely additive/narrowing but `_find_cuda_home` Windows behavior change warranted documenting. The additive fast-skip check didn't fully apply.
- **Suggested skill change**: Consider adding a `tooling-enablement` shape for commits whose primary goal is enabling a linter/type checker — these require checking for behavioral side-effects (like `_find_cuda_home`) but can skip most invariant mining. Detection signal: commit message contains "enable mypy/ruff/clang-tidy/pyright" and >50% of diff is annotation additions/`type: ignore` suppressions.
