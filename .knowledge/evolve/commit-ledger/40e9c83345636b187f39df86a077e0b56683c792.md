# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.5 Key Exports asks for Python-style pseudocode of "public API surface introduced or changed." For a type-annotation-only commit, the "changed" surface is every function that got a tighter type -- the guidance doesn't clarify how to scope exports when runtime behavior is unchanged but static types are improved. Had to use judgment to pick the ~5 most significant signature changes rather than listing all ~30.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 breaking-changes check was fully irrelevant -- all changes are type-annotation-only and additive (new `.pyi` file, new `@overload` stubs). The "additive only" fast-skip worked correctly.
- **Suggested skill change**: None -- skill fit well.
