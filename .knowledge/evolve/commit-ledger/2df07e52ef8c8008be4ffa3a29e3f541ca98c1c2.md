# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 requires `cd "$REPO_WORKTREE" && git checkout` but sandbox blocks the `cd+git` form; must use `git -C` instead. The procedure could note that `git -C "$REPO_WORKTREE" checkout "$COMMIT"` is the preferred form.
- **Template gap**: None.
- **Wasted effort**: None — the commit is small (2 files, standard shape) and the steps fit well.
- **Suggested skill change**: In Step 0.3, add a note: "Prefer `git -C \"$REPO_WORKTREE\" checkout \"$COMMIT\"` over `cd \"$REPO_WORKTREE\" && git checkout` to avoid shell state issues in sandboxed environments."
