# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2 diff-message alignment check was useful here -- it correctly flagged that the commit message describes a feature but the diff is only a benchmark script. However, the procedure does not provide guidance on how to adjust depth when the diff is purely a script/test that consumes undocumented APIs. Should the ledger document the consumed-but-undocumented API (autodlpack) in detail, or stay scoped to the diff?
- **Template gap**: None.
- **Wasted effort**: Step 3.3 (breaking changes) and Step 3.4 (cross-layer consistency) were trivially N/A for a single Python benchmark script. The single-layer fast-skip helped but the mental overhead of checking is nonzero.
- **Suggested skill change**: Add guidance in Step 2.2 for a "benchmark/test-only" shape variant: when the diff is entirely scripts/tests with no library code, skip Steps 3.3-3.4 and cap Key Exports at "None" with a note about consumed APIs. This would reduce overhead for commits that are purely exercising existing features.
