# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The diff-message alignment check (Step 2) was the most valuable step here — the commit message described a major FFI feature ("AutoDLPack") while the diff only contained a benchmark script. The procedure correctly calls this out but doesn't give specific guidance on how to handle "ghost feature" commits where the feature implementation appears in a prior commit and this commit only adds tooling around it.
- **Template gap**: None — the ci-infra shape's "N/A — standalone script" guidance fit cleanly.
- **Wasted effort**: Steps 3.3 (breaking changes) and 3.4 (cross-layer consistency) were trivially skipped per the ci-infra fast-path, as expected.
- **Suggested skill change**: For ci-infra/standalone-script commits whose commit message describes a broader feature, add a note to Step 2 diff-message alignment: "If the message describes a feature not present in the diff, identify the likely prior commit SHA where the feature landed (search git log by keyword) and note it as `implements_feature_from: <sha>` in the ledger frontmatter." This would help downstream distill connect benchmark scripts to the actual feature commit.
