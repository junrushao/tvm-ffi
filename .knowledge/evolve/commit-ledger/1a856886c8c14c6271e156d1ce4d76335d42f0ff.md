# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.3 (breaking changes) asks to check "public header signature changes" but doesn't explicitly flag C struct layout reordering (field reorder without rename) as a breaking change category. This commit reordered `TVMFFIFieldInfo` fields, which is ABI-breaking but doesn't match typical "signature change" language.
- **Template gap**: The Impact section has no dedicated "ABI-binary compat" row. Struct layout changes (field reorder, size change) are distinct from behavioral or API-name changes but don't fit cleanly into any existing Impact row.
- **Wasted effort**: None — the standard shape analysis path was fully appropriate.
- **Suggested skill change**: Add "ABI/struct layout" as an explicit bullet in Step 3.3's breaking-change checklist: "Struct member reordering or size changes in C ABI headers (`TVMFFIFieldInfo`, `TVMFFIAny`, etc.)". This surfaces the change category without requiring the analyst to infer it from first principles.
