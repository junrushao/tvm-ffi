# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification: this commit is a hybrid refactor+rename+feature. The shape taxonomy forces a single primary shape, but the commit genuinely has three facets (renames, new API surface, struct layout change). Picking "standard with rename secondary" works but the rename mapping table guidance only appears under the "rename" shape fast path, not as a general tool for standard-shape commits that include renames.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: In Step 2.2, add guidance: "For standard-shape commits that include significant renames (>5 symbols), include a rename mapping table in TL;DR regardless of primary shape classification." This avoids needing to designate rename as a secondary shape just to trigger the mapping table.
