# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.3 (breaking changes) — the "additive-only fast-skip" check was not applicable here, but the guidance to check both public headers and `TypeTraits` specializations was critical and adequate. No friction.
- **Template gap**: The Key Exports section works well for showing old→new signatures via comments, but for commits that change the *base class* of a type (ObjectRef → value type), there is no dedicated template slot for "class hierarchy change." The impact section absorbed this, but a structured "Inheritance/base class changes" bullet under Impact would save judgment calls.
- **Wasted effort**: Step 3.4 cross-layer consistency — only C++ changed in this commit (no Python/Rust diff); the single-layer fast-skip applied cleanly.
- **Suggested skill change**: Add an "Inheritance/base class changes" sub-bullet to the Impact section template for commits that add/remove base classes or change `ObjectRef` status of a type. This is a common pattern in TVM FFI refactors.
