# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 checkout fails silently in sandboxed environments (index.lock write blocked), leading to CHECKOUT_FAILED in Step 0.4 without a clear recovery path in the procedure. The workaround (read diff from main repo object store via `git show`) is natural but undocumented.
- **Template gap**: None — the template fit well for a feature+bugfix hybrid commit.
- **Wasted effort**: Step 3.3 (breaking changes) required examining all three function signatures but the change is purely additive (new optional parameter with default), making the analysis quick. The "additive-only fast-skip" check worked correctly here.
- **Suggested skill change**: Add a recovery note to Step 0.3: "If `git checkout` fails due to a sandbox `index.lock` restriction, fall back to reading all diffs via `git -C $REPO_MAIN show $COMMIT` (reads from object store, no index write needed) and note the workaround in the log."
