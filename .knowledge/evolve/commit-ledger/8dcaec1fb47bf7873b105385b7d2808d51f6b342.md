---
author: "Junru Shao"
subject: "Add keep_module_alive parameter to load_module"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - "ffi/extra"
---
# Add keep_module_alive parameter to load_module

## TL;DR
- Adds `keep_module_alive` parameter to `load_module` and `cpp.load` to prevent premature module unload
- Implements C++ side `ModuleKeepAlive` global function

## Key Exports
```python
def load_module(path: str, *, keep_module_alive: bool = False) -> Module:
    """Load a shared library module.
    # keep_module_alive: if True, module is kept alive for process lifetime
    # Interacts with: Module system (0011), ffi.ModuleKeepAlive
    """
    ...
```

## Impact
- API: New `keep_module_alive` parameter on `load_module` and `cpp.load/load_inline`
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Module system (`0011-module-system`): load_module

Design elements this commit produces:
- `keep_module_alive` module lifetime management feature

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
