# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The shape classification table has no dedicated shape for "annotation-only + incidental bugfix" commits. This commit was classified as `standard` (chore with secondary bugfix), but the `formatting` fast-path almost applied. The boundary between "formatting" and "standard/chore" is fuzzy when a commit adds type annotations (semantically meaningful, not just whitespace).
- **Template gap**: None.
- **Wasted effort**: Step 3.3 (breaking changes) and step 3.4 (cross-layer consistency) analysis were minimal for this commit since all changes were additive annotations. The existing fast-skip rules (additive-only, single-layer) handled them efficiently.
- **Suggested skill change**: Add a `annotation-only` shape to the shape table (detection: ANN lint rule enforcement commit, or diff where >80% of lines are signature-only changes with no logic changes). Apply the same fast-path as `formatting` except retain step 3.5 for any incidental behavioral fixes found alongside annotations.
