# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape detection: packaging/install-layout commits don't fit neatly into any shape. This commit touches Python code (`libinfo.py`), CMake, pyproject.toml, docs, and examples — all within a single packaging concern. `ci-infra` is closest but excludes Python package code; `standard` is technically correct but overweights the analysis requirement for what is essentially a path relocation.
- **Template gap**: No section for "install layout conventions" — the before/after cmake install destination change is architecturally significant for downstream consumers but doesn't fit naturally into Key Exports (which implies runtime API) or Impact (which focuses on behavioral semantics).
- **Wasted effort**: Step 3.3 (breaking changes) and 3.4 (cross-layer consistency) required checking but yielded thin results for a packaging-path change; the real impact is on downstream project cmake integration, which the template doesn't have a dedicated slot for.
- **Suggested skill change**: Add a `packaging` shape to the shape table in Step 2.2 for commits whose primary concern is install layout, build system config, or packaging metadata (pyproject.toml, setup.cfg, CMake install destinations). Fast-path: skip 3.3 full analysis, focus on before/after install path mapping and downstream consumer impact.
