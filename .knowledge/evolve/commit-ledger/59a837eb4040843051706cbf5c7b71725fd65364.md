# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 does not address a stale index.lock file preventing checkout in the worktree. The procedure has no retry guidance for this — the operator had to fall back to `dangerouslyDisableSandbox: true` to clear the lock.
- **Template gap**: None — the template's "old → new" comment pattern in Key Exports fit the signature-change pattern well.
- **Wasted effort**: None — commit shape was correctly identified as standard; all steps were relevant.
- **Suggested skill change**: Add a Step 0.3 retry note: if `git checkout` fails with "Operation not permitted" and a `*.lock` file is mentioned, remove the lock file (`rm <path>.lock`) and retry before escalating to sandbox bypass.
