# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification was awkward -- this commit touches 56 files but is a mechanical sweep, not a "large-bringup" with new functionality. The `large-bringup` shape's "30 new files" threshold triggered but the analysis path (structured template, skip per-file narration) didn't fit well since there are no "produced elements" in the bringup sense. Fell back to `standard`.
- **Template gap**: None.
- **Wasted effort**: None.
- **Suggested skill change**: Add a `mechanical-sweep` shape for commits that touch many files but apply a uniform transformation (linter fixes, rename sweep, formatting). Detection: >20 files, additions ~ deletions within 30%, commit message contains "lint"/"tidy"/"format"/"modernize". Analysis path: skip per-file narration, focus on embedded correctness fixes, list the transformation categories as bullet points.
