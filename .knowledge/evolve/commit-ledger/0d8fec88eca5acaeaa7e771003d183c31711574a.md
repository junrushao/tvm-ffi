# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification: the `chore` type with widespread mechanical changes across many files (noexcept, std::move, _v suffixes) mixed with a few genuine correctness fixes is awkward — neither `formatting` (has real correctness impact) nor fully `standard` (no new design elements). The procedure would benefit from a `lint-sweep` shape for "large linter-driven sweep with minor correctness fixes embedded."
- **Template gap**: None.
- **Wasted effort**: Step 3.5 (Key Exports pseudocode) for a chore/lint-sweep commit produced mostly NOLINT annotation inventory rather than architecturally meaningful pseudocode. The correctness fixes (noexcept, header init, MoveTVMFFIAnyToAny) were worth documenting but the rest was noise.
- **Suggested skill change**: Add a `lint-sweep` shape (between `formatting` and `standard`) for commits that are primarily linter-driven modernizations but include a small number of correctness fixes. Analysis path: skip Step 3.1/3.2 (no new consumers/producers), run Step 3.3 only for the correctness-fix subset, skip Step 3.6, keep Step 3.4/3.7.
