# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.3 breaking-change analysis: the "additive-only fast-skip" correctly fired (zero deletions), but the commit is part of an explicit rename (`TVM_DLL_EXPORT_TYPED_FUNC` → `TVM_FFI_DLL_EXPORT_TYPED_FUNC`) where the old name will be removed in a future commit. The fast-skip suppresses this deprecation signal. A note in Step 3.3 to check the commit message for explicit "will be removed" language would surface this without full analysis.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency — single-layer fast-skip applied correctly (only `include/tvm/ffi/` touched). No wasted effort.
- **Suggested skill change**: In Step 3.3 additive-only fast-skip, add: "Exception: if the commit message explicitly mentions future removal of a legacy name, note the pending deprecation even when the diff itself is additive."
