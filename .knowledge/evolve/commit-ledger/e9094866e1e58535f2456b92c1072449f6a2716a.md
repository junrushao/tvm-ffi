# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.7 (knowledge staleness check) required manually cross-referencing the design record against the diff to find the renamed class (`ReflectionDef` → `ObjectDef`) and renamed methods (`def_readonly`/`def_readwrite` → `def_ro`/`def_rw`). The procedure says "grep design-records for symbols renamed or removed" but the old names were not directly grep-able from the diff — they appeared in the design doc prose, not as raw identifiers in the diff. This required reading the full design doc.
- **Template gap**: None. The template covered the feature + refactor hybrid commit well.
- **Wasted effort**: Step 3.4 cross-layer consistency check was lightweight since the diff is entirely C++ (no Python changes in this commit). The check was fast, but the guidance is written for multi-layer changes — single-layer fast-skip path was applied correctly.
- **Suggested skill change**: None — skill fit well.
