# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None — the `rename` shape fast-path (Step 2.2) mapped cleanly to this commit: macro expansion table as the rename map, and skipping per-file narration was the right call for 197 identical substitutions across 16 files.
- **Template gap**: None — the before/after mapping table in the TL;DR section and the `Key Exports` section handled macro expansion changes adequately.
- **Wasted effort**: Step 3.7 (Knowledge Staleness Check) required checking for `__attribute__` strings in `.knowledge/` but the only hits were for *different* macros not touched by this commit. The check was quick but would benefit from a note to scope it to the actual changed symbol names rather than broad substring matching.
- **Suggested skill change**: None — skill fit well.
