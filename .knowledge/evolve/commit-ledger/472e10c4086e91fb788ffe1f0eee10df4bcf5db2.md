# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.3 (Breaking changes) — the "additive-only fast-skip" check doesn't cleanly apply to macro contract changes: deletions are present but the macro itself isn't a public API in the normal sense. The guidance could clarify that macro constructor changes affecting all subclasses count as breaking even with low deletion count.
- **Template gap**: None. The Key Exports section handled the macro before/after patterns well via `# OLD:` / `# NEW:` inline comments.
- **Wasted effort**: Step 3.6 (Usage Examples) for a refactor commit where all changes are internal plumbing required inventing pseudocode rather than extracting from tests. The existing test (`TEST(ObjectRef, UnsafeInit)`) was minimal and not very illustrative of the full pattern. Minor effort loss.
- **Suggested skill change**: None — skill fit well.
