# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency check required reading Python file diffs that turned out to be purely mechanical updates to embedded C++ string literals, not actual cross-layer semantic changes. The single-layer fast-skip heuristic ("all changed files belong to a single language layer") does not account for Python files whose only changes are to inline C++ source strings.
- **Suggested skill change**: Refine the single-layer fast-skip in Step 3.4 to also consider the _content_ of changes, not just file extensions. If Python/Rust files only change embedded C++ string literals (detected by the diff being entirely within triple-quoted strings or raw string literals), treat as single-layer for the purpose of cross-layer consistency analysis.
