# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.3 Breaking Changes — the "additive-only fast-skip" check is slightly ambiguous for `using` declaration commits: they add new names to an outer namespace, which is additive, but could shadow existing names in edge cases. The skip rule could clarify that `using` declarations in namespace blocks count as additive unless an existing outer-namespace symbol with the same name exists.
- **Template gap**: None — the Key Exports section handled `using`-alias commits naturally by showing before/after calling convention comments.
- **Wasted effort**: Step 3.4 Cross-layer consistency — this commit is single-layer C++ only; the fast-skip rule fired correctly. No friction.
- **Suggested skill change**: None — skill fit well for this small refactor shape.
