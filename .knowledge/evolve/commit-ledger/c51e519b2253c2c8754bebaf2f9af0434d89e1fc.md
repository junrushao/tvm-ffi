---
author: "DarkSharpness"
subject: "Add C++ dtype_trait and Python-side dtype mapping"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/extra"
  - "python/tvm_ffi"
---
# Add C++ dtype_trait and Python-side dtype mapping

## TL;DR
- Adds `include/tvm/ffi/extra/dtype.h` (206 LOC) with C++ dtype_trait for compile-time dtype queries
- Adds `python/tvm_ffi/cpp/dtype.py` (104 LOC) for Python-side dtype mapping to C++ types

## Key Exports
```python
# include/tvm/ffi/extra/dtype.h
# dtype_trait<T>::dtype() -> DLDataType  (compile-time)
# Specializations for: float, double, int8..int64, uint8..uint64, float16, etc.

# python/tvm_ffi/cpp/dtype.py
def dtype_to_cpp_type(dt: dtype) -> str:
    """Map tvm_ffi dtype to C++ type name."""
    # Interacts with: DLDataType, tvm_ffi._dtype
    ...
```

## Impact
- API: New `dtype_trait<T>` C++ template and Python dtype mapping
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Type traits (`0005-type-traits-protocol`)

Design elements this commit produces:
- `extra/dtype.h` compile-time dtype traits
- `cpp/dtype.py` Python dtype mapping

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
