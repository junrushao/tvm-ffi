---
author: "Junru Shao"
subject: "Add package generation with --init-* flags to stubgen"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "python/tvm_ffi"
  - "build/packaging"
---
# Add package generation with --init-* flags to stubgen

## TL;DR
- Adds `--init-imports`, `--init-register`, `--init-ffi-api` flags to stubgen CLI for generating package boilerplate
- Adds `lib_state.py` module for tracking library state during generation
- Major refactor of `cli.py` and `codegen.py`

## Key Exports
```python
# tvm-ffi-stubgen CLI new flags:
# --init-imports: generate __init__.py with proper imports
# --init-register: generate object registration code
# --init-ffi-api: generate _ffi_api.py stubs
# Interacts with: stubgen pipeline (analysis -> codegen -> file_utils)
```

## Impact
- API: New stubgen `--init-*` flags for package generation
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Python bindings (`0012-python-bindings`): stubgen pipeline

Design elements this commit produces:
- Package generation mode for stubgen

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
