---
author: "Guan-Ming (Wesley) Chiu"
subject: "Add find and substr methods to String"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/containers"
---
# Add find and substr methods to String

## TL;DR
- Adds `String::find(substr)` and `String::substr(pos, count)` methods
- Adds C++ and Python test coverage

## Key Exports
```python
class String:
    def find(self, substr: str, pos: int = 0) -> int:
        """Find first occurrence of substr. Returns -1 if not found.
        # Interacts with: std::string_view::find
        """
        ...
    def substr(self, pos: int, count: int = -1) -> str:
        """Extract substring.
        # Invariant: pos must be <= length
        """
        ...
```

## Impact
- API: New `find` and `substr` methods on String
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- Containers (`0006-containers`): String

Design elements this commit produces:
- `String::find` and `String::substr` methods

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
