# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The name oscillation (TVMFFIEnvSetStream → TVMFFIEnvSetCurrentStream → back to TVMFFIEnvSetStream across 3 commits) made it hard to describe a "stable" rename without cross-referencing other commits. Current template has no field for "transient rename" status.
- **Template gap**: A `net_effect` or `superseded_by` frontmatter field would help flag commits whose ABI impact is cancelled by a subsequent commit.
- **Wasted effort**: None.
- **Suggested skill change**: None — the Reflection section handled the oscillation note adequately.
