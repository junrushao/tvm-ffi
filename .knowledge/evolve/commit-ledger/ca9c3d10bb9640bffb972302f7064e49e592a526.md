# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 checkout failure (sandbox lock restriction) required falling back to `git show` from main repo. The procedure has no documented fallback for this case; a note that `git show $COMMIT` from `$REPO_MAIN` is a valid read-only alternative when checkout is blocked would reduce uncertainty.
- **Template gap**: None — the template handled the ABI mutation story (Key Exports with old→new annotations, Impact section for breaking changes) well.
- **Wasted effort**: Step 3.6 cross-layer check was lightweight here because the Cython changes were mechanical renames with no logic delta; the procedure correctly let this pass quickly.
- **Suggested skill change**: Add a fallback note in Step 0.3: "If `git checkout` fails due to filesystem restrictions, use `git -C $REPO_MAIN show $COMMIT -- <file>` to read diff content. The run may still succeed read-only; log the workaround in $LOGFILE."
