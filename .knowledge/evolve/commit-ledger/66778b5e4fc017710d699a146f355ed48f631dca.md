# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 checkout fails in sandbox environment due to inability to create index.lock. The workaround (using `git -C $REPO_MAIN show`) works but the procedure doesn't mention this fallback pattern.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 (cross-layer consistency) was a no-op for this ci-infra commit; the per-shape skip table correctly guided skipping this.
- **Suggested skill change**: Add a note in Step 0.3 that if `git checkout` fails with a lock error (sandbox restriction), all diff analysis can proceed via `git -C $REPO_MAIN show $COMMIT` without requiring worktree checkout — since the diff is identical regardless.
