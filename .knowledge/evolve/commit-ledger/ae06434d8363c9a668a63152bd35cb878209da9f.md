# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None — the standard shape path fit this small breaking-refactor commit well.
- **Template gap**: None.
- **Wasted effort**: Step 3.6 (Usage Examples) for a pure refactor commit produces a before/after snippet that is almost identical to the Key Exports section. The two sections feel redundant for pure signature-swap commits; one before/after block would suffice.
- **Suggested skill change**: For `refactor` type commits where the sole change is a macro/function signature swap, allow merging Key Exports and Usage Examples into a single "Before/After" block to avoid duplication.
