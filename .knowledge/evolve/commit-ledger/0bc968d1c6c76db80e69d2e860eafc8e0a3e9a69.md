# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification: the `formatting` shape says "skip Steps 3-4 entirely" but this commit also deletes two non-trivial shell scripts and upgrades tooling config. The `chore` shape has no explicit fast-path. There is no shape for "tooling-upgrade" that combines config changes with mechanical code updates — the closest is `chore` which falls through to `standard`. A `tooling` shape with a lighter-weight path (like `ci-infra`) would fit better.
- **Template gap**: None.
- **Wasted effort**: Steps 3.3 (breaking changes) and 3.4 (cross-layer consistency) required explicit reasoning to reach "N/A" — both could be fast-skipped by a rule: if commit_type=chore and diff is all docstring/import/style changes, auto-skip both.
- **Suggested skill change**: Add a `tooling` shape to the shape table (detection: pyproject.toml / .pre-commit-config.yaml / ruff/black/isort config + mechanical code application, no logic changes) with the same skip table as `chore`: skip 3.1–3.7, abbreviated template. This avoids mis-classifying as `standard` and paying for full Steps 3–4.
