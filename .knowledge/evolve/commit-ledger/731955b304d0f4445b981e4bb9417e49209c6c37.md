# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.6 (Usage Examples) asks for "one 5–15 line code snippet per produced design element." For refactor commits that produce no design elements, this guidance is slightly misfit — it is helpful to show the before/after pattern instead, which the procedure allows but only mentions as a secondary option. The primary framing should more clearly foreground the before/after form for refactor+standard commits.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 (Breaking changes) required checking — the additive-only fast-skip did not apply because lines were deleted, but the answer was trivially "none" since the changed files are standalone examples with no importers. A fast-skip rule for "standalone example scripts with no importers" would save a step.
- **Suggested skill change**: Add an explicit fast-skip to Step 3.3: "If all changed files are in `examples/` with no importers detected by grep, write 'N/A — standalone example' and skip."
