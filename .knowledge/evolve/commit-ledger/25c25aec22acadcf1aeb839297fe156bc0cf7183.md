---
author: "Tianqi Chen"
subject: "Introduce TVMFFIHandleInitOnce for thread-safe singleton initialization"
commit_shape: "standard"
commit_type: "feature"
potential_duplicate: ""
has_design_updates: false
scope:
  - "ffi/c-api"
  - "ffi/extra"
---
# Introduce TVMFFIHandleInitOnce for thread-safe singleton initialization

## TL;DR
- Adds `TVMFFIHandleInitOnce` C API for thread-safe lazy singleton initialization
- Implements via `src/ffi/init_once.cc` with comprehensive test coverage (288 LOC tests)

## Key Exports
```python
# C API: TVMFFIHandleInitOnce
def TVMFFIHandleInitOnce(
    handle: Pointer[void_p],
    init_func: Callable[[Pointer[void_p]], int],
) -> int:
    """Thread-safe lazy singleton initialization.
    # Interacts with: C ABI (0007), used by env context singletons
    # Invariant: init_func called exactly once, even under concurrent access
    """
    ...
```

## Impact
- API: New `TVMFFIHandleInitOnce` C API
- Behavioral: None
- Flags/config: None
- Data formats/schemas: None

## Design Elements
Design elements this commit consumes:
- C ABI (`0007-c-abi`)

Design elements this commit produces:
- `TVMFFIHandleInitOnce` thread-safe singleton init primitive

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
