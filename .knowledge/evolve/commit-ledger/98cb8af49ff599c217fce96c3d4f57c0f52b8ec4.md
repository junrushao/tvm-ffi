# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification: this commit is `standard` but also has a `rename` secondary concern (`type_acenstors` → `type_ancestors`). The composite shape rules say to pick the primary shape and apply secondary fast-paths where non-conflicting. For `standard` + `rename`, this worked fine, but the procedure doesn't explicitly describe how to annotate both in Key Exports (I used old→new comments inline). Adding an explicit "for composite standard+rename: include rename mapping table inline in Key Exports" would reduce ambiguity.
- **Template gap**: None. The before→after comment pattern in Key Exports covered the rename adequately.
- **Wasted effort**: None. All steps produced useful output for this commit.
- **Suggested skill change**: None — skill fit well for a cross-layer feature+rename commit.
