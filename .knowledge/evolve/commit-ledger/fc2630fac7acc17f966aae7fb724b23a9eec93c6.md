# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None — the standard shape path and per-shape skip table worked well for this refactor commit.
- **Template gap**: The "before/after mapping table" note in the template TL;DR is useful for rename commits; for refactors that change calling convention (not name), a "before/after calling convention" template hint would be more precise. A one-liner note like "For refactor shape: show old vs. new API call pattern in TL;DR" would help.
- **Wasted effort**: None.
- **Suggested skill change**: In Step 2.2 shape table, the `rename` row says "Generate rename mapping table." For pure refactors that change calling convention without renaming, a similar prompt ("For refactor type: show old→new calling convention in Key Exports") would make the step more actionable without requiring a separate shape classification.
