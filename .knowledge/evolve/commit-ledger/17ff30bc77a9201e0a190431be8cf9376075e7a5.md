# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 checkout is blocked by the sandbox in certain environments (`index.lock` write denied). The procedure has no explicit fallback guidance for this case — it only says "retry the checkout." A note that `git show $COMMIT` against `$REPO_MAIN` is a valid fallback for read-only analysis when the checkout is sandbox-blocked would reduce ambiguity.
- **Template gap**: None.
- **Wasted effort**: Steps 3.3 and 3.4 skip table for `docs-only` shape is correct and saved significant analysis time. The shape classification was unambiguous for this commit.
- **Suggested skill change**: Add a fallback note to Step 0.3: "If the sandbox blocks `index.lock` creation, all analysis can proceed via `git -C $REPO_MAIN show $COMMIT` without modifying worktree state. Proceed with the analysis steps; note the skip in the log."
