# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The `trivial` shape threshold (<5 files, <50 LOC) doesn't cover pure-tooling migrations that touch many files cosmetically. A `chore-migration` shape or relaxed trivial threshold for config-only commits would avoid forcing `standard` shape on these.
- **Template gap**: None.
- **Wasted effort**: Steps 3.3–3.6 (breaking changes, cross-layer, exports, examples) were all trivially "None" for this tooling migration; the shape detection correctly routes to standard but there's no fast-path for large-footprint tooling chores.
- **Suggested skill change**: Add a `tooling` shape detection signal: "only config files (pyproject.toml, .pre-commit-config.yaml, CI yaml) and annotation-only Python changes changed" → abbreviated template path identical to `trivial`.
