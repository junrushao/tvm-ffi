# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 assumes `git checkout` in `$REPO_WORKTREE` is always possible, but sandbox write-restrictions on `.git/` can block it. The procedure has no fallback for read-only diff access via `git show` without a checkout.
- **Template gap**: None.
- **Wasted effort**: Steps 3.1–3.7 were correctly skipped for trivial shape. No wasted effort.
- **Suggested skill change**: Add a fallback note to Step 0.3: "If `git checkout` fails due to filesystem permissions (e.g., sandbox), use `git -C $REPO_MAIN show --format="" $COMMIT -- <file>` for diff access without requiring HEAD to move. This is safe for read-only analysis of any shape."
