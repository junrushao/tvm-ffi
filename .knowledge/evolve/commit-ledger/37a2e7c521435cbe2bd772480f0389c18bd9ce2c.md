# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The composite-shape guidance ("pick primary shape, note secondary") worked well for this rename+feature commit. The per-shape Step 3 skip table correctly directed me to retain Key Exports despite the rename primary shape. No friction.
- **Template gap**: The rename mapping table placement (in TL;DR) worked, but commits with both a rename table AND meaningful new API exports produce a slightly crowded TL;DR. A dedicated "Rename Mapping" section below TL;DR would be cleaner than embedding it in the summary bullets.
- **Wasted effort**: None — shape-aware depth matched the commit's actual complexity well.
- **Suggested skill change**: For composite `rename+feature` shapes, consider adding "Rename Mapping" as an optional explicit section between TL;DR and Key Exports, rather than relying on the note "For rename commits, include a before/after mapping table here" in TL;DR prose. This would make tables easier to skim and keep TL;DR focused on the behavioral change narrative.
