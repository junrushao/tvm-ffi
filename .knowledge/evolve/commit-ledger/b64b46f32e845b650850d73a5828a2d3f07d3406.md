# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification: `multi-concern` threshold is ">3 logically separable changes each >=10% LOC". This commit has exactly 2 concerns (free-threaded build + OpaqueObject bugfix). The table says multi-concern requires >3, but 2 significant orthogonal concerns clearly warrant separate sub-headings. The threshold of >3 is too high — a 2-concern commit can be just as hard to analyze as a 4-concern one.
- **Template gap**: None.
- **Wasted effort**: None — both concerns needed full Step 3 treatment.
- **Suggested skill change**: Lower `multi-concern` threshold from ">3 logically separable changes" to ">=2 logically separable changes where each contributes >=10% of total LOC and the changes are orthogonal in purpose." This would let 2-concern commits get proper sub-heading treatment without forcing them into `standard` shape.
