# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.1 type classification — commit message says REFACTOR but the diff is clearly a bugfix (ABI fix) + feature (new flag). The skill correctly resolves this via diff-message alignment check in Step 2.2, but the type field has no "primary+secondary" split equivalent to shape composite handling. Ended up noting discrepancy in TL;DR prose.
- **Template gap**: None.
- **Wasted effort**: None significant; commit was small (4 files, 20 LOC) and the standard shape path fit well.
- **Suggested skill change**: In Step 2.1, explicitly allow `commit_type` to be a primary/secondary pair (like `commit_shape` composites), especially when the commit message tag and diff evidence disagree. E.g., `commit_type: bugfix+feature` rather than forcing one type into `secondary_commit_type` which is described as "optional secondary type if hybrid commit" — it could be clearer that this is the intended slot for message/diff disagreements.
