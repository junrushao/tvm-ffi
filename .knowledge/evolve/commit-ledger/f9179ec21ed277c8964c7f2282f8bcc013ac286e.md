# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: None.
- **Wasted effort**: Steps 3.3–3.6 are formally skipped for `trivial` shape per the skip table, but the before/after usage example was still the most useful content for this commit. The template's "None" guidance for trivial commits is slightly too strict for bugfix-trivial commits where a 2-line before/after is highly informative.
- **Suggested skill change**: For `trivial` commits with `commit_type: bugfix`, allow (but do not require) a compact before/after snippet in Usage Examples rather than mandating "None". This preserves brevity while capturing the corrective intent.
