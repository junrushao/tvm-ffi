# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification was awkward — this commit is a single-file bugfix to docs build infrastructure. It doesn't fit `trivial` (which requires "no functional change") or `ci-infra` (which requires CI/CD files). It ended up as `standard` despite being a very simple fix. A `docs-infra` shape or broadening `ci-infra` to include `docs/conf.py` would avoid this ambiguity.
- **Template gap**: None.
- **Wasted effort**: Steps 3.3-3.6 (breaking changes, cross-layer, invariants, key exports, usage examples) all produced "None" for this simple docs build fix. The standard shape forced walking through all substeps.
- **Suggested skill change**: Broaden `ci-infra` shape detection to include docs build configuration files (`docs/conf.py`, `docs/Makefile`, `.readthedocs.yml`) so they get the streamlined ci-infra treatment instead of full standard analysis.
