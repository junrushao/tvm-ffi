# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None — the `docs-only` shape path (skip Steps 3.3, 3.4, 3.5; retain 3.1, 3.2, 3.6, 3.7) was clean for this purely documentary commit.
- **Template gap**: None.
- **Wasted effort**: The staleness check (Step 3.7) required grepping `.knowledge/` for the old `build_from_source.md` filename, which only surfaced historical commit ledger entries (not design records). The effort was minor but the result was only `low`-severity noise. For `docs-only` commits that rename/replace docs, the staleness check applies only to `.knowledge/design-records/`, not to prior commit ledger entries — the procedure could make this distinction explicit.
- **Suggested skill change**: In Step 3.7, add a note that for `docs-only` shape, only scan `design-records/` for stale references (not prior commit ledgers), since historical ledger entries referencing old doc filenames are not actionable stale references.
