# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The "diff-message alignment check" in Step 2.2 was the most useful step for this commit — the message title ("Phase out getattr based attribute handling") describes a goal at the PR/series level, not this individual commit's diff. The current guidance handles this case correctly (scope analysis to actual diff) but the ledger template has no dedicated field to flag title vs. diff misalignment for downstream consumers. Worth considering a `title_scope_mismatch: true/false` frontmatter key.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 (breaking changes) — the additive-only fast-skip applied immediately (no deletions, no modified signatures). No friction, just boilerplate mental check.
- **Suggested skill change**: None — skill fit well for this small feature commit.
