---
author: "junru.shao"
subject: "Add cmake-format and cmake-lint pre-commit hooks with full CMake reformatting"
commit_shape: "broad-but-shallow"
commit_type: "chore"
potential_duplicate: ""
has_design_updates: false
scope:
  - "build/cmake"
  - "ci/pre-commit"
---
# Add cmake-format and cmake-lint pre-commit hooks with full CMake reformatting
## TL;DR
- Adds `cmake-format` (v0.6.13) and `cmake-lint` hooks to `.pre-commit-config.yaml`.
- Introduces `.cmake-format.json` configuration (100-col, 2-space indent, canonical command case, dangle parens).
- Reformats all 10 CMakeLists.txt / `.cmake` files to comply with the new formatter; renames internal variables to follow cmake-lint conventions and adds docstring blocks to CMake functions/macros.

## Key Exports
None

## Impact
- API: none
- Behavioral: none (no functional CMake logic changes; only formatting and variable renames)
- Flags/config: new `.cmake-format.json` governs cmake-format/cmake-lint rules; new pre-commit hooks `cmake-format` and `cmake-lint` will now run on all `.cmake`/`CMakeLists.txt` files
- Data formats/schemas: none

## Design Elements
Design elements this commit consumes: none

Design elements this commit produces: none (build tooling only)

## Usage Examples
None

## Reflection

### Design docs to write or update
None

### Stale knowledge references
None
