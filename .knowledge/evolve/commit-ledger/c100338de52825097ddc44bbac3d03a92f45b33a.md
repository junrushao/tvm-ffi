# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: The shape classification table does not have a clear slot for a "docs + working example + minor ABI fix" commit. `docs-only` would skip the ABI analysis (Step 3.3) which found a real ABI change (v_char32 removal). Had to classify as `standard` even though docs was the primary concern. A `docs-with-code` composite shape or a note that docs commits touching headers should be promoted to `standard` would help.
- **Template gap**: None — the template handled this shape adequately.
- **Wasted effort**: Step 3.4 cross-layer consistency check was not very productive here; the C example is intentionally low-level and the Python/C layers are intentionally decoupled.
- **Suggested skill change**: In the shape table, clarify that `docs-only` applies only when *no* code files outside `docs/` and `examples/` are changed. If `include/` or `python/` files are also changed, classify as `standard` regardless of doc LOC dominance.
