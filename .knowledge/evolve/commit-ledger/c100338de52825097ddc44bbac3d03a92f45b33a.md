# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None.
- **Template gap**: None.
- **Wasted effort**: Step 2.2 shape classification required deliberation between `docs-only` and `standard` because the commit is predominantly docs/examples but includes a real ABI change (`v_char32` removal) and tooling change (`--cflags`). The heuristic "Only docs/md/rst/examples changed" does not account for commits where examples are the main content but a public header also changes.
- **Suggested skill change**: In Step 2.2, add a note under `docs-only` shape: "If any changed file is a public header with functional changes (not just comment edits), classify as `standard` even if the majority of LOC is documentation."
