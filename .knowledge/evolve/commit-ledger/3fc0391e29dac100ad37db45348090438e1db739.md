# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 0.3 checkout required a sandbox permission workaround (`chmod u+w` on the worktree index file). This is an environment-specific issue, not a procedure gap.
- **Template gap**: None — the rename mapping table and before/after usage examples fit the rename shape well.
- **Wasted effort**: Step 3.3 (breaking changes) and Step 3.6 (usage examples) partially overlap for rename commits — the before/after example essentially *is* the breaking-change documentation. Could be merged for rename shape.
- **Suggested skill change**: For `rename` shape, explicitly allow combining Step 3.3 and Step 3.6 output into a single "Before/After" section to avoid duplicate before/after patterns written in two separate steps.
