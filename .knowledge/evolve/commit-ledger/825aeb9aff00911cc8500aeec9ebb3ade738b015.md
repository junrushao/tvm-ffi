# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 rename shape detection — "additions ~ deletions" signal fits the core file (76 adds / 60 deletes) but the commit also adds a new parameter (`build_directory`) and changes mapping semantics, making pure rename classification incorrect. The boundary between `rename` and `standard` is unclear for API-redesign commits that happen to involve renames as the primary mechanism.
- **Template gap**: None.
- **Wasted effort**: Step 3.4 cross-layer consistency was quick to skip (single Python layer), but the skip decision itself required re-reading the step. The fast-skip signal could be in the shape table rather than embedded in the step prose.
- **Suggested skill change**: In Step 2.2, add a clarifying note under `rename` shape: "If the commit also changes parameter semantics or adds new parameters (not just mechanical renames), classify as `standard` with rename secondary concern and include the mapping table in TL;DR."
