# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 2.2 shape classification required choosing between `docs-only` and `rename` (secondary). The composite-shape guidance handled this cleanly, but the detection signal for `docs-only` says "only docs/ changed" while this commit also changed `tests/cpp/` and `src/ffi/extra/testing.cc`. A note that companion test/example files added alongside docs still qualify as `docs-only` primary shape would reduce ambiguity.
- **Template gap**: None.
- **Wasted effort**: Step 3.3 breaking-change analysis was mechanical to dismiss (additive only) but still required explicit evaluation. For `docs-only` commits the fast-skip text could explicitly say "skip Step 3.3".
- **Suggested skill change**: In Step 2.2 `docs-only` detection signal, add: "companion example/test files added alongside docs do not change the shape classification."
