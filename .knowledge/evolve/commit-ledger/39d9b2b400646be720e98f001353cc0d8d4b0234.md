# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: None — the standard shape path fit this commit well. The secondary refactor concern (helper relocation) was naturally handled as a TL;DR note.
- **Template gap**: The template has no dedicated place to document "known TypeAttr column registry" updates, which are a recurring pattern across commits. A brief "TypeAttr Columns Added/Changed" row under Impact would help downstream distill track the growing column inventory.
- **Wasted effort**: Step 3.4 cross-layer fast-skip was quick (single C++ layer); no wasted effort there.
- **Suggested skill change**: Add a `TypeAttr columns` row to the Impact section in `template-commit-ledger.md` (alongside API/Behavioral/Flags/Data formats) for commits that add or modify TypeAttr column names, since these columns form a cross-cutting API surface queried by Python, Rust, and C++ without being listed in any single header.
