# Skill /commit-ledger - Self-Evolution
- **Procedure friction**: Step 3.4 cross-layer consistency check required an explicit single-layer skip; the fast-skip rule is clear but the step numbering is slightly confusing (Step 3.4 appears twice in the spec — once for "Cross-layer consistency" and once for "Invariants"). The duplication is likely a copy-paste error in SKILL.md.
- **Template gap**: None — the template handled the private-field-rename aspect well under Impact: "Data formats/schemas: None" and the Stale knowledge references subsection.
- **Wasted effort**: The "Diff-message alignment check" in Step 2.2 adds little value for small, focused commits where the subject line exactly matches the diff scope.
- **Suggested skill change**: Fix the duplicate Step 3.4 label in SKILL.md: rename the "Cross-layer consistency" step to 3.4a and the "Invariants" step to 3.4b (or renumber). The current ambiguity required re-reading to understand which skip rules applied.
