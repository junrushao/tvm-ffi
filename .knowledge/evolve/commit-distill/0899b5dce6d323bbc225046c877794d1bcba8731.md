# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None.
- **Upstream skill gap**: Ledger frontmatter `scope` is a YAML list but the triage step assumes single-line grep extraction. The ledger template could document that `scope` is always a multi-line YAML list so triage scripts can parse it reliably.
- **Quality bar friction**: None -- the batch was large (57 commits) but the active/trivial split made it manageable.
- **Procedure friction**: Step 1.5 batch triage says "read only the YAML frontmatter" but the frontmatter format (multi-line YAML lists for `scope`) makes shell-based extraction fragile. Consider suggesting a frontmatter parser utility.
- **Suggested skill change**: None -- skill fit well.
