# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None.
- **Upstream skill gap**: Ledgers for chore/CI commits (13 of 38) correctly had minimal Key Exports, so triage worked well. Dtype converter ledger (`d77606a`) had excellent detail enabling direct design doc updates without source re-derivation.
- **Quality bar friction**: None -- the batch had enough feature/refactor commits to meet all quality bars naturally.
- **Procedure friction**: Step 0.3 checkout is problematic when the worktree created by the agent framework is NOT a proper git linked worktree (just a subdirectory). The checkout detached the main repo HEAD. Consider adding a pre-check: verify `git worktree list` shows REPO_WORKTREE as a separate entry before attempting checkout.
- **Suggested skill change**: Add to Step 0.3: "Before checkout, verify REPO_WORKTREE appears in `git worktree list` output. If not, skip checkout and note that source will be read from HEAD."
