# Commit-Distill: Group 15 (27 commits)
## Representative SHA: 34ad335202b27cc802441675571e07ec161556b2
## Date range: 2025-12-21 to 2026-01-08

### Summary
No commits in this group have `has_design_updates: true`. The group primarily contains:

1. **Dynamic method overloading** (`reflection/overload.h`): Runtime method dispatch based on argument types for FFI objects. 501 LOC. Extends Reflection (0008).

2. **C++ dtype_trait** (`extra/dtype.h`): Compile-time dtype queries via `dtype_trait<T>::dtype()`, plus Python-side dtype mapping.

3. **Convenience header** (`tvm/ffi/tvm_ffi.h`): Single-include header for all core FFI headers.

4. **Container improvements**: Array `__contains__`, `__bool__` for Array/Map, String `find`/`substr`, negative index bounds checking.

5. **Cubin launcher refactor**: Multiple embedding strategies (cpp_embed, embed_with_tvm_ffi), improved CMake infrastructure.

6. **TypeTraits overflow check**: int64-to-uint64 overflow detection in `TypeTraits<Int>::CopyToAnyView`.

7. **Documentation**: Extensive new docs for Python packaging (python_packaging.rst), Tensor/DLPack (tensor.rst), Any/AnyView (any.rst), C++ toolchain (cpp_tooling.rst), release process.

8. **Build fixes**: dl/pthread linking, allocator return value fix, CMake target renames.

### Design Record Updates
None required -- all changes are incremental additions to existing subsystems.
