# Commit-Distill: Group 14 (35 commits)
## Representative SHA: a754afe248c0a29509236b5e0c8d2e30a26f7ffa
## Date range: 2025-11-30 to 2025-12-21

### Summary
No commits in this group have `has_design_updates: true`. The group primarily contains:

1. **STL Interop** (`extra/stl.h`): New header providing automatic TypeTraits conversions between C++ STL containers (vector, map, tuple, string, optional) and FFI types. Consumes TypeTraits (0005) and Containers (0006).

2. **kwargs_wrapper utility**: Python-side wrapper converting positional FFI functions to accept keyword arguments via TypeSchema.

3. **Generic value protocol**: Refactored Cython argument dispatch via `tvm_ffi_python_helpers.h` for improved DLPack/stream performance paths.

4. **TVMFFIHandleInitOnce**: Thread-safe lazy singleton initialization C API primitive.

5. **Tensor CreateView**: New API for creating strided tensor views with shared data pointer.

6. **keep_module_alive**: New parameter on `load_module`/`cpp.load` to prevent premature module unload.

7. **load_lib_module**: Helper for loading package-bundled shared libraries.

8. **Map.get performance fix**: Dedicated `MapGetWithDefault` C++ function eliminates double lookup.

9. **Package tooling**: stubgen `--init-*` flags for package generation, `tvm_ffi_configure_target`/`tvm_ffi_install` CMake helpers.

10. **DLPack improvements**: Various DLPack compatibility fixes, importlib.metadata-based DSO discovery.

### Design Record Updates
None required -- all changes are incremental additions to existing subsystems.
