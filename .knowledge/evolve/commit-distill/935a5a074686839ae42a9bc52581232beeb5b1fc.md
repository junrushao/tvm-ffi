# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None — the design doc template handled all artifact types in this batch (struct layout updates, macro additions, class splits, semantic renames).
- **Upstream skill gap**: Ledgers captured rename direction (type_schema → metadata) correctly. The `TVM_FFI_CHECK` arg-order consolidation cluster was clear from the two ledgers covering commits 327e8cc6 and ae06434d — no re-derivation from source needed.
- **Quality bar friction**: None — all 10 updated design records already had usage examples, invariants, failure modes, and extension points from prior runs. Surgical updates fit naturally without needing to restructure sections.
- **Procedure friction**: Step 0.3 sandbox restriction for `git checkout` on the worktree `.git` index lock recurred (same issue seen in prior batches). Skill should note this as a known pattern requiring `dangerouslyDisableSandbox:true` specifically for the checkout step, not just generically when a command fails.
- **Suggested skill change**: Add a note in Step 0.3 that `git checkout` in a linked worktree reliably fails under sandbox due to index lock file access — auto-retry with `dangerouslyDisableSandbox:true` without waiting for the error.
