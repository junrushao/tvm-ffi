# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None — design doc and decision record templates fit all artifact types in this batch well.
- **Upstream skill gap**: Ledger for 777cf8d listed `0007-containers` as a stale reference (medium severity) in its Reflection section, but the skill's planning step doesn't explicitly include "check all cross-referenced records for staleness and schedule updates". The 0007 update was caught only via manual reviewer judgment, not via a structured staleness-processing step. Adding a "Step 2.1: process all Stale Knowledge References from active-pool ledgers and plan micro-updates" would make this systematic.
- **Quality bar friction**: None — requirements (invariant, trade-off, failure mode, extension point, usage example, decision record) were all achievable for this batch.
- **Procedure friction**: LAST_COMMIT tiebreaking: when all commits share identical committer timestamps, the procedure says "use git topology (parent order in `git rev-list`)". However the commits are on divergent feature branches not accessible from HEAD, so `git rev-list HEAD` doesn't yield them. Fallback to PR number ordering worked but isn't explicitly described in the procedure.
- **Suggested skill change**: In Step 2, add a micro-step: "For each stale reference listed in active-pool ledgers' Reflection sections (low/medium/high severity), plan a surgical patch to the referenced design record as part of the same run." This prevents silently leaving medium-severity stale references unfixed.
