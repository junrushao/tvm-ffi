# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None.
- **Upstream skill gap**: Ledgers for rename commits (e.g., `0966c368` type key rename) listed no produced design elements despite creating a new naming convention (`ffi.*` prefix). The triage classified them as active but the "produces: none" signal was misleading for distillation. Ledgers for renames should note the convention change as a produced invariant.
- **Quality bar friction**: None.
- **Procedure friction**: For 40-commit batches with cascading renames (TypeTraits method names appear in 6+ design records), the surgical edit approach hit whitespace/context mismatches. Full-file rewrites were more reliable for heavily-modified documents. Consider advising full rewrites when >30% of a design record's content changes.
- **Suggested skill change**: None -- skill fit well for this batch size.
