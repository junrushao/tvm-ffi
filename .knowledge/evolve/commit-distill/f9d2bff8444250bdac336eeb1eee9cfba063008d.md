# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None — the design doc template worked well for both updates and new creation. The unified design-doc-plus-decision-record format handled the function system migration narrative cleanly.
- **Upstream skill gap**: Ledger frontmatter `scope` field was always `"-"` (dash), making automated scope-based triage impossible. Had to read full frontmatter to partition commits. Suggest `/commit-ledger` populate `scope` from the changed directories/subsystems.
- **Quality bar friction**: None — the 40-commit batch had enough architectural substance to meet all quality bars (invariants, trade-offs, failure modes, usage examples, evolution timelines).
- **Procedure friction**: Step 1.5 batch triage says "read only YAML frontmatter" but the `scope` field being empty forced reading more content anyway. The revert-pair detection (5a4038fc/9a1d41a4) worked well — net-zero effect correctly identified.
- **Suggested skill change**: Add a "scope auto-derivation" rule to `/commit-ledger` so that the `scope` YAML field is always populated (e.g., from the first path component of changed files), enabling the batch triage to work on frontmatter alone.
