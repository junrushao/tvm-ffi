# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None.
- **Upstream skill gap**: None — ledger inputs were sufficient for all 32 commits. Frontmatter triage was effective at partitioning the batch.
- **Quality bar friction**: None — all updated records meet the reconstructibility bar with invariants, trade-offs, failure modes, and usage examples.
- **Procedure friction**: When a previous run has already partially applied changes for the same commit batch, the skill lacks guidance on how to detect and handle this cleanly. Currently relies on manual grep verification.
- **Suggested skill change**: Add a Step 4 pre-check: before editing a design record, compare its existing content against planned changes (e.g., grep for key commit SHAs) and skip already-applied edits. This would make re-runs truly idempotent without manual verification.
