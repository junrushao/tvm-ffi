# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None — the design-doc template handled all three artifact types (new language-binding doc, container type addition, invariant fix) cleanly. The "Decision Record" section was not needed for any of the three artifacts since all were uncontested additions/fixes.
- **Upstream skill gap**: 70927743 (memory leak bugfix) had `has_design_updates: false` but the Cython ownership invariant it establishes is architecturally significant for any Cython binding author. `/commit-ledger` could add a `has_ownership_invariant` flag so distill can detect these without reading the full ledger.
- **Quality bar friction**: None — all three quality bar requirements (invariant, trade-off, failure mode, usage example) were satisfied naturally by the evidence.
- **Procedure friction**: The `storage_enabled=false` pattern in 0008-type-traits required updating both 0007 (TensorView pseudocode) and 0008 (pattern documentation) in a coordinated way. The skill instructions don't explicitly guide cross-doc synchronization when a feature touches two existing docs; adding "check related docs for cross-references" to Step 4 would help.
- **Suggested skill change**: In Step 1.5 Batch Triage, add a note: "Even `has_design_updates: false` bugfix commits may establish ownership invariants worth noting; scan Key Exports section for `# Invariant:` annotations before classifying as trivial."
