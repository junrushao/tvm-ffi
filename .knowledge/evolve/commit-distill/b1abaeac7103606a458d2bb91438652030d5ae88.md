# Skill /commit-distill - Self-Evolution
- **Template mismatch**: The design doc template assumes a single cohesive design concern per file, but the 6b39efbf "consolidation" commit spans 4 distinct operations (repr/deepcopy/hash/compare) plus auto-init — creating tension between splitting into 2 records vs. one large one. The current single-record approach in 0031-dataclass-ops works but stretches the template's problem statement structure.
- **Upstream skill gap**: Ledger for commit 6b39efbf correctly flagged all 4 concerns and suggested both "extend 0030-repr-print" and "create 0031". Clear guidance on when to create a new record vs. extend an existing one in the ledger reflection would reduce the planner's decision load.
- **Quality bar friction**: The "Evolution Timeline when >=3 commits" requirement sometimes generates thin timelines (e.g., 0031 has only 4 meaningful commits over a narrow window). A "minimum meaningful divergence" criterion would be clearer than a raw count.
- **Procedure friction**: None — procedure was clear and well-suited to this batch. The batch triage (trivial vs. active pool) worked well, with ~8 trivial commits correctly skipped.
- **Suggested skill change**: Add a note in Step 2 (Planner) that when a "consolidation" commit merges 2+ prior independent subsystems, the preferred artifact pattern is a new design record for the merged system plus brief updates to each prior record pointing to the new one, not replacing existing records.
