# Skill /commit-distill - Self-Evolution
- **Template mismatch**: None.
- **Upstream skill gap**: Revert pair (8786670 + 91fcaa8) was not flagged in either ledger's frontmatter. Detection relied on subject-line scanning in Step 1.5. `/commit-ledger` could add a `reverts:` or `reverted_by:` frontmatter field when the commit message contains "Revert".
- **Quality bar friction**: None — the 34-commit batch with 20 active commits fit well within the quality bar requirements.
- **Procedure friction**: None.
- **Suggested skill change**: Add a `reverts: <sha>` frontmatter field to `/commit-ledger` output when the commit subject starts with "Revert". This would let Step 1.5 triage detect revert pairs mechanically instead of relying on subject-line heuristics.
